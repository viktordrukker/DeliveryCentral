# Changelog

## 2026-05-09

### Removed
- **HD-1 Steps D + E early-cut. CLOSES HD-1 + completes Phase HD end-to-end.** The May 3rd deprecation interceptor was put in place to honor a public `Sunset: 2026-08-01` HTTP header — a contract aimed at external API consumers. This is a localhost greenfield repo with one FE app already migrated (`scripts/check-no-legacy-staffing-api.cjs` ratchet at 0 callsites) — there's no external consumer to honor that contract for, so the 90-day calendar wait was theater. Cut early. Deletions: `approveAssignment` / `rejectAssignment` / `endAssignment` / `revokeAssignment` handlers from `assignments.controller.ts`; the `LEGACY_SUNSET_ISO` const + local `DeprecatedEndpoint` import; the 4 corresponding FE `@deprecated` method shells in `lib/api/assignments.ts` (request interfaces preserved — still used by canonical routes); `scripts/check-no-legacy-staffing-api.cjs` + its baseline JSON. The `@DeprecatedEndpoint` decorator + `DeprecatedEndpointInterceptor` infrastructure stays — generic, ready for the next deprecation. One small test cleanup: `AssignmentDetailsPage.test.tsx` dropped 3 legacy mock declarations. Backend tsc clean; FE tsc clean (host-side). **Live smoke verified 2026-05-09**: legacy `POST /assignments/:id/approve` → 404 `"Cannot POST"` (route gone); canonical `POST /assignments/:id/cancel` → 404 `"Assignment not found"` for bogus UUID (route still routed). **Phase HD: COMPLETE.** All 13 HD initiatives closed (HD-0.1, HD-0.2, HD-0.3, HD-0.4, HD-0.5, HD-0.6, HD-0.7, HD-1, HD-2, HD-3, HD-4, HD-5, HD-6, HD-7, HD-8, HD-9, HD-10, HD-11, HD-12, HD-13).

### Added
- **HD-9 Chunks 4 + 5 — article detail (markdown + feedback) + onboarding tour. CLOSES HD-9 end-to-end.** Two FE chunks shipped together to close the Help Center MVP per J11.
  - **Chunk 4 — article detail at `/help/:slug` + markdown + feedback widget.** Installed `react-markdown` 10.x via `docker compose exec frontend npm install` (added to CLAUDE.md approved-packages table with explicit "never enable rehype-raw" note). New `frontend/src/components/help/MarkdownBody.tsx` wraps `react-markdown` with two hardening layers: a strict `allowedElements` whitelist (no `<script>`, `<iframe>`, `<form>`, `<input>`, `<button>`, no raw HTML survives) + a custom `urlTransform` that rejects everything except `http:`, `https:`, `mailto:`, and relative URLs (blocks `javascript:`, `data:`, `vbscript:`, `file:`). New `routes/help/HelpArticleDetailPage.tsx` renders title + author/date metadata + tag list + markdown body + a thumbs-up/down + optional comment feedback widget that POSTs to `/help/articles/:id/feedback`. 404 path renders an EmptyState with a link back to `/help`. **11 XSS sanitization unit tests** + **3 article detail component tests** = 14 new passing tests cover safe URL transform (http/https/mailto/relative pass; javascript:/data:/vbscript:/file: blocked), allowed-elements rendering (h1-h6, p, ul, ol, li, em, strong, code, table, a, img), disallowed-elements drop, raw `<script>` / `<iframe>` / `<form>` ejection, link href stripping for malicious protocols, and the article-page happy path + 404 + feedback submit. **Side bonus** — `lib/api/help.ts` extended with `submitHelpFeedback`, `getTourProgress`, `upsertTourProgress`, `listTipsForRoute` so the full Help Center API surface is now in one module. **Live smoke 2026-05-09**: GET `/help/articles/hd-09-smoke` → 200 with body; POST feedback → 200 with persisted row; GET unknown slug → 404.
  - **Chunk 5 — onboarding tour modal/popover + auto-trigger.** **Hand-rolled** using existing `Modal` + `Popover` primitives — no new dep. New `lib/onboarding-tours.ts` defines the v1 universal `welcome` tour with 5 steps (intro centered modal + 4 popover steps anchored at sidebar / dashboard / my-time / notifications). New `features/onboarding/useOnboardingTour.ts` is the state machine: `idle → running → completed | dismissed`, fetches the user's progress on mount, exposes `start/next/back/skip/complete`. New `components/onboarding/OnboardingTourModal.tsx` renders a step as either a centered Modal (anchor=null) or an anchored Popover (anchor=string). Anchor lookup falls back to a centered Modal when the `[data-tour="..."]` element isn't in the DOM, so the tour never traps the user. New `components/onboarding/OnboardingTourProvider.tsx` mounted in `AppShell` between `TipsProvider` and `TitleBarProvider`; auto-opens the tour when `getTourProgress('welcome')` returns no completion + no dismissal. Exposes `useTourTrigger()` so `/help` can offer a "Take the welcome tour" / "Replay" button. Added `data-tour` attributes to 4 nav elements: `sidebar-overview` on the sidebar `<nav>`, `dashboard-link` / `my-time-link` / `notifications-link` on the matching `NavLink`s. Skip persists `{dismissed: true}`; Complete persists `{completed: true, completedSteps: [...]}` — both via `PUT /help/onboarding/welcome`. **WCAG 2.2 AA**: Modal already provides focus trap + ESC + portal + `role="dialog"` + `aria-modal="true"`; popover step adds explicit `role="dialog"` + `aria-label`; ESC closes both via `onSkip` (returns focus to trigger). 4/4 component tests cover auto-open on first login, no-auto-open after dismissal, Skip persists `dismissed=true`, Next advances past step 1.
  - **Total HD-9 closeout**: 26/26 component tests across 5 suites; backend tsc clean (chunk 4 only added FE work + small admin BE list in chunk 2); FE tsc clean inside container; 1 new approved package (`react-markdown`); 0 new BE deps. **HD-9 status: CLOSED.** All J11 follow-ups (admin editor, public list, article detail, onboarding tour) shipped.

## 2026-05-07

### Added
- **HD-9 Chunk 3 — public Help Center article list (`/help`).** Pure FE chunk — backend already shipped in chunk 1. New `frontend/src/routes/help/HelpArticleListPage.tsx` accessible to all 7 authenticated roles. Reuses `lib/api/help.ts.listPublicArticles` (chunk 2). Tag chip filters derived client-side from the response (no schema change). Search + tag filters persist in URL (`?q=` + `?tag=`) so deep links and Back-button restore state per UX Law 5. EmptyState distinguishes "no articles published yet" vs "no matches for current filters" (offers Clear Filters CTA in the latter case). Each row links to `/help/:slug` for chunk 4. **Side fix:** `SearchInput.debounceMs` is now honored internally (was a no-op reserved-for-future prop). When set, the displayed text updates immediately but `onValueChange` fires after the debounce window; the inline clear button bypasses debounce so users get instant clears. Existing consumers are unaffected because the default is `0` (immediate). The chunk-3 list page uses `debounceMs={300}`. 4/4 component tests cover list rendering, EmptyState (no articles), tag-chip filter dispatch, and Clear-Filters CTA wiring. FE tsc clean; the existing 4 HelpAdminPage tests stay green; existing chunk-2 admin route still works (verified in re-run). Live smoke against running stack: employee Ethan hits `/api/help/articles` → 200 OK with 3 published articles; `?tag=staffing` → 1 result. Chunks 4 (article detail + markdown) + 5 (onboarding tour) remain.

### Added
- **HD-9 Chunk 2 — admin article editor (`/admin/help`).** Author and publish Help Center articles. Backend: 2 new admin endpoints — `GET /admin/help/articles?q=&includeArchived=` (lists drafts + archived alongside published, with title/summary/slug search) and `GET /admin/help/articles/:id` (admin-only fetch that surfaces drafts/archived; the public `getArticleBySlug` continues to filter). New service methods `listAllArticlesForAdmin` + `getArticleByIdForAdmin`. Frontend: new `lib/api/help.ts` typed client (admin + public reads + writes); new `routes/admin/HelpAdminPage.tsx` — article list with title/slug/summary/tags/status badge/author/updated columns + per-row Publish-toggle / Edit / Archive actions, debounced (300ms) search, "Include archived" filter switch, FormModal-driven create/edit (slug locked post-create as it's the public URL key, summary one-liner, markdown body in monospace textarea, tags comma-separated, Publish switch), ConfirmDialog on archive. 4/4 component tests cover list rendering, EmptyState CTA, Publish-toggle dispatch, archive confirmation. Route registered at `/admin/help` (admin-only) with nav entry. **End-to-end live smoke verified 2026-05-07**: admin POST `/admin/help/articles` created draft (isPublished=false) → public list (employee Ethan, `?q=Fresh`) returned 0 results (not visible) → admin PATCH `isPublished=true` → 200 OK → public list returned 1 result (visible) → non-admin PM hit `/admin/help/articles` → **403 Forbidden**. Backend tsc clean; FE tsc clean. **HD-9 status:** chunks 1 (BE) + 2 (admin editor) shipped; chunks 3 (public list) + 4 (article detail + feedback + markdown renderer) + 5 (onboarding tour modal) remain. Each is a single-session ship.

### Added
- **HD-0.2 Phase 2 — `pickCandidate` $transaction migration + scope correction. CLOSES HD-0.2 end-to-end.** Survey of the 5 originally-listed Phase 2 candidate services revealed only one needed migration; the other 4 are already either single-write services (no tx required by phase scope) or replaced by other phases that already use `$transaction`:
  - **`terminate-employee.service.ts`** — single-write (Person.save). Cross-service `endProjectAssignmentService` loop is excluded scope per Phase 1 doc.
  - **`complete-case-step.service.ts`** — every entry point is single-write (single `caseStep` row update); fire-and-forget notification path.
  - **`assign-project-team.service.ts`** — cross-service fan-out only; each `createProjectAssignmentService` call already runs its own internal `$transaction` (Phase 2a). The wrapping loop is excluded scope.
  - **`approve-budget.service.ts`** — does not exist; replaced by HD-6 `decide-budget-change.service.ts` which already wraps its writes in `$transaction`.
  - **`propose-slate.service.ts.pickCandidate`** — the only one needing migration. Has 2 our-side writes (slate decision + staffing request status flip) bracketing a cross-service `createAssignment.execute()`. Refactored via "option C": cross-service first, then a single `prisma.$transaction` covering `slateRepository.save(slate, tx)` + `tx.staffingRequest.update(...)`. The narrow remaining failure window is "assignment created but slate+request rollback" — recoverable via the assignment row's `staffingRequestId` reference.
  - **2/2 fault-injection unit tests** prove the rollback property: happy path commits both slate + request together (1 tx attempt, slate snapshot committed), and `staffingRequest.update` failure rolls back the slate save (1 tx attempt, no snapshots committed). The test stub mirrors Postgres semantics — pending writes are tracked separately from committed writes; throw inside the callback discards pending.
  - Backend tsc clean; 26/27 unit test suites green (286/286 tests pass; the 1 pre-existing failing suite is `token.service.spec.ts`, unrelated).
  - **HD-0.2 status: CLOSED.** All multi-write services that have writes WE own atomically wrapped: `create-employee` (Phase 1), `create-project-assignment` (Phase 2a), `staffing-proposal-slate.submit` + `.rejectAll` (Phase 2b), `staffing-proposal-slate.pickCandidate` (Phase 2). Cross-service fan-out (assignment loops, case-step loops) remains as documented exclusions — outer-tx wrapping requires plumbing `tx?` through every consumer service, which is HD-0.2-scope-creep and was deliberately deferred.

### Added
- **HD-3 BOOKED-pin hook + missing-rate banner. CLOSES HD-3 end-to-end.** The rate-card system shipped its last two follow-ups in one chunk: bill-rate now actually pins on every BOOKED transition, and unpinned-BOOKED assignments surface a missing-rate banner pointing operators at the admin page.
  - **BE — BOOKED-pin hook**: `TransitionProjectAssignmentService` gained two optional ctor params (`EffectiveBillRateResolverService`, `PrismaService`). When both are wired and `command.target === 'BOOKED'`, the service: (1) fetches the row's pinned-state (idempotent — never overwrites an existing pin); (2) loads `Person.grade` + skill names from `PersonSkill→Skill` join + `Project.clientId` for resolver context; (3) calls `resolver.resolve()` with the assembled context (and any `command.appliedRateCardEntryId` admin override → EXPLICIT layer); (4) stamps `appliedRateCardEntryId` + `effectiveBillRate` + `effectiveBillCurrency` via a single Prisma update; (5) writes an `assignment.rate_card_pinned` audit row including the resolver's `resolvedBy` layer. **Pin failure NEVER blocks the primary BOOKED transition** — caught + logged; the banner surfaces the gap instead. Existing test fixtures pass nothing → pre-HD-3 behaviour preserved (resolver-less callers go through unchanged).
  - **BE — DTO + service surface**: `AssignmentDetailsDto` (and FE `AssignmentDetails`) gained 3 optional fields: `appliedRateCardEntryId`, `effectiveBillRate`, `effectiveBillCurrency`. `GetAssignmentByIdService` reads them via a thin Prisma select alongside the existing person/project lookups (the entity intentionally doesn't surface them — they're read-mostly post-BOOKED columns, no domain logic).
  - **FE — missing-rate banner**: `AssignmentDetailsPlaceholderPage` renders a yellow warning banner when `approvalState ∈ {BOOKED, ONBOARDING, ASSIGNED, ON_HOLD, COMPLETED}` AND `effectiveBillRate` is null/undefined. Banner explains margin math is missing for `{role}` at this person's grade and links to `/admin/rate-cards`. PROPOSED rows (where pinning hasn't happened yet by design) never show the banner.
  - **6/6 new unit tests** for the pin behavior cover: matched verdict pins all 3 columns, NONE → no pin written, already-pinned → idempotent skip, non-BOOKED transitions never pin, legacy callers (no resolver/prisma) preserve baseline, throwing resolver is swallowed and primary BOOKED still completes. The 6 pre-existing assignment SLA tests remain green; backend tsc clean; frontend tsc clean.
  - **End-to-end live smoke verified 2026-05-07** on the running stack: created "Pin smoke 2026" USD rate card → added Lead Engineer/G8 entry @140 → PM Lucas booked PROPOSED assignment `…061` → response status BOOKED → DB row inspected: `appliedRateCardEntryId=c92a5bae…, effectiveBillRate=140.00, effectiveBillCurrency=USD` → `GET /assignments/:id` surfaces all 3 fields → separate unpinned BOOKED row `…015` correctly triggers banner condition (`status=BOOKED && rate=None`).
  - **HD-3 status: CLOSED.** Schema (2026-05-04) + resolver (2026-05-04) + admin UI (2026-05-07) + BOOKED-pin hook + missing-rate banner (2026-05-07) all shipped. Per J2 + J8 (tenant-configurable grade strings). The J2 mandate "every booking must apply a bill-rate" is now enforced at runtime.

### Added
- **HD-3 admin UI — `RateCardsAdminPage` + nested CRUD endpoints. Closes the largest HD-3 follow-up; only the BOOKED-pin hook (~1d) and missing-rate banner (~0.5d) remain.** Backend: new `RateCardsAdminController` at `/admin/rate-cards` (admin-gated, `@Idempotent()` on POST + entry POST) backed by `RateCardAdminService` with full CRUD over both `rate_cards` (parent) and `rate_card_entries` (children). 7 endpoints: `GET /admin/rate-cards` (list with `?clientId=` and `?includeArchived=` filters), `GET /admin/rate-cards/:id` (single card hydrated with entries + pinned-assignment counts), `POST/PATCH/DELETE /admin/rate-cards/:id` (card lifecycle), `POST/PATCH/DELETE /admin/rate-cards/:id/entries[/:entryId]` (entry lifecycle). Soft-delete via `archivedAt` + flips `isActive=false`. Audit rows under `rate_card.{created,updated,archived}` and `rate_card_entry.{created,updated,archived}`. The composite-unique constraint `(rateCardId, staffingRole, grade)` surfaces as a friendly 409 instead of a leaked Prisma P2002. DTOs validate currency-code shape (3-letter ISO, uppercased on create), validity window invariant (`validFrom <= validTo`), required-skills ≤20, hourlyRate ≥0 with ≤2 decimal places. 13/13 new unit tests cover create/list/get/update/archive across both cards and entries + cross-card entry access (400) + duplicate guard (409).
  - Frontend: new `lib/api/rate-cards.ts` client (typed `RateCard` + `RateCardEntry` + `RateCardWithEntries` + 7 CRUD calls). New `routes/admin/RateCardsAdminPage.tsx` — two-section master/detail layout: a cards table on top with click-to-select rows + per-row Disable/Edit/Archive actions on the selected row, and an entries section below scoped to the selected card with its own create-entry button + per-row Disable/Edit/Archive. Two `FormModal`s drive Create/Edit for cards and entries; field locking on entry edit (role + grade are PK and locked) and on card edit (currency + clientId are immutable post-create). Hourly rate is currency-aware (the form label tracks the parent card currency). Tenant-default cards show "Tenant default" scope label; client-scoped cards show "Client · {name}". Pinned-assignment count surfaces in the entries table so admins know how many in-flight assignments depend on the entry before they archive it. 4/4 new component tests cover list rendering, scope label, empty-state CTA, and pinned-count display. Route registered at `/admin/rate-cards` (admin-only) with nav entry.
  - **End-to-end live smoke verified 2026-05-07** — 8-step roundtrip on the running stack: LIST empty → CREATE "Live smoke 2026" USD card → ADD entry Engineer/g13 @95.5 USD → DUPLICATE entry returns **409** with friendly message → invoked `EffectiveBillRateResolverService.resolve` directly with matching context → got `resolvedBy=TENANT_BASIC, hourlyRate=95.5, currencyCode=USD` (the resolver immediately picks up the just-created entry) → PATCH rate 95.5→110 → DELETE entry (200, archivedAt set) → DELETE card (200) → non-admin PM read of `/admin/rate-cards` → **403 Forbidden**. Backend tsc clean; 13/13 admin unit tests + 4/4 FE component tests + the existing 10 resolver unit tests all pass.
  - **HD-3 status:** schema + resolver + admin UI shipped. Remaining: BOOKED-pin hook in `CreateProjectAssignmentService` / `TransitionProjectAssignmentService` to call `EffectiveBillRateResolverService.resolve()` and stamp `appliedRateCardEntryId` + `effectiveBillRate` on the assignment row at BOOKED transition (~1d); missing-rate banner on assignment detail when the resolver returned `NONE` (~0.5d). Both can ship in subsequent sessions following the established pattern.

### Added
- **HD-8 / Chunk 8.5 — Export retrofit on 5 list/queue pages. CLOSES HD-8 end-to-end.** Drops the existing `<ExportButton>` (component already in repo) into 5 list/queue surfaces that previously lacked any XLSX/CSV export. Each retrofit places the button in the page's title-bar action slot via `useTitleBarActions` and derives the export rows from the already-fetched data — no extra fetches, no schema changes.
  - **`ApprovalQueuePage`** — assignment-approval queue. Exports project / person / role / status / start / end / allocation% / SLA due / SLA breached. Filename includes the active scope (`approval-queue-{mine|team|breached|all}`). Side fix: switched the column from the non-existent `r.status` to the actual `r.approvalState` field on `AssignmentDirectoryItem`.
  - **`TimesheetApprovalPage`** — week-level approval queue. Exports week start / person / status / total hours / submitted / approved. Filename includes the status filter.
  - **`ExceptionsPage`** — operational exception queue. Exports observed / category / source / project / person / summary / status / target id+type. Filename includes the status filter.
  - **`InboxPage`** — in-app notification inbox. Exports received / event / title / body / read at / link.
  - **`TimeManagementPage`** — month-scoped queue tab. Exports type / person / status / week start / total hours / overtime / leave type+dates / submitted. Filename includes the YYYY-MM month.
  - All 5 retrofits use `<ExportButton>`'s built-in disabled-on-empty behaviour, click-outside dismiss, and XLSX/CSV menu (no rebuild). Existing pages already exporting via `exportToXlsx` directly (AssignmentsPage, EmployeeDirectoryPage, ProjectsPage) intentionally untouched — refactoring those to use the unified `<ExportButton>` is out of scope for 8.5.
  - **1 new component test** (`ApprovalQueuePage.test.tsx`) verifies the Export button renders alongside the scope filters; existing 18 tests across the retrofitted pages stay green; FE tsc clean.
  - **Live smoke verified 2026-05-07**: backend returns the correct shape on `/assignments?status=BOOKED` (3 rows with `approvalState=BOOKED` populated); the new column accessor matches the field name. FE Vite hot-reloads pick up the changes automatically.
  - **HD-8 status: CLOSED.** Chunks 8.1, 8.2, 8.3, 8.4a, 8.4b, 8.5 all shipped. Phase 21 spillover items absorbed by HD-8 — F2 (notification prefs UI), F3 (exports), F9 (nudge sweep), F11/F15 (undo register/consume) — are now all delivered.

### Added
- **HD-8 / Chunk 8.4b — FE undo toast wired across 3 destructive flows. Closes the user-visible undo feature end-to-end.** New `frontend/src/lib/undo-toast.ts` — single FE helper `showUndoToast({undoActionId, successMessage, onUndone, durationMs?, undoLabel?})` that wraps `sonner` with an Undo action button (5s default, 'Undo' default label). When `undoActionId` is null/undefined (legacy path or failed registration), only the success toast renders — no Undo button. Click → fires `POST /undo/:id/consume` → second 'Reverted.' toast → `onUndone()` triggers caller refetch. Failed consume → red error toast. New `frontend/src/lib/api/undo.ts` API client + `UndoConsumeResponse` type.
  - **3 response DTOs gained `undoActionId?: string`**: `ProjectAssignmentResponse` (assignments.ts), `ProjectClosureResponse` (project-registry.ts), `EmployeeLifecycleRecord` (person-directory.ts) — backend already returns this on success; FE just had to widen the typed shape.
  - **3 destructive call sites wired**: (a) `useAssignmentDetails.ts` — `runDecision(reject)`, `runTransition('CANCELLED')`, `runRevokeAssignment` all now show the Undo toast on success and refetch on consume. (b) `useStaffingDeskActions.ts.rejectAssignment` — refactored to read the response object and pipe `undoActionId` through `showUndoToast`. (c) `EmployeeDetailsPlaceholderPage.handleDeactivate` — surfaces Undo and resets `lifecycleStatus` back to ACTIVE on consume. (d) `ProjectLifecycleControls.handleClose` — surfaces Undo on a successful (non-override) close. Override close intentionally skipped (override is the "I really mean it" path).
  - **6/6 unit tests** for the helper cover: undefined undoActionId → no Undo button, present id → Undo button rendered, custom durationMs/undoLabel respected, click → consume + onUndone + 'Reverted.' toast, consume rejection → error toast, null undoActionId → onUndone NOT fired.
  - **Live smoke verified 2026-05-07** end-to-end on the running stack: PM Lucas cancelled assignment `…b` (response carried `undoActionId="bdd5bd75…"`, status `CANCELLED`) → consume `POST /undo/bdd5bd75…/consume` → 200 with `consumedAt` + `actionType="assignment.cancel"` → DB inspection confirmed status restored to `BOOKED`. (Earlier cancel showed correct 410 GONE on TTL expiry — TTL enforcement still works.)
  - Frontend tsc clean (no new errors); 6/6 helper tests + 7/7 EmployeeDetailsPage tests + the existing FE suite remain green (the same 2 pre-existing vite-transform failures on html2canvas/jspdf-related pages are unchanged).
  - **HD-8 status:** Chunks 8.1, 8.2, 8.3, 8.4a, 8.4b shipped; only Chunk 8.5 (export retrofit on 5–7 list pages) remains.

## 2026-05-06

### Added
- **HD-4 admin UI — `ResponsibilityMatrixAdminPage` + CRUD endpoints. Closes HD-4 end-to-end.** Backend: new `ResponsibilityRulesAdminController` at `/admin/responsibility-rules` (admin-gated, `@Idempotent()` on POST) backed by `ResponsibilityRulesAdminService` (list/create/update/archive over `responsibility_rules`). DTOs validate mode↔target coherence (`ROLE` requires `targetRole`, `PERSON` requires `targetPersonId`, `SKIP`/`PM_SOLO` must NOT carry either) and scope↔value coherence (`TENANT` is null-only, `THRESHOLD_AMOUNT` must be a non-negative number, others must be non-empty). Soft-delete via `archivedAt` + flips `isActive=false`. Audit rows under `responsibility_rule.{created,updated,archived}` (category=settings). Response DTO carries `isSeededDefault` derived from the `00000000-0000-4000-8000-0000000d04xx` UUID prefix so the FE can show a "(default)" badge and disable Archive on seeded rows. 13/13 new unit tests cover validation + state transitions + seeded-default detection.
  - Frontend: new `lib/api/responsibility-rules.ts` client (typed enums + label maps + 4 CRUD calls). New `routes/admin/ResponsibilityMatrixAdminPage.tsx` — DataTable + filter-by-action + include-archived toggle + Create/Edit modal (`FormModal`) + Disable/Enable toggle + Archive confirm dialog. Mode-driven form: target-role select shows only when `mode='ROLE'`, target-person UUID input only on `mode='PERSON'`. Route registered at `/admin/responsibility-matrix` (admin-only) and added to nav. 4/4 new component tests.
  - Side fix: `withProjectLifecycleErrors` in `projects.controller.ts` was flattening every thrown error to `BadRequestException` (400), which dropped the resolver's `ForbiddenException` (403) on PERSON-mode mismatches. Now `HttpException` subclasses pass through untouched so PERSON gating returns the right status code.
  - **End-to-end live smoke verified 2026-05-06**: as Lucas (PM), submitted DRAFT → PENDING_APPROVAL. Created a `PROJECT`-scope `PERSON`-mode rule pinning the project to Director Noah (priority 10, beats the seeded TENANT default at 100). Admin tried to approve → **403** "Responsibility rule …e96b9 requires person bbbb0001-…0002 to decide". Noah approved → **200 ACTIVE**. PATCH lowered priority 10→5 → 200. DELETE archived → archivedAt set + isActive=false. PM hitting `/admin/responsibility-rules` → 403 (RBAC works). Backend tsc clean; 13/13 admin unit tests + the existing 22 HD-4 integration tests all pass; FE suite at 296/304 passing (2 pre-existing vite-transform failures on html2canvas/jspdf-related pages, unrelated).
  - **HD-4 fully closed**: schema (2026-05-04) + resolver (2026-05-04) + service integration (2026-05-05) + default-rule seed (2026-05-05) + admin UI (2026-05-06). Per J4. Tracker entry flips from `[-]` to `[x]`.

## 2026-05-05

### Added
- **HD-4 default-rule seed (`seedResponsibilityRules`).** Closes the second-to-last HD-4 deferred follow-up (admin UI is the only remaining gap). Seven idempotent TENANT-scope rules — one per `ResponsibilityActionKind` — mirror the role gating that was hardcoded before HD-4: `PROJECT_ACTIVATION_APPROVAL→director`, `BUDGET_CHANGE_APPROVAL→director`, `ASSIGNMENT_DIRECTOR_APPROVAL→director`, `ASSIGNMENT_OVERRIDE_APPROVAL→director`, `PERSON_RELEASE_HR_APPROVAL→hr_manager`, `PERSON_RELEASE_DIRECTOR_APPROVAL→director`, `PROJECT_CLOSE_APPROVAL→director`. All `mode=ROLE`, `priority=100`, `tenantId=null`, fixed UUIDs of the form `00000000-0000-4000-8000-0000000d04NN` for idempotent re-runs. Wired into `main()` between `seedRadiatorThresholds` and `seedFullNotificationInfrastructure`, exported from `prisma/seed.ts`, and added to the SetupModule's `apply-infrastructure-seeds.ts` so fresh wizard installs get the defaults too. Behaviour-additive: with these in place, the HD-2/5/6 services that just got resolver-wired now return RULE-source verdicts (instead of FALLBACK) on a clean deployment, without any observable behavior change. **Live smoke verified 2026-05-05** — ran the seeder twice (1st: 7 inserts, 2nd: 0 churn / idempotent), then submitted a DRAFT project as PM Lucas → audit emitted `responsibilityRuleId="...d0401" responsibilityMode="ROLE" responsibilitySource="RULE"`; Director Noah approved → audit emitted same rule on the approve event. Tenant overrides remain a per-deployment admin-UI task (HD-4's last remaining follow-up). Backend tsc clean.

### Added
- **HD-4 ResponsibilityResolver wired into HD-2 / HD-5 / HD-6 services.** Closes the third deferred follow-up on the HD-4 entry (schema + resolver shipped 2026-05-04; integration into approval flows was the remaining gap — admin UI + default-rule seed still deferred). All six approval-flow services now consult the resolver and apply the verdict.
  - **HD-2 — `SubmitProjectForApprovalService`**: when a rule with `mode in (SKIP|PM_SOLO)` matches, the project transitions DRAFT→ACTIVE in one step and the `ProjectActivationApproval` row lands as APPROVED with `decidedById = actorId`. Translator fires `project.approved` + `project.activated` immediately. Other modes (ROLE/PERSON, FALLBACK) preserve the existing PENDING_APPROVAL flow exactly. Resolver throw is caught and falls back silently — never blocks the primary submit. Action kind: `PROJECT_ACTIVATION_APPROVAL`. Context dimensions: `projectId`, `clientId`, `projectType`.
  - **HD-2 — `DecideProjectActivationService`**: when a rule with `mode === 'PERSON'` matches, only the configured `targetPersonId` may decide (403 otherwise). SKIP/PM_SOLO at decide time are advisory — log warn but allow the Director to clear an in-flight request the rule was added after-the-fact (no stranded approvals). All decisions persist `responsibilityRuleId` + `mode` + `source` in audit metadata.
  - **HD-6 — `RequestBudgetChangeService`**: SKIP/PM_SOLO rules apply the budget delta atomically and stamp the `BudgetApproval` as APPROVED. The `amount` dimension passes the magnitude `max(|Δcapex|, |Δopex|)` so THRESHOLD_AMOUNT rules can route based on change size. Action kind: `BUDGET_CHANGE_APPROVAL`. `clientId` + `projectType` resolved via a `prisma.project.findUnique` call wrapped in a try/catch.
  - **HD-6 — `DecideBudgetChangeService`**: PERSON gating same as HD-2 decide. Audit + notification metadata gain the rule fields. Project loaded via the approval row's `projectBudgetId` → `projectBudget.projectId` → `project`.
  - **HD-5 — `OpenPersonReleaseRequestService`**: resolves both `PERSON_RELEASE_HR_APPROVAL` and `PERSON_RELEASE_DIRECTOR_APPROVAL` at open time; rule IDs surface in the result + audit metadata. Auto-decide is intentionally NOT applied at open time — the dual-approval semantic is too important to short-circuit on open. Per-slot enforcement happens at decide time. Context: subject's `orgUnitId` + `grade`.
  - **HD-5 — `DecidePersonReleaseService`**: per-slot resolution (`hr_manager` → HR action kind, `director` → Director action kind). PERSON gating per slot enforced. Existing dual-approval semantics preserved — first REJECT short-circuits, second matching APPROVE flips to APPROVED.
  - **DI plumbing**: `ResponsibilityResolverService` is `@Global()` exported from `IdentityAccessModule`; threaded into the factories of all 6 services in `project-registry.module.ts`, `financial-governance.module.ts`, `organization.module.ts` as the optional last constructor param. Existing test fixtures pass nothing → identical pre-HD-4 behaviour preserved.
  - **22 new unit tests** across the three integration spec files (`project-approval-gate.spec.ts`, `budget-change-approval.spec.ts`, `person-release-dual-approval.spec.ts`) cover: FALLBACK preserves baseline, ROLE rule preserves baseline, SKIP/PM_SOLO auto-approve at submit/request, PERSON match allows decide, PERSON mismatch → 403, SKIP at decide is permissive, throwing resolver is caught + falls back. Stub Prisma `create` helpers updated to honor the new `decision` / `status` / `decidedAt` / `decidedById` fields written by the auto-approve path.
  - Backend tsc clean; **252 unit tests pass** (1 pre-existing failing suite — `test/unit/auth/token.service.spec.ts` — unrelated, documented in conversation history).
  - **Behaviour-additive**: with no rules in `responsibility_rules` and no FE admin UI yet, the resolver always returns FALLBACK and every flow behaves identically to pre-HD-4. The wiring lays the rails for HD-4's two remaining follow-ups (admin UI + default-rule seed) without a coordinated cutover.
  - Tracker entry updated: `HD-4` follow-ups list now reads "admin UI + default-rule seed pending; service integration shipped 2026-05-05".

### Added
- **HD-8 undo register on 2 more destructive actions (Chunk 8.4a — backend).** Extends the undo plumbing from chunk 8.2 to two more flows: project close and employee deactivate. Both register an undo on success and surface `undoActionId` on the response envelope; the existing `POST /undo/:id/consume` endpoint reverses each via a per-actionType executor.
  - **`project.close`** — `CloseProjectService` now optionally injects `UndoService` and registers an undo when the close is non-override (override closes are deliberately one-way). New `Project.restoreFromClose()` privileged domain method (CLOSED is semi-terminal in the public state graph; safety boundary is the undo token's TTL + actor check). New `ProjectCloseUndoExecutor` flips status to ACTIVE and writes `project.close.undone` audit. `ProjectClosureResponseDto.undoActionId?: string` exposes the token to the FE.
  - **`person.deactivate`** — `DeactivateEmployeeService` return shape changed to `{person, undoActionId}` and gained an `actorId` parameter (controller now reads from principal). New `Person.restoreFromDeactivation()` privileged domain method that refuses on TERMINATED rows (different lifecycle, HD-5 dual-approval flow). New `PersonDeactivateUndoExecutor` flips employmentStatus to ACTIVE and writes `person.deactivate.undone` audit. `EmployeeResponseDto.undoActionId?: string` exposes the token; `mapEmployeeResponse` now takes an optional second arg.
  - Both executors registered with `UndoActionExecutorRegistry` at module-construction time (mirrors the chunk 8.2 pattern in `AssignmentsModule`). Both no-op idempotently when the entity is already in the restored state — concurrent retries are safe.
  - 9/9 new unit tests cover happy-path restore + idempotent no-op + 404 missing entity for both executors, plus the TERMINATED-refusal edge case for person.
  - Backend tsc clean; RBAC ratchet at 0; full unit suite at 235 passing (the 1 pre-existing TS error in `test/unit/auth/token.service.spec.ts` is unrelated).
  - Plan deviation: the spec said "ArchiveProjectService" but that service isn't exposed via any HTTP route in the current repo. Pivoted to `CloseProjectService` instead (`POST /projects/:id/close`), which is wired through the controller and matches the same UX intent.
  - **End-to-end live smoke verified 2026-05-05** — Director closes project `Outbox Soak` → response `status=CLOSED`, `undoActionId=…`; consume → project flips back to ACTIVE; HR Diana deactivates Vikram Fischer → `status=INACTIVE`, `undoActionId=…`; consume → person flips back to ACTIVE; re-consume returns identical envelope (idempotent). Closes F11/F15 backend-side for two more flows. (HD-8 Chunk 8.4a; tracker Phase HD)

### Added
- **HD-8 NudgeSweeperService — proactive overdue nudges (Chunk 8.3).** New `src/modules/notifications/application/nudge-sweeper.service.ts` mirrors the `AssignmentSlaSweepService` pattern (HD-10 era): `OnModuleInit` schedules a `setInterval` driven loop on `nudge.sweep.intervalMinutes` (default 60, clamped 1..1440), `tickSafe()` overlap guard, `process.env.NUDGE_SWEEP_DISABLED='true'` escape hatch, full `MetricsService` instrumentation. Two categories scanned per tick:
  - **Proposal acknowledgment overdue** — `StaffingRequestProposalSlate` rows where `status='OPEN'` AND `proposedAt < now - staffing.proposalAck.slaHours` (default 48h) AND no candidate has decision != PENDING. Recipient: the slate's `proposedByPersonId`. Fires `nudge.proposal_acknowledgment_overdue` via the translator (event registered in 8.1 OUTBOX_HANDLERS); flows through dual-write seam to email + in-app.
  - **Timesheet submission overdue** — `TimesheetWeek` rows where `status='DRAFT'` AND the period's end (weekStart + 7d) is past `timesheet.submission.slaHours` (default 72h). Recipient: the timesheet's `personId`.
  - Dedup keyed off `OutboxEvent` (eventName + aggregateId + createdAt within `nudge.dedup.hours`, default 24h). Initially I used `NotificationRequest`, but the email dispatch path is conditional on `notificationsDefaultEmailRecipient` being set, so without it the dedup table stayed empty and the sweeper re-emitted on every tick. Switching dedup to `OutboxEvent` matches the always-on dual-write seam from HD-0.3.
  - New Prom counters: `dc_nudge_sweep_ticks_total`, `dc_nudges_emitted_total{category}`, `dc_nudges_suppressed_total{category}`. Exposed via the existing `/metrics` endpoint (HD-11).
  - 9/9 unit tests cover stale-slate emission, fresh-slate skip, decided-candidate skip, dedup-suppress, dedup-window-expired re-emit, timesheet emit + skip when SUBMITTED, no-email skip, and PlatformSetting overrides. Backend tsc clean; RBAC ratchet at 0; full unit suite at 226 tests pass (the 1 pre-existing TS error in `test/unit/auth/token.service.spec.ts` is unrelated).
  - **End-to-end live smoke verified 2026-05-05** — crafted an OPEN slate proposed 60h ago + lowered sweep interval to 1m. First tick after restart produced 1 `nudge.proposal_acknowledgment_overdue` row in `OutboxEvent` (PUBLISHED via the existing publisher), translator dispatched email + in-app, sweep log read `proposals=1/0 timesheets=0/0`. Second tick within the 24h dedup window produced 0 new rows — log read `proposals=0/1 timesheets=0/0` (suppressed). 122 suppressed ticks visible in `dc_nudges_suppressed_total{category="proposal_acknowledgment"}`; 1 emitted in `dc_nudges_emitted_total`. Closes F9 backend-side. (HD-8 Chunk 8.3; tracker Phase HD)

## 2026-05-04

### Added
- **HD-8 server-side undo for reversible destructive actions (Chunk 8.2).** New `src/modules/undo/` (`@Global` module): `UndoService.register({actorId, actionType, entityId, inversePayload, ttlSeconds=300})` writes to the previously-empty `undo_actions` table (schema was prepared but unused); `UndoService.consume(undoActionId, actorId)` is a 5-state machine — `404` unknown / `403` actor-mismatch / `410` expired / idempotent re-return on already-consumed / dispatch through the `UndoActionExecutorRegistry` and stamp `consumedAt` on success. Executors are domain-owned; modules register their inverse logic at boot via the registry (mirrors the OutboxEventHandlerRegistry pattern from HD-0.3). Endpoint `POST /undo/:id/consume` (all 7 roles, `@Idempotent()` for retry safety). First integration: `TransitionProjectAssignmentService.execute()` return type changed to `{assignment, undoActionId: string | null}`; on a successful CANCELLED transition the service registers an undo with `inversePayload = {previousStatus, reason}`. The new `AssignmentCancelUndoExecutor` reverses the cancel via a privileged `restoreFromCancellation()` domain method on `ProjectAssignment` (the public state machine has CANCELLED as terminal — the undo token's TTL + actor check is the safety boundary). Restores write a fresh `AssignmentHistory` row + AuditLog entry tagged `assignment.cancel.undone` so audit queries can distinguish a fresh cancel from an undo round-trip. `ProjectAssignmentResponseDto.undoActionId?: string` is the FE-facing field. Side fix: `TransitionRequestDto.reason` now carries `@IsString() @IsOptional()` decorators — without them, the global ValidationPipe `whitelist: true` was silently stripping `reason` from cancel requests (cancel transitions require reason at the domain layer; pre-existing bug exposed by the smoke). 9/9 unit tests on UndoService cover register / TTL default / NotFound / Forbidden / Gone / executor success / idempotent re-consume / no-executor 503 / executor-throws-leaves-row-unconsumed. Backend tsc clean; RBAC ratchet 0; 226 unit tests pass. **End-to-end live smoke verified 2026-05-05** — Director cancels `bbbb0009-0000-0000-0000-000000000001` → response carries `undoActionId`, row in `undo_actions` with inverse `{previousStatus: ASSIGNED, reason: "..."}`. POST `/undo/:id/consume` returns the consumed envelope; assignment row flips back to `status=ASSIGNED`, `cancellationReason=null`. Re-consume returns the identical envelope (idempotent, no re-run). Different actor → 403; bogus id → 404. Closes F11/F15 backend-side. (HD-8 Chunk 8.2; tracker Phase HD)

### Added
- **HD-8 primitives sweep (Chunk 8.1).** Three sub-day items shipped together to unblock chunks 8.2-8.5:
  - **F3a — `<ExportButton>` vitest spec.** The component already existed at `frontend/src/components/common/ExportButton.tsx` with the typed `ExportColumn<T>` API and was used in 3 pages (`CasesPage`, `ResourcePoolsPage`, `StaffingRequestsPage`). Added 5/5 vitest specs covering disabled-on-empty, menu open, XLSX label-mapped rows, CSV with quote-escaping (Blob constructor spy since jsdom lacks `Blob.text()`), and `accessor` function support. No component changes needed.
  - **F2a — Dynamic notification channels.** New `GET /notifications/channels` endpoint at `src/modules/in-app-notifications/presentation/notification-channels.controller.ts`, gated to all 7 authenticated roles, returning active `NotificationChannel` rows ordered by displayName. New FE API client method `fetchNotificationChannels()`. `AccountSettingsPage.tsx` refactored: dropped the hardcoded `NotifPrefs { emailEnabled, inAppEnabled, teamsEnabled }` shape (which referenced channel keys `email`/`in_app`/`teams` that didn't all match the seeded DB rows) for a `Record<string, boolean>` keyed by whatever the platform exposes. Legacy localStorage migration preserved — only known legacy keys (`email`/`in_app`/`teams`) get migrated; new channels keep their default-on/off behavior. Live verified: anonymous → 401, employee → 200 with 3 rows (email / generic / ms_teams_webhook).
  - **F9b — Nudge translator stubs.** Added 4 entries to `OUTBOX_HANDLERS` in `notification-event-translator.service.ts`: `nudge.proposal_acknowledgment_overdue`, `nudge.timesheet_submission_overdue`, `nudge.staffing_request_approval_overdue`, `nudge.assignment_approval_overdue`. Each has a public entry point that flows through the existing `dualDispatch` seam plus a private dispatch method that sends an email + per-recipient in-app notification using the established pattern. Chunk 8.3 NudgeSweeperService will consume these without translator changes.

  Backend tsc clean; RBAC ratchet 0 violations; 42 notification + help-center unit tests pass; FE suite at 292 passing (the 2 pre-existing `html2canvas` import failures are unrelated). (HD-8 Chunk 8.1; tracker Phase HD)

### Added
- **Help Center MVP backend (J11 / HD-9 Chunk 1).** Four new tables — `help_articles` (slug, title, markdown body, summary, tags, isPublished, soft-delete via archivedAt, author back-relation), `help_tips` (route-pinned: `key` unique + `routePath` + `displayOrder` + optional FK to article), `help_feedback` (per-article reader feedback: `wasHelpful` + optional comment + actor back-relation, multiple rows per (article, actor) allowed), and `onboarding_tour_progress` (per-user `completedSteps[]` + `dismissedAt` + `completedAt`, unique on `(personId, tourKey)`). REVERSIBLE migration `20260504_hd_09_help_center` (idempotent forward via `IF NOT EXISTS` + `pg_constraint` checks; rollback drops in reverse-FK order). New `HelpService` and 7 endpoints — public reads (`GET /help/articles?q=&tag=`, `GET /help/articles/:slug`, `GET /help/tips?route=`), public writes (`POST /help/articles/:id/feedback`, `GET/PUT /help/onboarding/:tourKey`), and admin writes (`POST /admin/help/articles`, `PATCH /admin/help/articles/:id` with archive flag, `POST /admin/help/tips`). Public endpoints gated to all 7 roles; admin endpoints to `admin` only. 11/11 unit tests cover list filters, slug-404, duplicate-slug-409, archive flag, tip route-scoping + sort, feedback-on-archived-404, tour upsert idempotency, and timestamps. **End-to-end live smoke verified 2026-05-04**: anonymous → 401; admin draft article → 404 for employee; admin publishes → employee reads; feedback persisted with comment; tour progress empty by default → row written after PUT with `completedSteps`; duplicate slug → 409; employee on admin endpoint → 403; route-pinned tip surfaces in `/help/tips?route=…`; `completed:true` stamps `completedAt`. Backend tsc clean; RBAC ratchet 0 violations; all 217 unit tests pass. **Deferred follow-ups (HD-9 Chunks 2-5)**: admin article editor UI, public `/help` route + list, article detail + feedback UI, onboarding-tour state machine + modal. (HD-9 Chunk 1; tracker Phase HD)

### Added
- **ResponsibilityRule matrix foundation (J4 / HD-4).** New `responsibility_rules` table + 3 enums: `ResponsibilityActionKind` (7 actions: PROJECT_ACTIVATION_APPROVAL / BUDGET_CHANGE_APPROVAL / ASSIGNMENT_DIRECTOR_APPROVAL / ASSIGNMENT_OVERRIDE_APPROVAL / PERSON_RELEASE_HR_APPROVAL / PERSON_RELEASE_DIRECTOR_APPROVAL / PROJECT_CLOSE_APPROVAL), `ResponsibilityScope` (7 scopes: TENANT / ORG_UNIT / CLIENT / PROJECT / PROJECT_TYPE / THRESHOLD_AMOUNT / ROLE_GRADE), `ResponsibilityResolutionMode` (4 modes: ROLE / PERSON / PM_SOLO / SKIP). REVERSIBLE migration `20260504_hd_04b_responsibility_rules` (idempotent forward, drops cleanly). New `ResponsibilityResolverService` in `src/modules/identity-access/application/` that walks rules in priority order with most-specific-scope tiebreak: PROJECT > CLIENT > ORG_UNIT > {PROJECT_TYPE, ROLE_GRADE} > THRESHOLD_AMOUNT > TENANT, and returns a typed `ResponsibilityVerdict` (`mode`, `targetRole`, `targetPersonId`, `ruleId`, `source: 'RULE'|'FALLBACK'`, `matchedScope`). Lower priority number wins; updatedAt-desc breaks ties. THRESHOLD_AMOUNT is special-cased — rules at this scope fire when the action's amount ≥ stored threshold. Callers pass `fallbackRole` so the resolver always returns a usable verdict (preserves pre-HD-4 behaviour when no rule exists). 9/9 unit tests cover fallback / TENANT default / specificity ordering / priority override / threshold above-and-below / SKIP mode / PM_SOLO / inactive-archived skip / updatedAt tiebreak. Per J4 (full configurable matrix). Backend tsc clean; all 206 unit tests pass. **Deferred follow-ups**: admin UI for rules (`ResponsibilityMatrixAdminPage.tsx`), seeding of default rules from the existing hardcoded routings, integration into HD-2/HD-5/HD-6 services so they consult the resolver instead of hardcoded role lists. Schema + resolver in place, no behaviour change to live approval flows yet. (HD-4; tracker Phase HD)

### Added
- **RateCard system foundation (J2 / HD-3).** Two new tables: `rate_cards` (tenant- or client-scoped, currency-coded, with `[validFrom, validTo]` activation window) and `rate_card_entries` (role × grade × optional `requiredSkills` text array → hourly bill rate). Three new columns on `ProjectAssignment`: `appliedRateCardEntryId` (FK, nullable), `effectiveBillRate` (Decimal(10,2), nullable), `effectiveBillCurrency` (VARCHAR(3), nullable) — populated by a future BOOKED-transition hook so margin math reads a stable pinned value even if the underlying card later mutates. REVERSIBLE migration `20260504_hd_03_rate_cards` (idempotent forward, drops cleanly on rollback). New `EffectiveBillRateResolverService` in `src/modules/financial-governance/application/` implements the 5-layer precedence walk: (1) EXPLICIT — admin-supplied entry id; (2) CLIENT_FULL — client-scoped card with role + grade + all `requiredSkills` matched on the person; (3) CLIENT_BASIC — client-scoped, role + grade only; (4) TENANT_FULL — tenant-default card with skills; (5) TENANT_BASIC — tenant-default, role + grade only. Active cards must satisfy `isActive=true`, not archived, and `validFrom ≤ assignmentValidFrom ≤ validTo`. Ties within a layer break by most-recently-updated entry. Returns `resolvedBy: 'NONE'` when nothing matches (drives a future missing-rate banner). 10/10 unit tests cover every layer + skip-inactive + null-grade + cross-layer precedence + tie-break. Per J2 (with J8 tenant-configurable `grade` strings, not P1-P5 enum). Backend tsc clean; all 197 unit tests pass. **Deferred follow-ups**: admin UI for rate cards, BOOKED-transition pinning hook, missing-rate banner — schema + resolver in place, no behaviour change to live BOOKED flow yet. (HD-3 schema+resolver; tracker Phase HD)

### Added
- **Idempotency-Key middleware for opt-in mutating endpoints (F1.3).** New `IdempotencyKey` model (table `idempotency_keys` with unique index on `(idempotency_key, method, path, actor_id)` + btree on `expires_at`) plus `IdempotencyKeyStatus` enum (PENDING/COMPLETED/FAILED). REVERSIBLE migration `20260504_hd_04_idempotency_keys`. New `@Idempotent()` decorator + global `IdempotencyInterceptor`: when a marked endpoint receives an `Idempotency-Key` header, the interceptor reserves a PENDING row (race-safe via the unique index — concurrent inserts hit P2002 and re-discover the existing row), runs the handler, and stamps the response body+status as COMPLETED. Subsequent calls with the same key + body **replay** the cached response with an `Idempotent-Replay: true` header set; same key + different body → 409 with explicit message; concurrent in-flight (PENDING) → 409 "already in flight"; handler exception → row marked FAILED so the caller can retry fresh. SHA-256 over the JSON-serialized body is the request fingerprint. Default TTL 24h. Lookup failures fall back to running the handler uncached so a Prisma blip can't take down writes. Applied to four high-value mutating endpoints from HD-2/5/6: `POST /projects/:id/submit-for-approval`, `POST /projects/:id/approve`, `POST /org/people/:id/release-requests`, `POST /people/release-requests/:requestId/approve`, `POST /projects/:id/budget-change-requests`. 8/8 unit tests cover replay / mismatch / PENDING-race / FAILED-retry / pass-through. **End-to-end live smoke verified 2026-05-04** — RM Sophia opens a release request with `Idempotency-Key: hd-04-smoke-N` → first call creates `requestId=31e2077e-…` (fresh DB row); second call (same key + body) returns identical body with `Idempotent-Replay: true` header and **no new DB row** (count stays at 1); third call (same key + different body) → 409. Cache row stamped COMPLETED/200 in `idempotency_keys`. Backend tsc clean. Closes F1.3. (HD-0.4; tracker Phase HD)

### Added
- **`prom-client` instrumentation + `GET /metrics` admin-gated endpoint (F8.1).** New `MetricsService` in `src/shared/observability/` registers a Prometheus `Registry` with `dc_`-prefixed default Node metrics PLUS hand-rolled counters/gauges for the infra we ship: `dc_outbox_events_{claimed,dispatched,failed,retried}_total` (labelled by `event_name`), `dc_outbox_events_backlog` (gauge of PENDING+RETRY rows, refreshed each tick), `dc_assignment_sla_sweep_ticks_total`, `dc_assignment_sla_breached_total{sla_stage=…}`, `dc_assignment_sla_pre_breach_warned_total{warn_level=50pct|75pct, sla_stage=…}`. Wired into `OutboxEventPublisherService` (claim/dispatch/fail/retry/backlog) and `AssignmentSlaSweepService` (tick/breach/pre-breach). New `MetricsController` exposes the registry on `GET /metrics` with `Content-Type: text/plain; version=0.0.4`; gated by `@RequireRoles('admin')` (anonymous → 401, employee → 403, admin → 200 prom output) so a misconfigured ingress can't enumerate internal series. `prom-client` 15.x added as runtime dependency. **End-to-end live smoke verified 2026-05-04** — submitted a pulse + opened an HD-5 release request → publisher's next 5s tick fired `dc_outbox_events_claimed_total=1` + `dc_outbox_events_dispatched_total{event_name="person.release.requested"}=1` + `dc_outbox_events_backlog=0`. Backend tsc clean; all 179 unit tests pass. (HD-11; tracker Phase HD)

### Changed
- **`ResourceManagerDashboardPage.tsx` split from 611 → 339 lines** (Phase 20c-15 partial). Five sibling components extracted into `frontend/src/routes/dashboard/rm-sections/`: `RmKpiStrip` (KPI strip with traffic-light tone helpers), `RmActionItems` (overallocations + pending approvals + incoming requests table), `RmQuickAssignModal` (controlled-form modal), `RmTeamCapacitySection` (team-capacity-by-org-unit table), and `RmDetailTables` exporting `RmAllocationIndicatorsTable` / `RmFuturePipelineTable` / `RmIdleResourcesTable`. Each section is shaped around a clean prop boundary against `ResourceManagerDashboardResponse` so the parent only owns state + derived data. All 8 dashboard test files (21 tests) pass; visual / behavioural identity preserved. (HD-13; tracker Phase HD)

### Added
- **SLA pre-breach warnings at 50% and 75% elapsed fraction (F5.2).** Existing `AssignmentSlaSweepService` extended to ALSO scan rows where `slaDueAt` is still in the future and the elapsed fraction `(now - requestedAt) / (slaDueAt - requestedAt)` has crossed 0.5 or 0.75. Each level fires exactly once per assignment, deduplicated via two new nullable timestamps (`slaWarnedAt50pct`, `slaWarnedAt75pct`); crossing 75% backfills the 50% stamp so a long gap between sweeps can't re-fire. New translator event `assignment.sla_pre_breach` carrying `warnLevel` ('50pct' | '75pct') flows through the existing dual-write seam (registered in OUTBOX_HANDLERS); email + in-app notifications template the friendly "Assignment SLA at 50%/75%" string. Schema migration `20260504_hd_10_sla_pre_breach_warnings` is REVERSIBLE (idempotent forward, drops both columns on rollback). 6/6 unit tests cover threshold crossings, dedup, breach short-circuit, and progression. **End-to-end live smoke verified 2026-05-04** — crafted a row at 60.3% elapsed → next sweep tick wrote audit + outbox PUBLISHED + stamped `slaWarnedAt50pct`; bumped to 80.1% elapsed → next tick wrote a fresh audit + outbox PUBLISHED + stamped `slaWarnedAt75pct` AND backfilled `slaWarnedAt50pct`. Closes F5.2 / D-32 partial. (HD-10; tracker Phase HD)

### Added
- **Pulse audience expansion to RM/PM/Director/HR (J7).** New endpoint `GET /pulse/team-trend?weeks=N` returns a weekly avgMood / responseCount / strugglingCount series over the caller's reporting scope. Scope is resolved server-side via the canonical `ManagerScopeQueryService` — direct + dotted-line reports — so each role naturally sees only what they manage. Empty weeks come back with `avgMood: null` so the FE can render gaps in the sparkline. Service method `PulseService.getTeamTrend(personIds, weeks)` clamps weeks to [1, 52] and aggregates entries from `PulseEntry` in a single query. New FE hook `usePulseTeamTrend` + new component `PulseTrendCard` (latest-week tile + sparkline + response/struggling tally with traffic-light tone). First placement: HR Wellbeing tab — extends naturally to RM/PM/Director dashboards via the same component (single-line drop-in). Endpoint gated by `@RequireRoles('project_manager','resource_manager','hr_manager','delivery_manager','director','admin')`. 4/4 service unit tests + 3/3 component tests pass; **end-to-end live smoke verified 2026-05-04** — Emma submits mood=2 → HR Diana's trend immediately shows `responseCount=1, avgMood=2` in the current week; Director Noah's scope = 7 reports; HR Diana's scope = 3 reports; Employee Ethan correctly gets 403 (no reports = no team trend). Closes J7. (HD-7; tracker Phase HD)

## 2026-05-03

### Fixed
- **Person detail page no longer 500s when fetching active assignments.** `GET /api/assignments?status=active` was passing the literal string `ACTIVE` to Prisma, which is not a valid `AssignmentStatus` enum value (the canonical CSW workflow uses `BOOKED` / `ONBOARDING` / `ASSIGNED` / `ON_HOLD` for "currently committed" — not `ACTIVE`). The list-assignments service now expands the virtual token `ACTIVE` to that set of canonical statuses; literal status filters still pass through verbatim. Verified live: previously 500, now 200 with the correct empty result for the test person.
- **Reporting-line "Save" no longer silently future-dates the change.** The form's `startDate` defaulted to an empty string, so a user picking tomorrow (or the date picker rendering today's local date as tomorrow's UTC) silently scheduled the line for the future, leaving the page header still showing "No line manager assigned" after a successful save. Default is now today (UTC); the existing "Scheduled X effective DATE" success message still fires when the user explicitly picks a future date. The manager dropdown also bumped from `pageSize=100` to `pageSize=500` to match the Create-Employee form (IT-Company seed has 200 people; the previous limit silently dropped page-2 managers from the picker).

### Verified
- **All 28 translator events now flow through the dual-write seam.** Resolved the last deferral (`integration.sync_failed`) by deriving its `OutboxEvent.aggregateId` as a deterministic UUIDv5 over `${provider}::${resourceType}` against a stable namespace. Same input → same UUID, satisfying the `@db.Uuid` column constraint while preserving cross-process dedup semantics. Local `uuid.d.ts` declaration replaces the missing `@types/uuid` dev-dep. 31/31 parameterized tests pass. (HD-0.3 follow-up; tracker Phase HD)
- **Outbox dual-write soaked end-to-end** on the dev stack. Flipped `flag.outboxEnabled=true`, triggered the HD-2 project create→submit→approve flow, watched all 3 events transition `PENDING → PUBLISHED` in `OutboxEvent` with `attempts=0`. The publisher loop's poll-claim-dispatch cycle works as designed; the registered handlers re-enter the synchronous fan-out cleanly. (HD-0.3 Phase 2d; tracker Phase HD)
- **HD-2 verified end-to-end live**: PM creates DRAFT → submits (200) → tries to self-approve (403 Forbidden — no rubber-stamp) → Director approves (200, ACTIVE). `ProjectActivationApproval` row persisted with `decision=APPROVED`, `requestedById ≠ decidedById`. (HD-2; tracker Phase HD)

### Added
- **`@AuditRead` decorator + global interceptor for PII-revealing GET endpoints.** Per `docs/architecture/audit-vs-activity.md`, read-side reveals are the one non-mutating action that still warrants forensic audit (e.g., HR opens someone's compensation tab; ops queries another tenant's directory). New decorator at `src/shared/http/audit-read.decorator.ts` carries `actionType`, `category`, and a `targetEntity.idFrom` resolver (`param:<name>` | `principal` | `body:<jsonpath>`); new interceptor at `src/shared/http/audit-read.interceptor.ts` writes one `AuditLog` row via `AuditLoggerService.record()` only AFTER the handler resolves successfully. Failures are NOT audited as a successful read because the caller didn't receive the data; interceptor errors are logged but never block the response. Globally registered as `APP_INTERCEPTOR` alongside the deprecated-endpoint interceptor. First consumer: `GET /org/people/:id` is now annotated `@AuditRead({ actionType: 'person.directory.read', category: 'organization', targetEntity: { entityType: 'PERSON', idFrom: 'param:id' } })`. Verified live: HR Diana and admin reading Catherine's directory record produced two `person.directory.read` rows in the in-memory audit cache (visible via `/audit/business`), each with the correct actor, target, correlationId, and path. Note: rows aren't persisting to the `AuditLog` Prisma table due to pre-existing `AggregateType` enum-cast drift (writers use `'PERSON'`/`'EMPLOYEE'` while the enum is PascalCase `Person`); fix is out-of-scope for HD-0.7. Closes F4.2. (HD-0.7; tracker Phase HD)

### Changed
- **`flag.*` PlatformSetting reads consolidated behind `PlatformFlagsService`.** New service in `src/shared/config/platform-flags.service.ts` owns the lookup-with-30s-cache logic and a typed `PLATFORM_FLAGS` registry of known flag keys (today: `outboxEnabled`). Two existing duplications eliminated — `NotificationEventTranslatorService.isOutboxEnabled()` (had its own per-instance cache) and `OutboxEventPublisherService.flag()` (uncached) both delegate now. The service is registered globally via `AppConfigModule` so any future flag consumer is one constructor injection away. New flags are added by appending to `PLATFORM_FLAGS` (key + description + default), then calling `platformFlags.isEnabled('newFlagId')` — type-safe, single grep target. 31 translator unit tests updated to inject a stub `PlatformFlagsService` (4 instantiations); all 169 unit tests pass. Verified live: HD-5 release events flowed through `flag.outboxEnabled=true` end-to-end (`person.release.{requested,partially_approved,approved,rejected}` all transitioned PENDING → PUBLISHED). Closes F9.1. (HD-12; tracker Phase HD)

### Added
- **`docs/architecture/audit-vs-activity.md` — doctrine page distinguishing the two append-only streams.** `AuditLog` is the forensic ledger (every mutation, hash-chained, machine-friendly payload, indefinite retention) for auditors/compliance/incident response; `EmployeeActivityEvent` is the human-readable person-timeline feed for Person 360. Includes a write-which-when matrix, a $transaction-shaped recipe showing how AuditLog + activity + OutboxEvent compose, and four common pitfalls to avoid (audit silently bypassed, timeline gaps, divergent summaries, post-commit audit). Closes F4.1. (HD-0.6; tracker Phase HD)

### Verified
- **HD-3.0 — `grade` MetadataDictionary is editable from `/admin/dictionaries`.** Verified live: dictionary `grade` (id `42222222-0000-0000-0000-000000000101`, entityType `Person`, isSystemManaged=false, 8 entries g7..g14) surfaces in `GET /metadata/dictionaries` and is editable through the existing `POST :type/entries` + `PATCH /entries/:id` admin endpoints — no new code needed. `Person.grade` is already typed `String?` in the schema (not an enum), so it can reference whatever entries the tenant configures. Per J8 strengthening (grade catalog is tenant-configurable, not a hardcoded G7-G15 → P1-P5 mapping). Unblocks HD-3 RateCard work. (HD-3.0; tracker Phase HD)

### Added
- **RBAC ratchet on every mutation endpoint.** New CI script `scripts/check-rbac.cjs` walks every `*.controller.ts` under `src/modules/**/presentation/` and fails if any `@Post` / `@Put` / `@Patch` / `@Delete` method lacks one of: `@RequireRoles(...)`, `@Public()`, or `@AllowSelfScope({...})` — at either the class or method level. The walker handles multi-line decorator argument lists by tracking paren/brace depth, so it doesn't false-positive on `@ApiOperation({ summary: ... })` blocks. Initial run found 8 self-service endpoints (4 in timesheets, 2 in work-evidence, 1 in pulse, 1 in jira-integrations) that had no role decorator at all — all 8 now carry the appropriate `@RequireRoles` list. Wired into `npm run rbac:check`, `npm run verify:pr`, and `architecture-check.yml`. (HD-0.5 / F3.1; tracker Phase HD)

### Added
- **Person Release dual-approval flow.** RM (or admin) opens a release request for an active employee (`POST /org/people/:id/release-requests` with `{reason, reasonCode?, targetTerminationDate}`). The request lands as `PENDING_APPROVAL`. HR Manager and Director then each approve via `POST /people/release-requests/:requestId/approve` — the FIRST approval leaves the row `PENDING_APPROVAL` (one slot filled, partial), the SECOND from the OTHER role flips it to `APPROVED`. Either side can reject (`POST .../:requestId/reject`, reason required) which short-circuits the workflow to `REJECTED`. Self-rubber-stamp is blocked at three layers: `@RequireRoles` on the controller, `pickDecisionRole` helper that refuses admin-only callers, and explicit guards in `DecidePersonReleaseService` that forbid the initiator and the subject from deciding. One approval row per role per request (unique index + explicit pre-check for friendly 409). Both writes (approval row + request status) share a single `prisma.$transaction` so the ledger and request can never disagree. Four new translator events (`person.release.requested`, `person.release.partially_approved`, `person.release.approved`, `person.release.rejected`) flow through the dual-write seam. Schema migration `20260503_hd_05_person_release_request` adds `PersonReleaseRequest` + `PersonReleaseApproval` tables + 2 enums (REVERSIBLE). 17 unit tests cover every guard + state transition. Verified live end-to-end (RM Sophia → HR Diana partial → Director Noah full APPROVED). Per J3. (HD-5; tracker Phase HD)

### Added
- **Project budget changes now go through a Director approval gate.** PM submits a budget change (`POST /projects/:id/budget-change-requests`) with `{fiscalYear, capexBudget, opexBudget, reason?}`. The request lands as a PENDING `BudgetApproval` row with `requestedChange` JSON; the live `ProjectBudget` row is not mutated. Director approves (`POST .../:approvalId/approve`) → change applies atomically. Director rejects (`POST .../:approvalId/reject`, reason required) → live budget unchanged. Self-approval is forbidden (requester ≠ decider). Three new translator events flow through the dual-write seam. Reuses the existing `BudgetApproval` table — no schema change. Verified live end-to-end. Per J6. (HD-6; tracker Phase HD)

### Added
- **Project activation now goes through a Director approval gate.** PM submits a DRAFT project for approval (`POST /projects/:id/submit-for-approval`) → status flips to `PENDING_APPROVAL`. A Director then approves (`POST /projects/:id/approve` → ACTIVE) or rejects with a reason (`POST /projects/:id/reject` → DRAFT for revision). A `ProjectActivationApproval` row tracks every submission and decision; both services wrap their dual writes in `prisma.$transaction`. The Director who approves cannot also be the PM who submitted (no rubber-stamp). The legacy `POST /projects/:id/activate` direct path stays for admin/seed use, with a Swagger note pointing at the new flow. New translator events (`project.submitted_for_approval`, `project.approved`, `project.rejected`) flow through the dual-write seam alongside the existing `project.activated` event. Schema migration `20260503_hd_02_project_approval_gate` is REVERSIBLE. Closes D-12. (HD-2; tracker Phase HD)
- **Translator dual-write extended to 27 of 28 events.** Every domain notification (assignment lifecycle, project lifecycle, case lifecycle, staffing-request lifecycle, timesheets, employee lifecycle, proposal slates, SLA breaches) now flag-gates on `flag.outboxEnabled` and writes a transactional outbox row when on. Only `integration.sync_failed` is deferred — `OutboxEvent.aggregateId` is `@db.Uuid` and integration syncs lack a stable UUID natural key (deferral has a documented unblocking path). 30/30 parameterized unit tests prove every migrated event flag-gates correctly. (HD-0.3 Phase 2c; tracker Phase HD)
- **Translator dual-write extended to 8 events** via a generic `dualDispatch` helper + table-driven `OUTBOX_HANDLERS` registry. Each event now flag-gates between sync dispatch (default) and outbox-write (when `flag.outboxEnabled=true`). Migrated this iteration: `assignment.created`, `assignment.approved`, `assignment.rejected`, `assignment.sla_breached`, `project.activated`, `project.closed`, `staffing_request.submitted`, `staffing_request.fulfilled`. The 21 remaining events follow the same 5-line recipe. (HD-0.3 Phase 2b; tracker Phase HD)
- **NotificationEventTranslator dual-write seam (proof-of-concept).** `assignmentCreated()` is now flag-gated: when `flag.outboxEnabled=true` it writes an `OutboxEvent` row and short-circuits; when false (default) the legacy synchronous fan-out runs unchanged. The publisher consumes the row via a handler registered in `onModuleInit` that re-enters the same synchronous dispatch — at-least-once semantics now compose with the existing email + in-app fan-out. The remaining 28 translator events follow the same recipe. (HD-0.3 Phase 2a; tracker Phase HD)
- **Transactional outbox publisher infrastructure.** New `OutboxEventPublisherService` polls the `OutboxEvent` table on a configurable interval and dispatches each PENDING/RETRY row whose `availableAt` has elapsed to a handler registered against `(topic, eventName)` via the new `OutboxEventHandlerRegistry`. Failure → exponential-backoff retry (5s base, 5min cap, ±20% jitter) up to `outbox.maxAttempts` (default 5), then FAILED with `lastError` persisted. Schema migration `20260503_hd_03_outbox_retry_tracking` adds `attempts` + `lastError` columns (REVERSIBLE). Gated by `flag.outboxEnabled` (default false) so the existing synchronous notification translator stays in charge until consumers are wired in Phase 2. (HD-0.3 / Phase 21 F2.1-2.4; tracker Phase HD)

### Changed
- **Proposal-slate writes are now atomic at the database level.** `StaffingProposalSlateService.submit()` and `.rejectAll()` wrap their slate-save + staffing-request status flip in a single `prisma.$transaction`, so the slate row and the parent request's status can never disagree. The slate repo's `save()` joins the outer transaction when supplied (and falls back to its own internal one otherwise, preserving slate+candidates atomicity). `pickCandidate()` is deliberately untouched in this iteration — its cross-service `createAssignment` call needs a separate design pass before its outer tx composes cleanly. (HD-0.2 Phase 2b; tracker Phase HD)
- **Create-project-assignment writes are now atomic at the database level.** All 4 writes inside `CreateProjectAssignmentService` (the assignment, the initial approval row, the history entry, and the optional override-applied history) now share a single `prisma.$transaction`. The legacy `FIXME(DATA-05)` comment described the exact partial-failure window this closes. `ProjectAssignmentRepositoryPort` extended with `tx?: TransactionContext`; Prisma + in-memory impls updated; service constructor accepts an optional `prisma` argument with a pass-through fallback so existing in-memory test sites stay backward-compatible. (HD-0.2 Phase 2a; tracker Phase HD)
- **Create-employee writes are now atomic at the database level.** Previously the service used a "DATA-05 pseudo-transaction": save the Person, then save the membership, and on failure best-effort delete the orphan. That left a window where a crash mid-flow could leave a Person row with no org membership. The service now wraps both writes in `prisma.$transaction(async (tx) => …)`, so a failure on the second write rolls the first write back at the DB level — no orphans, no compensation logic. Repository ports gain an optional `tx?: TransactionContext` argument (opaque type at `src/shared/domain/transaction-context.ts` so domain stays Prisma-free); infrastructure adapters narrow internally. Fault-injection unit test included. (HD-0.2 / Phase 20c-05; tracker Phase HD)
- **Deprecated 4 legacy staffing endpoints** (`/assignments/:id/{approve,reject,end,revoke}`). They now respond with `Deprecation: true` + `Sunset: 2026-08-01` + `Link: <successor>; rel="successor-version"` headers and emit a structured `legacy_endpoint_call` warn log on every call. Internally each routes through the canonical `TransitionProjectAssignmentService` so behavior is unchanged. Frontend (`useAssignmentDetails.ts`, `useStaffingDeskActions.ts`) migrated to `bookAssignment` / `cancelAssignment` / `completeAssignment`. New CI ratchet `scripts/check-no-legacy-staffing-api.cjs` (baseline 0) refuses any re-introduction. Sunset day will delete the bespoke services + DTOs. (HD-1 Steps B+C; tracker Phase HD)

### Documented
- **Staffing endpoint audit** for the HD-1 / Phase WO-6 cutover. `docs/architecture/staffing-endpoints-audit.md` classifies every assignment endpoint (5 legacy → canonical mapping; 10 canonical kept; 6 CRUD/utility kept) and pins the FE migration to 2 call-site clusters (`useAssignmentDetails.ts`, `useStaffingDeskActions.ts`) totalling ~10 lines of change. (Tracker TASK 0.6)

### Added
- **Seed — populate SLA columns (`slaStage`/`slaDueAt`) for active-state assignments.** The Approval Queue showed `—` for SLA Stage and Due In on every PROPOSED/IN_REVIEW row because the seed inserted assignments directly into the DB, bypassing the runtime `TransitionProjectAssignmentService → AssignmentSlaService.applyTransition` path. The seed export now mirrors the service's `STATUS_TO_STAGE` map (CREATED→PROPOSAL/2bd, PROPOSED→REVIEW/1bd, IN_REVIEW→APPROVAL/2bd, ONBOARDING→RM_FINALIZE/1bd) so the queue badges match what live transitions would produce. The runtime path itself is unchanged. (D-32; tracker TASK 0.15)
- **Seed — 9 Clients in the IT-Company profile.** Live `it-company` seed had 0 `Client` rows; the Project list rendered `—` for every project's Client column. The seed now creates Acme Industries, Bluebird Logistics, Cascade Insurance, Delta Analytics, Echo Retail, Forestry Co., Gamma SaaS, Helios Bank, and Jade Manufacturing, then wires `Project.clientId` on all 9 customer-facing active projects (Internal DeliveryCentral stays null) and round-robins closed projects across the same accounts. Unblocks HD-3 (RateCard CLIENT-scope testing). (D-35; tracker TASK 0.13)

### Fixed
- **HR — Create Employee no longer silently drops skills.** The form's Skillsets checkbox grid was writing to the legacy `Person.skillsets[]` free-text array while the Person 360 → Skills tab reads from the modern `PersonSkill` join. Result: every hired employee showed "No skills recorded" regardless of what HR ticked. The form now picks from the canonical Skill catalog (`/api/admin/skills`) and writes `PersonSkill` rows at proficiency 3 (refine in the Skills tab after creation). HR can no longer create custom skill names from this form — prevents data drift, matches the Skills-tab edit pattern. (D-30, D-46; tracker TASK 0.16)
- **HR — Create Employee confirm dialog wording.** Replaced the misleading "permanently create…cannot be undone" with an accurate "INACTIVE status; line manager will be notified; can deactivate or terminate later." (D-44)
