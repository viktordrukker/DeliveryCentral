# DS Canvas ↔ FE Conformance — DeliveryCentral V2

_Generated by the autonomous QA run (Phase 2). DS canvases: `/DS/page-*.jsx` (local-only spec) vs FE routes in `frontend/src/routes/`. Verdict per canvas: MATCH (all sections present, correct order, DS primitives, no raw hex) / PARTIAL (named gaps) / MISSING / NO-CANVAS (FE surface with no spec, or canvas not a page spec)._

## Summary
- Canvases assessed: **17** — **1** NO-CANVAS, **15** PARTIAL, **1** MATCH.
- Reverse-pass: V2 nav routes with **no DS canvas** → `/admin`, `/admin/settings`.
- Token discipline: no raw-hex *violations* in implementing files (the few hex hits are in `design-tokens.ts`, the sanctioned single-source token file — guardrail-exempt). Minor: see per-canvas notes.

## Conformance matrix

| Canvas | Implementing route | Verdict | Missing | Extra | Token/primitive notes |
|--------|--------------------|---------|:------:|:-----:|-----------------------|
| `page-admin-setup` | /admin (AdminPanelPage, ~30 sub-pages) + /setup (SetupWizardPage) | NO-CANVAS | 4 | 3 | 2 note(s) |
| `page-approvals` | /approvals | PARTIAL | 3 | 7 | 2 note(s) |
| `page-bench` | /people/bench | PARTIAL | 5 | 4 | · |
| `page-bulk-positions` | /staffing-requests/bulk (router.tsx:451; lazy import router.tsx:147; guarded by STAFFING_DESK_ROLES) | PARTIAL | 6 | 3 | 3 note(s) |
| `page-case-detail` | /cases/:id | PARTIAL | 6 | 4 | 4 note(s) |
| `page-director` | /dashboard/director (router.tsx:214, RoleGuard DIRECTOR_ADMIN_ROLES, FeatureGuard flag=dashDirector) | MATCH | 0 | 1 | · |
| `page-directory-hr` | /people (EmployeeDirectoryPage, exported as PeoplePage); HR Queue tab mounts CasesPanel | PARTIAL | 8 | 5 | 3 note(s) |
| `page-extras` | /projects/new (CreateProjectWizard) ; /login (AuthForm) ; /projects/:projectId/positions/:positionId (PositionDetail) | PARTIAL | 10 | 3 | 2 note(s) |
| `page-plan-money` | /projects/:id?tab=money (also ?tab=plan) | PARTIAL | 8 | 1 | 2 note(s) |
| `page-profile` | /people/:id | PARTIAL | 10 | 4 | 3 note(s) |
| `page-projects` | /projects | PARTIAL | 7 | 5 | · |
| `page-pulse` | /projects/:id (tab=pulse, dsRefresh flag on) — router.tsx:310 | PARTIAL | 6 | 2 | 2 note(s) |
| `page-reports` | /reports | PARTIAL | 7 | 7 | 2 note(s) |
| `page-staffing-desk` | /staffing-desk (?view=table board + ?view=planner Distribution Studio); Find-candidates lives on /projects/:id/positions/:posId per canvas's own path note | PARTIAL | 4 | 4 | 2 note(s) |
| `page-time-management` | /time-management | PARTIAL | 11 | 6 | 7 note(s) |
| `page-timeline-ds` | (DS component spec — no app route; chrome.jsx section '17 · Timeline component (DS)'). Implemented as the <Timeline/> primitive used by Staffing Desk, Project Plan, Distribution Studio. | PARTIAL | 3 | 3 | 4 note(s) |
| `page-workspace` | /me (flag.workspaceMe) | PARTIAL | 7 | 3 | · |

## Gaps by canvas (PARTIAL / NO-CANVAS detail)

### `page-admin-setup` → /admin (AdminPanelPage, ~30 sub-pages) + /setup (SetupWizardPage) — **NO-CANVAS**
- **Missing (in canvas, absent in FE):** ENTIRE admin/setup spec absent: DS/page-admin-setup.jsx is a mislabeled duplicate of the Foundations/tokens reference sheet (header reads '// ds-foundations.jsx', component is FoundationsView, registers window.FoundationsView). It renders token swatches (Surfaces/Text/Brand accent/Status/Chart palette/Util heatmap), type scale, spacing/radii/shadows, and breakpoints — NOT an admin or setup page.; No AppShell/Sidebar/Topbar/PageHeader chrome (file does not wrap in chrome.jsx like real page-*.jsx specs do).; No admin control surface spec (no settings tabs, RBAC/governance, integrations registry, monitoring, feature flags, rate cards, etc.).; No setup-wizard spec (no preflight/migrations/tenant/admin/integrations/monitoring/seed/complete screens).
- **Extra (in FE, not in canvas):** FE /admin AdminPanelPage renders a consolidated admin control surface with no canvas counterpart.; FE ships ~30 admin sub-pages with no canvas: Settings, IntegrationsAdmin, IntegrationsRegistry, SsoAdmin, Monitoring, FeatureFlags, RateCards, LeavePolicy, PeriodLocks, AccessPolicies, RolePermission, ResponsibilityMatrix, CustomRole, BusinessAudit, Notifications, Webhooks, HrisConfig, Vendors, BulkImport, Help, RadiatorThresholds, OrganizationConfig, V2SoakChecklist, SetupOps, etc.; FE ships a full /setup SetupWizardPage (routes/setup/{SetupWizardPage.tsx,useSetupWizard.ts,screens,components}) plus app/setup-gate.tsx — entirely unspecced by this canvas.
- **Order:** N/A — canvas contains no page section list to order against (it is a tokens reference, not a page spec).
- **Token deviations:** Not assessed against this canvas: NO-CANVAS means there is no admin/setup spec to validate FE token usage against. (The canvas file ITSELF is token-clean — uses var(--color-*) / var(--shadow-*) / var(--radius-*) throughout, consistent with the token system, but it specs Foundations, not admin/setup.)
- **Non-DS primitives:** Not assessed: no canvas admin/setup primitive contract exists to compare the FE AdminPanelPage / SetupWizardPage primitives against.
- _Evidence:_ DS/page-admin-setup.jsx:1 header literally reads '// ds-foundations.jsx — Tokens reference: colors, type, spacing, radii, shadows.'; line 31 'function FoundationsView()'; line 198 'window.FoundationsView = FoundationsView'. diff page-admin-setup.jsx vs ds-foundations.jsx: only the line-1 header comment differs (md5 df293281... vs the chrome diff was a mismatched pairing; the two files share the id

### `page-approvals` → /approvals — **PARTIAL**
- **Missing (in canvas, absent in FE):** Inspector header 'High priority' StatusBadge (canvas page-approvals.jsx:46 renders <StatusBadge tone="danger">High priority</StatusBadge>; FE ApprovalInspector.tsx:253-281 header has source tag + title + submitter/age/SLA chip but no priority badge); Inspector quick-links row 'Full EVM / Change request / Prior approvals (3)' (canvas:116-121, three btn-tertiary buttons; absent from both BudgetApprovalInspector.tsx and ApprovalInspector.tsx); Footer 'Escalate to Director' button (canvas:132); FE ApprovalInspector.tsx:20-23,446-448 intentionally omits Escalate — no generic /api/approvals/:id/escalate endpoint, replaced by 'Open source ↗' deep-link (documented UX-Law-2 deviation, not an accidental gap)
- **Extra (in FE, not in canvas):** SLA-breached header StatusBadge (ApprovalsPage.tsx:255-257); Refresh title-bar action (ApprovalsPage.tsx:227-235); Generic non-budget inspector layouts: Leave detail tiles (ApprovalInspector.tsx:343-382) and position 'Proposed fill' tiles (ApprovalInspector.tsx:384-418) — canvas only specs the budget inspector; Submitter-rationale block (ApprovalInspector.tsx:420-425; BudgetApprovalInspector.tsx:270-275); View-only hint for non-decider roles (ApprovalInspector.tsx:480-489); Two extra queue sources beyond the canvas 5: 'skill-review' and 'timesheet' (ApprovalsPage.tsx:29-30,SOURCES); Per-chip result counts (ApprovalsPage.tsx:199-220) — canvas hard-codes counts in chip text but FE derives live counts
- **Order:** Source filter chips are RELOCATED, not reordered within the page: canvas places them in the left column above the list (page-approvals.jsx:160-167); FE injects them into the GLOBAL TITLE BAR via useTitleBarActions (ApprovalsPage.tsx:189-224, W3-03 note). Column orientation matches canvas (list left, inspector right rail). Inspector body sub-section order matches canvas exactly: key-figure tiles -> variance breakdown -> forecast sparkline -> comment -> footer. Inspector is single-column-default in FE (right rail appears only on row select, ApprovalsPage.tsx:293-300) whereas canvas always shows the 2-col grid with inspector pre-populated.
- **Non-DS primitives:** frontend/src/routes/approvals/ApprovalsPage.tsx:396-403 — list-row 'Open →' uses <Link className="btn btn-tertiary btn-sm"> (raw global btn classes) instead of the DS <Button as={Link}> atom used elsewhere on the same page (ApprovalsPage.tsx:225-226,491); frontend/src/routes/approvals/ApprovalsPage.tsx:301,322 — list card + rows built from raw <div className="card"> + <div role="button"> rows rather than DS Table/DataView; this matches the canvas (canvas also hand-rolls the list with divs, page-approvals.jsx:170-200) so it is canvas-faithful, not a regression
- _Evidence:_ Route: route-manifest.ts:294 (/approvals, group 'staffing'->workspace V2, navVisible, flag dsRefresh) + router.tsx:222-223 -> ApprovalsPage. Chrome contract (canvas AppShell active='approvals' context='Approvals', page-approvals.jsx:142) satisfied by the nav-visible Workspace entry. PageHeader present (ApprovalsPage.tsx:244-260) with title 'Approvals' + awaiting/breached badges + subtitle; canvas 

### `page-bench` → /people/bench — **PARTIAL**
- **Missing (in canvas, absent in FE):** Filter bar '+ Filter' add-filter button (canvas page-bench.jsx:71) — FE filter bar (BenchEnrichedPanel.tsx:364-415) has search+chips+sort but no add-filter affordance; Master list 'Cost/d' column (canvas page-bench.jsx:111,139) — FE table (BenchEnrichedPanel.tsx:523-671) omits cost column; DTO carries no cost rate (documented BenchEnrichedPanel.tsx:18-24); Inspector definition-list meta (canvas page-bench.jsx:179-185: Last project / Released / Cost-day / Office / Manager) — FE inspector (BenchInspector.tsx:172-190) renders only Days idle / Availability 14d / Headroom; no Released/Cost/day/Manager; Inspector 'Skills' chip section (canvas page-bench.jsx:187-192) — absent from BenchInspector.tsx entirely; Inspector per-fill 'Compare' button + alloc badge + cost StatusBadge (canvas page-bench.jsx:217-223) — FE suggested-fill rows (BenchInspector.tsx:213-251) show only role/project/match% + project deep-link, no Compare/alloc/cost
- **Extra (in FE, not in canvas):** Per-row & header select-all checkboxes wired to a live selection set (BenchEnrichedPanel.tsx:523-568) — canvas shows static checkboxes only; Inspector prev/next stepper '<index/total>' (BenchInspector.tsx:134-167) — canvas footer has 'Previous bench'/'Skip' buttons instead; Inspector 'Headroom %' metric (BenchInspector.tsx:186-189) — not in canvas; no-fill-history basis note (BenchInspector.tsx:176-180) — not in canvas
- **Order:** Section order matches canvas (PageHeader -> KPI strip -> filter bar -> bulk-action bar -> master/inspector grid). No reordering. Content swaps within sections: KPI tile 3 'Cost of bench (week)' (canvas page-bench.jsx:49-53) is replaced by 'Availability · 14d' (BenchEnrichedPanel.tsx:339-348); bulk-bar actions differ — canvas 'Propose to position / Add to slate / Plan training' (page-bench.jsx:93-95) vs FE 'Suggest position / Send message / Export' (BenchEnrichedPanel.tsx:442-489); breadcrumb root 'Workforce' (canvas page-bench.jsx:26) vs 'People' (BenchEnrichedPanel.tsx:297).
- _Evidence:_ Canvas DS/page-bench.jsx (List-Detail + Inspector). Implementing route /people/bench (route-manifest.ts:291, group people-org->workforce, navVisible, titleV2 'Bench'); router.tsx:430 -> BenchPage; BenchPage.tsx:99-105 renders BenchEnrichedPanel under dsRefresh flag (legacy SectionCard+Table path is fallback). BenchEnrichedPanel.tsx covers PageHeader (294-315), 4-tile KPI strip (318-359, each a Lin

### `page-bulk-positions` → /staffing-requests/bulk (router.tsx:451; lazy import router.tsx:147; guarded by STAFFING_DESK_ROLES) — **PARTIAL**
- **Missing (in canvas, absent in FE):** Count column on the entry grid — canvas spec (page-bulk-positions.jsx:101-104, BULK_ROWS count field, spec-note lines 16-17) makes count×alloc the core 'one row → N positions' expansion. FE columns are idx/project/role/start/end/alloc/status/remove (BulkCreatePositionsPage.tsx:375-546) — no count; each row creates exactly one ProjectPosition (handleSubmit loop, lines 196-217).; Aggregate validation banner — canvas section 4 (banner banner-danger, page-bulk-positions.jsx:140-147: 'N rows block submission · M warnings' + 'Jump to error'). FE has no banner; surfaces a generic ErrorState 'Fix N invalid row(s)' only after submit (BulkCreatePositionsPage.tsx:186-189, 260).; Summary footer metrics — canvas section 5 (page-bulk-positions.jsx:151-163) shows Rows / Positions / Total FTE (Σ count×alloc%) tiles. FE has only a one-line counter '{n} rows · pending · opened · failed' (BulkCreatePositionsPage.tsx:322-325); no Positions total, no Total FTE.; Paste-from-spreadsheet hint — canvas add-row strip (page-bulk-positions.jsx:135 'or paste rows… ⌘V maps role → count → alloc → start → end'). FE add-row strip has no paste hint (BulkCreatePositionsPage.tsx:312-326).; Per-row validation 'Check' state via StatusBadge — canvas Check column renders StatusBadge tone=active/danger/warning with the message (page-bulk-positions.jsx:121-125). FE Status column instead shows submission lifecycle (Pending/Opening…/Opened/Failed) as a plain colored <span> (BulkCreatePositionsPage.tsx:518-544), not the live validation verdict.; Warning (non-blocking) row state — canvas distinguishes error (blocks) vs warning (freeze-window, submittable, routes to Director approval) with row tint + warning StatusBadge (page-bulk-positions.jsx:38, 113-114, 124). FE validateRow only produces hard errors (BulkCreatePositionsPage.tsx:97-111); no warning tier.
- **Extra (in FE, not in canvas):** Default-project selector + 'Apply to empty rows' button (BulkCreatePositionsPage.tsx:270-296) — not in canvas (canvas fixes one project via the drawer header 'Change project' picker, page-bulk-positions.jsx:83-91; FE instead has a per-row Project select column, line 383-407).; 'Open immediately (skip DRAFT)' checkbox (BulkCreatePositionsPage.tsx:328-337) — not in canvas; canvas spec-note states positions are always created OPEN on submit (page-bulk-positions.jsx:25-26).; Per-row submission Status column with retry semantics (Pending/Opening/Opened/Failed, BulkCreatePositionsPage.tsx:518-544; partial-failure retry lines 176-233) — canvas models submit as an atomic disabled-while-error action with no per-row progress.
- **Order:** Header → entry grid → add-row strip → submit/cancel ordering is preserved. The canvas's two terminal zones are absent/relocated: the aggregate validation banner (canvas places it after the grid, before the footer) does not exist; the summary footer is collapsed into the inline counter + button row. Also FE is a full PageContainer page, not the wide drawer-over-Desk the canvas specifies (page-bulk-positions.jsx:1-5, 75-81) — so the 'land back on the filtered Desk' continuity is via navigate('/staffing-desk?view=table') (BulkCreatePositionsPage.tsx:223,350) rather than a drawer dismiss.
- **Non-DS primitives:** BulkCreatePositionsPage.tsx:533-542 — status cell is a raw <span> with inline token color; canvas (page-bulk-positions.jsx:122-124) specifies <StatusBadge> here. StatusBadge atom exists at components/common/StatusBadge.tsx but is not used.; BulkCreatePositionsPage.tsx:298-310 — DS Table is used (good) but error/warning row tint is not applied; DS Table exposes rowStyle (components/ds/Table.tsx:143, applied line 465) which the canvas row-tint (page-bulk-positions.jsx:112-115) maps to, so the affordance exists and is unused.; BulkCreatePositionsPage.tsx:328-337 — raw <input type=checkbox> + <label> for 'Open immediately'; no DS checkbox atom in components/ds, so this is a forced raw primitive (and the field itself is extra vs canvas).
- _Evidence:_ Implementing component BulkCreatePositionsPage.tsx routed at /staffing-requests/bulk (router.tsx:451). DS primitives used: Table + Button from @/components/ds (BulkCreatePositionsPage.tsx:11,298-310,287-295), SectionCard/PageContainer/PageHeader from common (lines 8-10,241-247). No raw hex (grep #[0-9a-fA-F]{3,8} → 0 hits); no raw <table>/<button> (grep → 0 hits); inline colors all use var() token

### `page-case-detail` → /cases/:id — **PARTIAL**
- **Missing (in canvas, absent in FE):** Case timeline (event-stream timeline, newest-first, tone-dotted) — canvas LEFT col card #1; FE renders a 'Workflow Steps' task table instead (CaseDetailsPage.tsx:475-533), no DS Timeline / chronological event stream; Decision rail (canvas RIGHT rail #1, section-card-emphasized + sticky): single shared decision-note textarea + terminal Resolve(primary)/Escalate/Reassign trio + active mitigation banner — FE has a flat 'Case Actions' card with Approve/Reject/Close/Cancel/Archive and no Escalate, no Reassign, no decision banner (CaseDetailsPage.tsx:361-423); Linked person card (avatar + name + role/grade/office/mgr + leave balance + drill chevron) — canvas RIGHT rail #2; FE has no person card, only a 'Subject Person'/'Owner Person' text link inside Case Summary (CaseDetailsPage.tsx:436-451); Linked position card with overlap warning banner (canvas RIGHT rail #3: POS code + StatusBadge + role/alloc/project + conflict banner) — FE collapses this to a bare 'Related Position' link row in 'Linked Context' with no status badge and no conflict banner (CaseDetailsPage.tsx:582-593); Comment composer with Avatar + @-mention affordance and per-comment avatars — FE Comments render as a compact Table of body/authorPersonId/date with no Avatar and author shown as raw personId (CaseDetailsPage.tsx:535-566,544-546); Header SLA badge + status badge cluster via StatusBadge (canvas PageHeader badges) — FE shows status as plain KPI string and SLA as a separate inline-styled banner below the KPI strip, not header badges (CaseDetailsPage.tsx:322-357)
- **Extra (in FE, not in canvas):** KPI strip of 4 SummaryCards (Case Number / Type / Status / Participants) — not in canvas, which puts case meta in the RIGHT-rail 'Case' card; these tiles are non-clickable (no href), violating UX Law 9 (CaseDetailsPage.tsx:321-326,633-642); Workflow Steps section with add/complete/remove custom steps — not in canvas (canvas deliberately omits step-management; it specs an event timeline) (CaseDetailsPage.tsx:475-533); Participants table section — not present in canvas case-detail spec (CaseDetailsPage.tsx:597-620); Separate 'Linked Context' card (Related Project + Related Position) — canvas has no project card; position lives in its own rail card (CaseDetailsPage.tsx:568-595)
- **Order:** Canvas is a 2-col grid (LEFT timeline+comments / RIGHT rail decision-first → person → position → meta) per UX Law 8 one-screen resolution. FE is a single vertical stack: PageHeader → KPI strip → SLA banner → Case Actions → uniform 4-tile 'dashboard-main-grid' (Case Summary, Workflow Steps, Comments, Linked Context, Participants). Decision actions are NOT in a sticky right rail; linked entities are de-emphasized below the fold; timeline is absent. Resolution is not one-screen as the canvas requires.
- **Non-DS primitives:** frontend/src/routes/cases/CaseDetailsPage.tsx:329-356 — raw inline-styled <div>/<span> SLA banner instead of a DS banner atom (canvas uses .banner banner-warning/banner-active); frontend/src/routes/cases/CaseDetailsPage.tsx:488-493 — step status rendered as raw inline-styled <span> chips instead of common/StatusBadge (which the canvas uses for status/SLA tones); frontend/src/routes/cases/CaseDetailsPage.tsx:544-546 — comment author shown as raw personId text with no ds/Avatar (canvas Comments use Avatar); frontend/src/routes/cases/CaseDetailsPage.tsx:633-642 — SummaryCard tiles are non-interactive (no href/Link), breaching UX Law 9 clickable-KPI rule
- _Evidence:_ Canvas /home/drukker/DeliveryCentral/DS/page-case-detail.jsx:43-176 specs AppShell(active='hr') with PageHeader(breadcrumb People>HR Queue>HR-0142, status+SLA StatusBadges) then a 1fr/360px grid: LEFT = 'Case timeline' card (CASE_EVENTS reversed newest-first, tl-row tone dots, lines 64-80) + 'Comments' card with Avatar rows and a composer (lines 82-110); RIGHT rail = section-card-emphasized 'Decis

### `page-directory-hr` → /people (EmployeeDirectoryPage, exported as PeoplePage); HR Queue tab mounts CasesPanel — **PARTIAL**
- **Missing (in canvas, absent in FE):** Directory table columns: canvas table = Person / Workstream / Office / Status / Util·L30D (inline util progress bar) / Joined date. FE EmployeeDirectoryTable (EmployeeDirectoryTable.tsx:31-72) renders Person Name / Org Unit / Line Manager / Dotted-Line Summary / Active Positions / Status — MISSING the Office column, the Util·L30D utilization progress bar (canvas page-directory-hr.jsx:102-109), and the Joined date column.; Directory row select: canvas has a per-row + header checkbox column for bulk selection (page-directory-hr.jsx:71,84). FE directory table has no row-checkbox column (selection only exists inside the separate dsRefresh bulk-reassign panel, not in the directory table itself).; HR Queue is the wrong surface: canvas HRQueue (page-directory-hr.jsx:151-326) specifies an HR approval queue. FE mounts a generic CasesPanel instead. MISSING: the 4-tile KPI strip (Awaiting approval / Approved·L7D / Onboardings in flight / Avg leave balance, page-directory-hr.jsx:177-198).; HR Queue bulk action bar (3 selected · Approve all 3 / Reject with comment / Reassign approver / Clear, page-directory-hr.jsx:200-208) — absent from CasesPanel.; HR Queue leave-approval table with inline Comment / Reject / Approve actions per row and columns Employee / Request / Dates / Manager / Balance / Flag / Decide (page-directory-hr.jsx:213-257). FE CaseListTable columns are Case / Status / Subject / Owner / Participants / Summary / Opened (CaseListTable.tsx:44-89) with no inline approve/reject.; HR Queue 'Active onboarding cases' panel with per-case onboarding checklist + progress meter (page-directory-hr.jsx:262-322) — absent.; HR Queue sub-tabs Leave / Onboarding / Offboarding / Skills review (page-directory-hr.jsx:166-172) — absent (CasesPanel is a flat case list, no leave/onboarding/offboarding segmentation).; HR Queue header badges '3 awaiting you' + 'SLA · 24h' and HR-specific actions (Filters / History, page-directory-hr.jsx:157-165) — not present on the HR Queue tab.
- **Extra (in FE, not in canvas):** Bulk org-unit reassign panel inside the Directory tab (accent-soft box with org-unit select, effective-from date, per-person checkbox list, ConfirmDialog) — EmployeeDirectoryPage.tsx:362-482; not specified in the canvas.; Directory FilterBar adds Department / Resource Pool / Status(lifecycle) / Role / Grade selects and a Group-by select (flat/role/grade/office) — EmployeeDirectoryPage.tsx:483-602. Canvas filter-bar specifies only search + role/London/L4+/Active chips + view toggle + group-by-workstream (page-directory-hr.jsx:44-63).; List-detail PersonDirectoryInspector right-rail drawer in flat+list layout — EmployeeDirectoryPage.tsx:697-730; canvas directory has no inspector (it navigates via the row chevron).; ColumnVisibilityMenu toolbar on the directory table — EmployeeDirectoryTable.tsx:87-92; not in canvas.; Directory grid/card layout variant — EmployeeDirectoryPage.tsx:640-680; canvas defines a Grid toggle but the spec only renders the list view (view==='list').
- **Order:** Top-level shell order matches the canvas (PageHeader → Tabs → FilterBar → table). The bulk-reassign panel is injected between the FilterBar and table inside the Directory tab (EmployeeDirectoryPage.tsx:362 before FilterBar at 483), so it actually renders ABOVE the FilterBar — not a canvas-specified zone. Within the HR Queue tab the canvas order (KPI strip → bulk bar → approval table → onboarding panel) cannot be assessed because those zones are not implemented; CasesPanel renders FilterBar → SectionCard(case list) only.
- **Non-DS primitives:** EmployeeDirectoryPage.tsx:381,496,510,523,539,553,567 — raw <select> filter controls (use the platform .field/.field__control pattern, consistent with the canvas which also uses raw <select className='select input-sm'>; not a DS-atom violation, listed for completeness).; EmployeeDirectoryPage.tsx:395 — raw <input type='date'> in bulk-reassign panel (.field__control class, matches canvas input pattern).; EmployeeDirectoryPage.tsx:461 — raw <input type='checkbox'> in bulk-select list (no DS Checkbox atom; canvas uses className='checkbox').
- _Evidence:_ Implementing route: PeoplePage.tsx:1 re-exports EmployeeDirectoryPage; router.tsx:293 binds it to path 'people'. Directory shell matches canvas: PageHeader title 'People' (EmployeeDirectoryPage.tsx:331-349) with people/bench badges, 3-tab TabBar Directory/Bench/HR Queue (EmployeeDirectoryPage.tsx:267-290,351-357), FilterBar (483-602), table region (604-749) — all DS primitives (PageHeader, TabBar,

### `page-extras` → /projects/new (CreateProjectWizard) ; /login (AuthForm) ; /projects/:projectId/positions/:positionId (PositionDetail) — **PARTIAL**
- **Missing (in canvas, absent in FE):** CreateProjectWizard: step-2 'Positions & Milestones' content missing entirely — no 'Start from template' chip row (canvas L52-59), no Positions table Role/Skills/Alloc/Start/Weeks (canvas L69-99), no Milestones GanttRow list + totals (canvas L109-125). FE step 2 renders only engagement fields (ProjectLifecycleForm.tsx:141-181). FE comment at CreateProjectPage.tsx:104-119 admits positions/milestones are deferred to the project Plan tab.; CreateProjectWizard: banded stepper with done/current/todo states + check-icon (canvas L16-41) is not implemented — FE uses 3 tab-style Buttons (ProjectLifecycleForm.tsx:73-85).; CreateProjectWizard: 'Save draft' footer button (canvas L130) missing — FE footer has only Previous / Next / Create Project (ProjectLifecycleForm.tsx:210-230).; AuthForm: entire left hero panel missing — brand mark+DeliverIT (canvas L163-166), eyebrow + headline (canvas L169-175), 3-tile stat grid Projects/People/Open positions (canvas L178-189), version/WCAG footer (canvas L192-195). FE is a single centered card (LoginPage.tsx:88-99).; AuthForm: two-column split layout (canvas L144 gridTemplateColumns '1fr 1fr') missing — FE centers one Card (LoginPage.tsx:89-98).; AuthForm: 'Remember this device for 30 days' checkbox (canvas L224-227) missing from FE form (LoginPage.tsx:131-179).; AuthForm: AUP / audited-activity footer line (canvas L230-232) missing.; PositionDetail right rail: SLA countdown card with progress bar (canvas L364-373) missing.; PositionDetail right rail: 'Approval chain' timeline card (canvas L374-392) missing — FE surfaces approvals as a main-column 'Pending approvals' table instead.; PositionDetail right rail: 'Quick keys' keyboard-shortcut card (canvas L407-423) missing.
- **Extra (in FE, not in canvas):** PositionDetail main column: 'Position lifecycle' WorkflowStages card (ProjectPositionDetailPage.tsx:638-644) — not in canvas.; PositionDetail main column: 'Pending approvals' table card (ProjectPositionDetailPage.tsx:668-711) — canvas placed approval chain in the right rail, not a main-column table.; PositionDetail right rail: 'Quick actions' card (ProjectPositionDetailPage.tsx:731-758) — canvas right rail has SLA / Approval chain / Linked / Quick keys; no Quick-actions card.
- **Order:** CreateProjectWizard: canvas step order is Identity -> Positions & Milestones -> Activation; FE renames middle step to 'Setup' (engagement fields) so the wizard's defining middle stage is replaced, not reordered (ProjectLifecycleForm.tsx:74). PositionDetail main column: canvas order Current fill -> Position spec -> Candidates -> Fill history; FE inserts 'Position lifecycle' between spec and candidates and 'Pending approvals' between candidates and history (ProjectPositionDetailPage.tsx:561,638,647,668,714). PositionDetail right rail order differs: canvas SLA/Approval chain/Linked/Quick keys vs FE Quick actions/Linked (ProjectPositionDetailPage.tsx:731,760).
- **Non-DS primitives:** /tmp/dc-qa/frontend/src/routes/auth/LoginPage.tsx:3-13 — login built entirely on raw MUI (Box, Card, CardContent, TextField, Button, Alert, Divider, Typography) instead of DS atoms (Button from @/components/ds, field/input pattern, Card/SectionCard); the canvas AuthForm uses .btn/.input/.field/.checkbox primitives.; /tmp/dc-qa/frontend/src/routes/auth/LoginPage.tsx:99-211 — entire form/card markup is MUI sx-based, no DS Card/SectionCard or DS Button used.
- _Evidence:_ CreateProjectWizard: FE step labels Identity/Setup/Activation at ProjectLifecycleForm.tsx:74; step-2 body is engagement-only fields (ProjectLifecycleForm.tsx:141-181) with NO positions table, milestones gantt, or template chips that the canvas mandates at page-extras.jsx:52-125; CreateProjectPage.tsx:104-119 comment explicitly defers positions+milestones to the project Plan tab. AuthForm: FE rende

### `page-plan-money` → /projects/:id?tab=money (also ?tab=plan) — **PARTIAL**
- **Missing (in canvas, absent in FE):** Money: hero 'Plan vs Actual · with forecast (EAC)' EVM line chart (PV dashed / EV / AC lines + EAC forecast curve + Today marker) — canvas page-plan-money.jsx:402-415 / PlanVsActualChart; no chart/svg rendered anywhere in MoneyTab subtree (MoneyTab.tsx, MoneyPanel.tsx); Money: 'Change requests · this quarter' table with chips (All/Pending/Approved) + '+ CR' — canvas:472-502; not on Money tab (CRs live on separate Plan tab / ?tab=change-requests; banner links there); Money: 'By fiscal period · monthly rollup' table (Period/Planned/Actual/Earned/CPI/SPI + sparkline) — canvas:504-538; absent; Money KPI strip: canvas tiles are BAC/AC/EV/CPI/SPI/EAC (6) — FE strip is BAC/EAC/EV/Variance/Remaining/Burn; the discrete Actual(AC), CPI and SPI tiles are absent (CPI/SPI surfaced only via the extra CpiWhatIfCard) — MoneyPanel.tsx:95-153 vs canvas:368-400; Money PageHeader actions: canvas currency select (USD/GBP/Both), Export, 'Change request' — canvas:344-348; FE header (DetailLayout) exposes none of these on Money tab, only 'Create Position' — ProjectDetailPage.tsx:262-284; Plan tab: dedicated 'Open positions · awaiting fill' SLA table (open-for days, candidate count, View candidates/Propose) — canvas:211-264; not reproduced as a discrete section (positions surface via TeamVendorsTab swim-lane gantt); Plan tab: segmented 'Gates' milestone strip (6 weighted gate bars) — canvas:133-159; not a discrete strip (milestones render as MilestonesTab gantt+register); Plan tab: plan-focused KPI strip (Schedule/Open positions/Filled/Active milestone/Effort) — canvas:104-131; intentionally removed (PlanTab.tsx:19-23 comment: KPI strip belongs on Pulse only)
- **Extra (in FE, not in canvas):** CpiWhatIfCard — interactive CPI what-if projection (scenario rows, Project CPI button, baseline/projected/delta result) appended to Money tab — CpiWhatIfCard.tsx; not in canvas ProjectMoney section (LEAN-P4-missing-7 add-on)
- **Order:** Money tab order matches canvas where sections exist (banner → 6-tile KPI strip → variance-drivers+cost-lines 2-col grid), but the hero chart that canvas places between KPI strip and the 2-col grid is absent, and the CpiWhatIfCard is appended after the grid (canvas has no equivalent there). Change-requests and fiscal-rollup tails are missing entirely. Plan tab reshapes canvas Gates/Gantt/Open-positions into MilestonesTab+RisksIssuesTab+ChangeRequestsTab+TeamVendorsTab stacked sections (PlanTab.tsx:24-35).
- **Non-DS primitives:** frontend/src/routes/projects/tabs/MoneyPanel.tsx:220-230 — hand-rolled .varbar with raw <i> bar for byRole driver rows (the Budget-variance row above it at :184-191 correctly uses the DS VarianceBar atom; the per-role rows do not, mixing primitives); frontend/src/routes/projects/tabs/MoneyPanel.tsx:84-90 — over-budget CTA is a react-router <Link> styled with .btn .btn-primary .btn-sm instead of the DS Button atom (Button is used elsewhere e.g. ProjectDetailPage.tsx:272-281)
- _Evidence:_ Route /projects/:id → ProjectDetailPage (router.tsx:310); V2 3-tab grammar Pulse/Plan/Money (ProjectDetailPage.tsx:52-56), Money renders <MoneyTab> (ProjectDetailPage.tsx:310). MoneyTab = MoneyPanel + CpiWhatIfCard (MoneyTab.tsx:64-68). MoneyPanel renders banner (MoneyPanel.tsx:69-92), 6-tile KPI strip BAC/EAC/EV/Variance/Remaining/Burn (MoneyPanel.tsx:95-153), and 2-col Variance-drivers + Top-cos

### `page-profile` → /people/:id — **PARTIAL**
- **Missing (in canvas, absent in FE):** PageHeader badges trio: canvas shows StatusBadge 'Proposed · MCB-UK' + outline 'L5 · Senior Engineer' + mono 'EID-04812' (page-profile.jsx:46-50); impl PageHeader only shows lifecycle StatusBadge (EmployeeDetailsPage.tsx:393), and the canonical dsRefresh-ON path's identityCard collapses identity into a single line with no employee-ID / grade-badge trio (PersonProfilePanel.tsx:133-166); PageHeader 'Propose to position' + 'Message' header actions (page-profile.jsx:53-54): impl header actions are lifecycle-only (Deactivate/Terminate, EmployeeDetailsPage.tsx:292-319); Message/Propose moved into the right-rail Quick actions instead (PersonProfilePanel.tsx:505-513), not the header; Tab counts: canvas tabs carry counts Positions:4 / Skills:12 (page-profile.jsx:59-60); impl V2_TABS have no counts (PersonProfilePanel.tsx:36-43). The OFF-path TabBar shows a Positions count but no Skills count (EmployeeDetailsPage.tsx:405-415); KPI strip content mismatch: canvas tiles = Utilization·L30D (with Sparkline), Billable hours (with delta), Cost/day, Leave balance (page-profile.jsx:71-92); impl tiles = Active positions, Total allocation, Last-4-weeks hours, Overtime (PersonProfilePanel.tsx:168-207). No Sparkline, no delta, no utilization %, no leave-balance KPI tile; Identity + Employment two-card dl block (page-profile.jsx:94-126: Employee ID/Email/Office/Working pattern/DOJ/Time-in-company and Grade/Role family/Org unit/Line manager/Cost centre/Employment type) — not rendered on the canonical dsRefresh-ON Overview tab, which shows only Manager chain + Resource pools cards (PersonProfilePanel.tsx:453-458, 391-437). Most of those fields (office, working pattern, cost centre, role family, employment type, time-in-company) are absent entirely; Position history as a dedicated table with Project/Role/Alloc/Start/End/Status columns (page-profile.jsx:128-163): impl Positions tab renders a DS Timeline + DS Table but omits the End-date column and Start/End split, using Since-only (PersonProfilePanel.tsx:281-291); Cost rates history table (Effective / Kind / Rate-per-day rows, page-profile.jsx:192-208): impl Cost tab shows only a single current-rate line, no effective-dated history (PersonProfilePanel.tsx:459-474). OFF-path is an EmptyState pointing to rate-cards admin (EmployeeDetailsPage.tsx:627-638); Time · this week 7-day hour grid with per-day tone + Total/target + Edit-week/Submit-for-approval footer (page-profile.jsx:211-249): impl Time&leave tab shows only a 'last 4 weeks' dl summary (logged/overtime/leave hours), no weekday grid, no submit action (PersonProfilePanel.tsx:475-501). OFF-path is an EmptyState redirect to /me (EmployeeDetailsPage.tsx:640-656); Leave card detail: canvas dl (Annual entitlement / Taken YTD / Pending / Remaining / In-lieu) + 'Upcoming leave' info banner + 'Request leave' link (page-profile.jsx:251-268); impl leaveCard renders a generic per-type used/pending/remaining grid with no entitlement/YTD breakdown, no upcoming-leave banner, no request-leave action (PersonProfilePanel.tsx:351-389); Suggested next positions: canvas shows project+role+match-donut+start-date+per-row Propose button + '5d remaining on proposal' header note (page-profile.jsx:285-303); impl renders donut+role+project but no start date, no per-row Propose button, no proposal-countdown header (PersonProfilePanel.tsx:553-595)
- **Extra (in FE, not in canvas):** Resource pools card on Overview tab (PersonProfilePanel.tsx:419-437) — not in canvas profile; Manager chain card on Overview tab (PersonProfilePanel.tsx:391-417) — canvas surfaces line-manager only inside the Employment dl, not as a standalone manager-chain card; Lifecycle action header (Deactivate / Terminate) + ConfirmDialogs (EmployeeDetailsPage.tsx:261-320) — operational header not present in canvas (canvas header actions are Message/Propose/more); OFF-path only: Reporting Relationships + Reporting Line Management scheduler cards (EmployeeDetailsPage.tsx:514-587), 360 View card (EmployeeDetailsPage.tsx:589-593), Lifecycle Actions + AuthTokenField card (EmployeeDetailsPage.tsx:675-686), Linked entities rail card (EmployeeDetailsPage.tsx:729-745), Change History audit timeline on Activity tab (EmployeeDetailsPage.tsx:663-671) — none of these appear in the canvas
- **Order:** Top-level order matches canvas on the dsRefresh-ON path (Identity → KPI strip → tabs → 1fr/320px body+right-rail; rail order Quick actions → Suggested next positions → Activity = canvas page-profile.jsx:273-321). The deviation is intra-tab: the canvas is a single scrolling Overview body (snapshot → identity/employment → position history → skills/cost → time/leave) whereas the impl splits that content across the 6 tabs, so canvas's always-visible Identity/Employment/Position-history/Skills/Cost/Time sections are now tab-gated rather than co-visible. The right-rail Quick-actions ordering also adds an extra 'View activity' button (PersonProfilePanel.tsx:541-549) and drops the canvas 'Deactivate…' danger item (page-profile.jsx:281).
- **Non-DS primitives:** /tmp/dc-qa/frontend/src/routes/people/EmployeeDetailsPage.tsx:447 — raw <input className="input"> in the Terminate form (OFF-path only) where a DS TextField/Input atom would belong; sibling date field already uses the DS DatePicker (line 457); /tmp/dc-qa/frontend/src/components/people/PersonProfilePanel.tsx:46,168-207 — KPI strip is a hand-rolled inline-styled <div> grid (KPI const + kpiStrip) rather than the global '.kpi-strip'/'.kpi' grammar the canvas uses (page-profile.jsx:71); functionally fine and token-clean, but not the shared KPI primitive; /tmp/dc-qa/frontend/src/components/people/PersonProfilePanel.tsx:306-346,356-386,396-414,424-434 — skills/leave/manager-chain/pools rendered as raw inline-styled <ul>/<li> lists rather than a DS Table/list atom (skills pip-bar list is intentional per V2-B.21 note, but leave/manager/pools could use DS Table)
- _Evidence:_ Canonical impl path is PersonProfilePanel (dsRefresh ON), delegated from EmployeeDetailsPage.tsx:254-329; OFF-path fallback lives in EmployeeDetailsPage.tsx:350-754. Verdict PARTIAL: top-level Detail-Surface grammar (Identity → KPI → 6 tabs → 1fr/320px split → rail Quick-actions/Suggested/Activity) faithfully matches page-profile.jsx, and all DS atoms are used (Avatar, Donut, Money, Pct, Tabs, Tim

### `page-projects` → /projects — **PARTIAL**
- **Missing (in canvas, absent in FE):** PM avatar+name column (canvas page-projects.jsx:156-161 Avatar+name; FE has NO PM/avatar field — grep NO PM AVATAR in ProjectsPage.tsx & ProjectDirectoryInspector.tsx); RAG pill column (canvas :48-53,162 RagPill On track/At risk/Off track; FE renders no RAG column — uses Health grade badge instead, ProjectsPage.tsx:306-326); Budget pill column (canvas :56-61,163 BudgetPill On budget/Watch/Over/Unset; FE exposes budget only as a filter select, ProjectsPage.tsx:445-460 — no budget column in table); Positions open/filled column (canvas :135,164-168 '{open} open / {filled}'; FE shows raw assignmentCount only, ProjectsPage.tsx:298-304 — no open/filled split); End date column (canvas :136,169 p.end; absent from FE columns ProjectsPage.tsx:231-341); Last activity column (canvas :137,170 p.activity; absent from FE columns); Multi-page pager (canvas :182-191 "Showing 1-9 of 24" + active Prev/Next): FE DOES render a DataView footer (DataView.tsx:578-621, passed to Table unconditionally) but pageSizeOptions={[1000]} (ProjectsPage.tsx:541,575) collapses all rows onto one page so Prev/Next are inert and no rows-per-page select shows. [corrected in validation r3: footer IS rendered; the gap is the single-page-of-1000 behaviour, not an absent footer]
- **Extra (in FE, not in canvas):** Priority column + filter + KPI tile (FE ProjectsPage.tsx:264-275 col, :395-409 filter, :485-493 KPI 'Critical priority' — not in canvas); External Links column (FE ProjectsPage.tsx:282-297 — canvas registry table has no external-links column); Source filter (Jira/M365/Radius) (FE ProjectsPage.tsx:426-440 — canvas filter bar has only lifecycle/RAG/budget); Engagement-model filter (FE ProjectsPage.tsx:380-394 — not in canvas filter bar); List-detail inspector pane (FE ProjectsPage.tsx:550-565 + ProjectDirectoryInspector.tsx — canvas spec is row-click→Project Pulse navigation :19-20, not an in-page right-rail inspector)
- **Order:** KPI tile set diverges: canvas tiles = Active / At-risk RAG!=G / Over budget / Open positions (page-projects.jsx:82-103); FE tiles = Total / Active / Critical priority / At risk red-yellow (ProjectsPage.tsx:466-503). Active is the only shared tile. Over-budget and Open-positions KPIs (canvas Law-9 drilldowns) are absent; Total + Critical-priority are FE-only. Overall PageHeader->KPI->FilterBar->table top-level order matches; divergence is within-table columns + pagination footer omission.
- _Evidence:_ Implementing route /projects -> ProjectsPage.tsx (route-manifest.ts:300, router.tsx:82). Redesigned surface is gated behind isFeatureEnabled('dsRefresh') (ProjectsPage.tsx:68); OFF path keeps legacy title-bar filters. Top-level grammar matches canvas (PageHeader actions Create+Export ProjectsPage.tsx:127-151; KPI strip :466-503; canonical FilterBar :367-461; DataView table :534-549). DS-primitive 

### `page-pulse` → /projects/:id (tab=pulse, dsRefresh flag on) — router.tsx:310 — **PARTIAL**
- **Missing (in canvas, absent in FE):** Section 1 (Title bar): canvas specifies a 3-tab Pulse/Plan/Money strip with a count badge on Plan ('8') and breadcrumb ['Projects','Mercury Core Banking — UK','Pulse']; FE DetailLayout/PageHeader renders the 3 tabs (ProjectDetailPage.tsx:52-56) but NO per-tab count badge and NO breadcrumb (DetailLayout is invoked with eyebrow='Projects' only, breadcrumbs prop omitted — ProjectDetailPage.tsx:256-261). Canvas header actions Export/Edit project/Add position become only 'Create Position' (+Plan-tab-only Add milestone/Add change request/Manage positions) — ProjectDetailPage.tsx:262-284; Export and Edit-project actions are absent.; Section 3 (KPI strip): canvas pins 5 explicit tiles (Open positions 3/8, Budget variance −8.2%, Milestone progress 62%, Days to next gate 11d, Burn rate 4w $248k with a <Sparkline>). FE renders pulse.signals dynamically (PulseTab.tsx:383-404) — keys are velocity/cpi/avgMood/openRisks/openCases/teamSize, NOT the canvas KPIs; no Sparkline tile, no delta-up/down rows, no per-KPI drilldown to plan/money (every tile links to ?tab=radiator). UX Law 9 drill targets diverge from schemas.json projectPulseKpiCards hrefs.; Section 5a (Open positions table): canvas columns Role / Alloc / Start / Status / Active fill / Suggest / actions plus a per-card filter ('Filter role…' input + Filter button + + Position button) — page-pulse.jsx:198-216. FE positionColumns (PulseTab.tsx:120-178) drops the Start and Suggest columns and the actions/more-menu column, and the card has NO filter/search/Position controls. Active-fill renders literal 'Booked' text instead of Avatar+name.; Section 5b (Top risks): canvas card has a footer with 'Log risk' + 'Mitigation plan' buttons and a 'View all 11' header link (page-pulse.jsx:259-277); FE Top-risks SectionCard (PulseTab.tsx:507-544) has neither the footer actions nor the View-all link.; Section 6a (Next milestone): canvas shows Donut(68) + eyebrow/gate name + Due date + Tasks 17/25 / Blockers 2 / Slip risk +3d sub-stats and a 'Plan tab' link (page-pulse.jsx:283-312); FE (PulseTab.tsx:549-585) shows Donut + hit/total + in-progress/missed + next-milestone name only — no Due date, no Tasks/Blockers/Slip-risk stat trio, no Plan-tab link.; Section 7 (External link tiles): canvas uses <Icon name={kind}> atoms and meta strings; FE uses emoji glyph literals (PulseTab.tsx:343-346, e.g. '\u{1F4CB}') rather than the Icon DS atom, and omits the 'Linked workspaces' card header + '+ Link' action (renders a bare grid, not a card — PulseTab.tsx:663-737).
- **Extra (in FE, not in canvas):** Section 6b Activity: FE adds an All/Mine scope toggle (PulseTab.tsx:592-610) — canvas Activity card has All/Mine ghost buttons too, so this is canvas-faithful, not extra.; ProjectDetailPage still renders a legacy kpi-strip (Status/Active Staff/Fill Rate/Time to Fill/Days Remaining — ProjectDetailPage.tsx:178-229) but it is explicitly suppressed on the Pulse tab (kpiStrip={activeTab==='pulse'?undefined:kpiStrip}, ProjectDetailPage.tsx:291), so no double-strip on Pulse.
- **Order:** Section order matches the canvas 8-zone grammar exactly (Banner → KPI strip → RAG quad + Decisions → Positions + Risks → Milestone + Activity → External tiles → Freshness footer; PulseTab.tsx:351-770). One content swap inside section 4: canvas 4th RAG quadrant is 'Quality' (defect escape rate / P0), FE 4th quadrant is 'Overall' (composite of schedule/budget/people — PulseTab.tsx:415-419). The People quadrant maps to rag.staffingRag. So RAG grid is present and ordered but the 4th cell semantics differ.
- **Non-DS primitives:** PulseTab.tsx:343-346 — external-link tiles use raw emoji glyph literals ('\u{1F4CB}' Jira, '\u{1F4D6}' Confluence, '\u{1F4AC}' Teams, '\u{1F4CA}' Smartsheet) instead of the Icon DS atom the canvas specifies (<Icon name={l.kind}>, page-pulse.jsx:358).; PulseTab.tsx:663-737 — Linked-workspaces grid is a bare <div> grid with inline-styled <a>/<div> tiles, not wrapped in a SectionCard with header + '+ Link' action as the canvas card pattern requires (page-pulse.jsx:337-369). No raw <table>/<button> anywhere (positions use DS <Table>, all actions use DS <Button>).
- _Evidence:_ Canvas spec page-pulse.jsx:1-8 (8-zone grammar) + 55-384 (full JSX). Implementing route: router.tsx:310 ('projects/:id' → ProjectDetailPage), gated on dsRefresh feature flag (ProjectDetailPage.tsx:81-83,299 — PulseTab only mounts when dsRefresh on AND activeTab==='pulse'). PulseTab.tsx:180-189 self-documents the 8-section mapping and renders all 8 zones in order (banner 351, kpi 383, rag+decisions

### `page-reports` → /reports — **PARTIAL**
- **Missing (in canvas, absent in FE):** Tab strip mismatch: canvas specifies EXACTLY two live tabs — 'Time analytics' and 'Utilization' (page-reports.jsx:36-39, spec note line 12-15 'V2 ships exactly TWO live tabs'). FE renders SIX tabs (ReportsPage.tsx:59-66: exceptions, time, capitalisation/CAPEX, export, utilization, evidence).; Time tab — canvas hero is a single grouped-bar chart 'Submitted vs approved hours · weekly' (page-reports.jsx:167-183). FE TimeReportPage hero is a stacked AREA chart 'Weekly Time Distribution' of Standard/Overtime/Bench (TimeReportPage.tsx:172-190) — different metric (no submitted-vs-approved encoding) and different chart type.; Time tab — canvas 'Approval gap widening' warning banner with 'Open Time Management' drill CTA (page-reports.jsx:174-181) is absent in FE.; Time tab — canvas breakdown table is 'by org unit' with columns Org unit / Submitted / Approved / Approval rate / Late submissions / Trend·6w (page-reports.jsx:195-233). FE detail table is 'Detail by Person' with Standard/Overtime/Bench/Total columns (TimeReportPage.tsx:271-295) — different grouping and columns; no approval-rate/late/trend-sparkline.; Utilization tab — canvas hero is a per-team horizontal bar with billable+allocated overlay and 70/110% threshold marks (page-reports.jsx:310-344). FE hero is a per-PERSON vertical bar of a single utilizationPercent value (UtilizationPage.tsx:201-233) — no team rollup, no billable-vs-allocated dual encoding, no 70/110 threshold marks.; Utilization tab — canvas supporting table is 'By person · outside 70–110%' showing Person/Team/Billable/Allocated(util-bar)/Flag, flagged rows floated to top, drill to Profile (page-reports.jsx:347-395). FE table shows Person/Available Hrs/Assigned Hrs/Actual Hrs/Utilization with no Team column, no over/under Flag badges, no outside-band sort, no Profile drilldown (UtilizationPage.tsx:250-262).; Utilization tab — canvas reuses the Staffing-Desk .util-bar/.over-track cell with 70/110 marks (page-reports.jsx:267-282). FE uses a bespoke inline UtilizationBar div (UtilizationPage.tsx:45-70) — not the shared util-cell encoding.
- **Extra (in FE, not in canvas):** ReportsPage.tsx:60 — 'Exceptions' tab (canvas page-reports.jsx does not list it; folded in via route-manifest D-137, not part of the Reports canvas).; ReportsPage.tsx:62 — 'CAPEX'/Capitalisation tab (canvas explicitly RETIRES capitalisation — page-reports.jsx:30 'capitalisation (retired)').; ReportsPage.tsx:63 — 'Export' / Export Centre tab (canvas omits scheduled/export center; Export is a per-tab ghost button, not a tab — page-reports.jsx:30-31).; ReportsPage.tsx:65 — 'Evidence' tab (canvas does not list it; folded in via route-manifest D-138).; TimeReportPage.tsx:143-170 — KPI strip of 6 tiles. Canvas deliberately omits a KPI strip ('KPI strip (the hero chart IS the summary)' — page-reports.jsx:31).; TimeReportPage.tsx:192-268 — 2x2 chart grid (Hours by Project / Hours by Person / CAPEX vs OPEX pie / Daily Hours). Canvas Time tab has only one hero chart + one breakdown table; these four panels are extra.; UtilizationPage.tsx:164-198 — KPI strip of 4 tiles (People / Avg Utilization / Over-allocated / Under-utilized). Canvas Utilization tab has no KPI strip.
- **Order:** For the two canvas-blessed tabs the high-level zone order (header > filter bar > hero > table > export) is preserved, but FE inserts an un-specced KPI strip ahead of the hero on both tabs (TimeReportPage.tsx:144 before hero at :173; UtilizationPage.tsx:165 before hero at :201), and the Time tab inserts a 2x2 chart grid (TimeReportPage.tsx:192) between hero and detail table. The umbrella tab strip itself carries 6 tabs vs the canvas-specified 2.
- **Non-DS primitives:** frontend/src/routes/reports/UtilizationPage.tsx:45-70 — bespoke inline UtilizationBar built from raw <div>s with inline styles; canvas calls for the shared Staffing-Desk .util-bar/.over-track util-cell (page-reports.jsx:267-282). Token-clean but not the DS/shared primitive the canvas blesses.; frontend/src/routes/reports/TimeReportPage.tsx:285-291 — custom CSS-grid table footer via raw <div>/<span> with inline styles instead of a DS footer atom (acceptable as Table footer slot, noted for completeness).
- _Evidence:_ Implementing route /reports -> ReportsPage.tsx (dsRefresh-flagged 'Reports umbrella', route-manifest.ts:336; nav group reports->workspace at route-manifest.ts:47, matching canvas 'Workspace > Reports' / AppShell active='reports' page-reports.jsx:43). Chrome contract MATCHES. Tab count MISMATCH: canvas spec (page-reports.jsx:36-39 + note lines 12-15) mandates exactly two tabs Time analytics + Utili

### `page-staffing-desk` → /staffing-desk (?view=table board + ?view=planner Distribution Studio); Find-candidates lives on /projects/:id/positions/:posId per canvas's own path note — **PARTIAL**
- **Missing (in canvas, absent in FE):** Planner: canvas groups lanes by workstream (Frontend/Backend/Mainframe/QA) with a per-group Sigma sparkline + heat band (page-staffing-desk.jsx L600-667); FE WorkforcePlanner groups by PROJECT rows in a project x week grid with per-cell heat layer + delta-FTE column — no workstream PlannerGroup, no per-group sigma sparkline, no group heat band; Bench sidebar 'Conflicts . 2' subsection (canvas L554-569) absent from PlannerBenchSidebar.tsx — conflicts surface only in the PlannerAnomalyTable drawer; Bench sidebar 'Recently moved . 7 / Undo all' subsection (canvas L571-589) absent from PlannerBenchSidebar.tsx — undo lives in the WorkforcePlanner toolbar (WorkforcePlanner.tsx L595) not the bench rail; Find-candidates ranked-top-5 panel (canvas FindCandidatesPanel L700-775) is not rendered on /staffing-desk; it is implemented on the separate ProjectPositionDetailPage route (candidate slate + Propose, L157-263) which matches the canvas's own /projects/.../positions/... path annotation
- **Extra (in FE, not in canvas):** Planner toolbar: horizon switcher 3m/6m/9m/12m, week-nav, heat-layer select (Coverage/Cost/Match/Risk), Scenarios menu, Simulate/Apply/Reset (WorkforcePlanner.tsx L549-603) — richer than canvas planner chrome; Planner per-project delta-FTE column + Summary bar with dispatches/hires/releases/budget (WorkforcePlanner.tsx L890-948); Bench sidebar Auto-Match (strategy + min-skill-match slider + Auto-Distribute), Hire-Needs, and Sensitivity/attrition panels (PlannerBenchSidebar.tsx L67-218) — not in canvas bench rail; Board: SavedFiltersDropdown + Columns configurator + Σ-util/VarianceBar column gated behind dsRefresh (StaffingDeskTable.tsx L108-138), BulkReassignPanel (StaffingDeskPage.tsx L380-385), person Workload-Timeline popup + Supply/Demand drilldowns
- **Order:** Board view section order matches canvas exactly (PageHeader+badges -> tab strip -> JQL bar -> 6-tile KPI strip -> Supply/Demand toggle -> master table with filter row + Σutil + dense cols -> pagination -> lifecycle legend). Planner is reorganized rather than reordered: canvas = JqlBar -> 240px bench sidebar + workstream PlannerGrid + Anomaly drawer; FE = toolbar -> bench sidebar + project-week grid + summary bar + Anomaly table (no top JqlBar inside planner; JQL row hidden when view=planner per StaffingDeskPage.tsx L292).
- **Non-DS primitives:** frontend/src/components/staffing-desk/StaffingDeskKpiStrip.tsx:83 — raw <button> for the non-link KPI tiles (Supply/Available FTE/Open Demand/Staffing Gap that open drilldowns); uses .kpi-strip__item class with token-only inline styles, so visually conformant but a raw element rather than a DS atom (canvas KPI tiles are <div>; CLAUDE.md KPI rule prefers <Link>/<a>). Link-target tiles (Fill Rate, Overallocated) correctly use <Link>.; frontend/src/components/staffing-desk/PlannerAnomalyTable.tsx:50 — S_TABLE style const declares borderCollapse for a raw <table> path; the live render uses the DS <Table> primitive (L5 import) so this is a dormant style, not a rendered raw table
- _Evidence:_ DS/chrome.jsx is an empty 177-byte JSON stub (no NAV_GROUPS block) so the chrome/nav contract referenced in the task could not be diffed; route-manifest.ts:348 places staffing-desk in group 'staffing', navVisible:true, titleV2 'Staffing Desk' (engineering put it under Workforce breadcrumb per StaffingDeskPage.tsx:257, not Workspace-after-Reports as the design-request Item C proposed). Terminology 

### `page-time-management` → /time-management — **PARTIAL**
- **Missing (in canvas, absent in FE):** PageHeader header-actions row (period select + Filters + Audit) — canvas page-time-management.jsx:154-158 renders these in PageHeader actions; FE moves a Prev/Next month selector + ExportButton into the title bar (TimeManagementPage.tsx:213-229) and has no Filters or Audit action; KPI tile 'SLA breach' (canvas:178-182) — intentionally removed in FE per W4-06 (TimeManagementPage.tsx:386-388; asserted absent in TimeManagementPage.v2.test.tsx:31-34) pending a backend field; SLA badge chip 'SLA · 24h' (canvas PageHeader badges:151) — intentionally removed per W4-06; Leave drawer 'Allocation in requested range' project/role/alloc table (canvas:67-85) — FE replaces with a 'Conflicting assignments' table (LeaveDecisionDrawer.tsx:309-341), similar intent but different framing/columns; Leave drawer 'Team coverage that week' tile triple — On leave / Remaining in pool / Public holidays (canvas:95-112) — absent in FE LeaveDecisionDrawer; Leave drawer pre/post-approval balance-remaining tiles (canvas:54-63) — FE renders only the BalanceMeter, not the two before/after numeric tiles (LeaveDecisionDrawer.tsx:251-259); Leave drawer sticky-footer keyboard hint '↑ ↓ navigate queue · A approve · R reject' (canvas:126) — absent in FE drawer footer (LeaveDecisionDrawer.tsx:218-241); Timesheet inspector mini Timeline 'Hours by project · this week' via window.Timeline (canvas:281-288) — FE renders a text placeholder instead, gated on issue 254 (TimesheetInspectorDrawer.tsx:291-299); Timesheet inspector 'Request revision' + 'Nudge' action-rail buttons (canvas:304-305) — FE footer has only Cancel / Reject / Approve (TimesheetInspectorDrawer.tsx:186-209); Queue-row inline action buttons Nudge / Reject / Approve (canvas:251-255, 347-350) — FE collapses these to a single 'Review…' button that opens a drawer (TimeManagementPage.tsx:503-522); DS-docs CalendarDS + BalanceMeterDS pages (canvas:362-471) — DS component-documentation surfaces, not app routes; out of scope for a runtime route but no equivalent Storybook-style page in the route map
- **Extra (in FE, not in canvas):** Secondary KPI strip (Gap days / Overtime / Compliance / Approved this month / All leave) — TimeManagementPage.tsx:393-436; canvas has a single 5-tile strip; Team Calendar tab — TimeManagementPage.tsx:534-578; canvas tabs are only Timesheets + Leave; Compliance tab — TimeManagementPage.tsx:581-610; Overtime tab — TimeManagementPage.tsx:613-641; Inline 'Flags' anomaly column on the timesheet queue (V2-A.11, dsRefresh-gated) — TimeManagementPage.tsx:475-501; not in canvas queue table; Leave-drawer overlap-warning banner with profile/projects links (V2-B.23) — LeaveDecisionDrawer.tsx:269-307; canvas has only the inline 'Overlaps an active assignment' banner inside the allocation card
- **Order:** Top-level order matches canvas (PageHeader → KPI strip → tab body → drawers). Within the body the FE adds a second KPI strip and a 4-tab strip (queue/calendar/compliance/overtime) where the canvas has a 2-tab strip (timesheets/leave) folded into the PageHeader; the calendar/compliance/overtime tabs are appended after the two canvas tabs, so no canvas section is out of order — the extras are additive.
- **Non-DS primitives:** frontend/src/routes/time-management/TimeManagementPage.tsx:49-112 — bespoke RejectModal built from a raw overlay div instead of the DS Modal/FormModal primitive (Modal.tsx / FormModal.tsx exist in components/ds); frontend/src/routes/time-management/TimeManagementPage.tsx:78 — raw <select className="field__control"> for the reject-reason category instead of DS Select (Select.tsx); frontend/src/routes/time-management/TimeManagementPage.tsx:93 — raw <textarea className="field__control"> instead of DS Textarea (Textarea.tsx); frontend/src/routes/time-management/TimeManagementPage.tsx:462 — raw <input type="checkbox"> for row selection instead of DS Checkbox (Checkbox.tsx); frontend/src/routes/time-management/TimeManagementPage.tsx:547-569 — Team Calendar tab hand-builds a per-day cell grid with inline color-mix spans inside a DS Table instead of the canvas-specified <Calendar/> primitive (Calendar.tsx exists and the canvas docs it for exactly this year/month overview); frontend/src/components/time-management/TimesheetInspectorDrawer.tsx:316 — raw <textarea className="field__control"> reject reason instead of DS Textarea; frontend/src/components/time-management/LeaveDecisionDrawer.tsx:370,390 — raw <textarea className="field__control"> (reject reason + optional approval comment) instead of DS Textarea
- _Evidence:_ Canvas page-time-management.jsx specifies: PageHeader w/ [Operations,Time Management] breadcrumb + '7 awaiting you' + 'SLA · 24h' badges + 2 tabs (jsx:147-164); 5-tile KPI strip incl. SLA breach (jsx:166-193); Timesheets tab = TimesheetQueueTable + TimesheetInspectorDrawer (jsx:197-202); Leave tab = LeaveQueueTable (jsx:203-207); LeaveDecisionDrawer w/ BalanceMeter + pre/post balance tiles + alloc

### `page-timeline-ds` → (DS component spec — no app route; chrome.jsx section '17 · Timeline component (DS)'). Implemented as the <Timeline/> primitive used by Staffing Desk, Project Plan, Distribution Studio. — **PARTIAL**
- **Missing (in canvas, absent in FE):** Overallocation heat band — 4-tier thresholds (100-119 grey-amber / 120-149 amber / 150-199 red / >=200 dark-red). FE collapses to a SINGLE binary >100% danger tint (ds-timeline__conflict: background var(--color-status-danger) opacity 0.08) — ds.css:1533-1541; no h100/h120/h150/h200 tiers exist anywhere in src.; Drag · resize · snap zone — canvas documents resting/mid-drag(snap-preview ghost)/invalid-resize states + tl-handle left/right grips. FE bars are click-only: no onMove/onResize/readOnly props, no drag handles, no snap-to-week preview, no collision/ON_HOLD validation. Timeline.tsx exposes only onSegmentClick + ArrowLeft/Right keyboard nav (Timeline.tsx:521-543,647-685).; Group header · weekly Σ aggregate swimlane — canvas pl-group-head with chevron + position count badge + Sparkline + 'Σ 105% · 12w' + heat band above grouped bars. FE Timeline has no groups/swimlane concept at all (only role='group' on the single track, Timeline.tsx:572); no groups[] prop, no heat prop, no sigma header.
- **Extra (in FE, not in canvas):** colorMode='lifecycle' prop + 8-state lifecycle token rendering (draft/open/proposed/booked/onboarding/assigned/hold/released) driven by --lifecycle-* tokens (design-tokens.ts:242-256 light, 301-313 dark). Canvas shows lifecycle bars as a doc section but does NOT expose colorMode as a prop in its matrix — FE adds it as a real API.; Portal-escaping rich hover card (DefaultHoverCard: allocation/span/relative-dates/weekly-workload-headroom/conflicts/meta/CTA) with smart-placement — Timeline.tsx:321-376,815-856. Canvas only documents renderHoverCard as a prop, not this default body.; Markers (line\|flag variants) + Today line + month grid/labels + optional tone Legend — Timeline.tsx:607-628,741-808. Canvas mentions markers/today but not the month-grid/label or legend sub-components.
- **Order:** N/A for visual zone ordering — this is a DS primitive, not a stacked page layout. Capability-level divergence rather than reordered sections: the canvas's interactive zones (heat-band tiers, drag/resize/snap, group sigma) are absent while the FE primitive is read/click-only with a lifecycle-color extension the canvas doesn't formalize.
- **Token deviations:** frontend/src/styles/design-tokens.ts:245-256 and :302-313 contain raw hex (#bcc5d0 #b8730a #fbf1dd #114b7a #e0f3f7 #0a7490 #1f8a4d #cfd5dd #5f6b7a, dark variants) for --lifecycle-* — NOT A VIOLATION: design-tokens.ts is the sanctioned single-source-of-truth token file, hex-guardrail-exempt per CLAUDE.md. Flagged only for completeness.; Timeline.tsx:206-210 default lifecycle fallback hardcodes var(--color-status-info) instead of a lifecycle token — minor, only hit when status is unrecognized.
- **Non-DS primitives:** Timeline.tsx:647 and :697 use raw <button> for bars/blocks — INTENTIONAL/acceptable: these are the focusable interactive timeline primitives (the canvas itself renders .tl-bar divs; FE upgrades to keyboard-accessible buttons, an a11y improvement). 15 inline style={{...}} sites (e.g. Timeline.tsx:664-681,713-734) are dynamic positional left/width/bottom/height % and token-bound colors — required for a positioned timeline, not DS-atom substitutes.; No raw <table> present. The canvas 'Props · Storybook' matrix uses <table className='table table-compact'> for documentation only; the FE equivalent is Timeline.stories.tsx (Ladle stories), so no table primitive is owed.
- _Evidence:_ Canvas page-timeline-ds.jsx documents 7 zones: live demo (stacked+bar) [L28-69], usage code [L72-103], 8 lifecycle states [L106-124], 4-tier heat band [L127-148], drag/resize/snap [L151-195], group-header Σ swimlane [L197-224], props matrix [L227-257]. FE: 8 lifecycle states FULLY present (lifecycleStatusOf + lifecycleStyleOf cover all 8, Timeline.tsx:95-213; tokens design-tokens.ts:242-256/301-31

### `page-workspace` → /me (flag.workspaceMe) — **PARTIAL**
- **Missing (in canvas, absent in FE):** Inbox tab content — canvas specs a filter-bar (search + Unread/Mentions/Approvals/System chips + Mark-all-read) and a full 7-row notifications table; FE renders only a stub SectionCard with an 'Open inbox →' link to /notifications (WorkspaceShellPage.tsx:205-229); Settings tab content — canvas specs Identity + Digest-schedule + Quiet-hours cards plus a Sessions/Privacy right rail; FE renders only a stub SectionCard with an 'Open account settings →' link to /settings/account (WorkspaceShellPage.tsx:230-254); Time tab KPI strip — canvas has a 4-tile strip (Filed h / Expected h / Variance / Lock status, page-workspace.jsx:360-365); FE TimeTab has no KPI strip; Time tab hero Timeline — canvas shows a logged-hours Timeline above the grid (page-workspace.jsx:367-382); FE deliberately removed it (TimeTab.tsx:15-21 comment); Time tab toolbar actions — canvas week-nav card has Copy week / Auto-fill / This-week / Draft-unsaved badge (page-workspace.jsx:344-358); FE TimeTab only has Prev/Next week + Submit week; Overview hero footer — canvas weekly-timeline card has a logged/expected/variance summary row + 'Auto-fill from assignments' and 'Submit timesheet' buttons (page-workspace.jsx:95-102); FE 'My positions' card has neither (OverviewTab.tsx:132-156); Projects table Manager column — canvas memberships table has a Manager column (avatar+name) and a chevron action col (page-workspace.jsx:287-309); FE MembershipsTable omits Manager (ProjectsTab.tsx:112-163)
- **Extra (in FE, not in canvas):** Skills tab — FE adds a 'Skills' tab (WorkspaceShellPage.tsx:35, SkillsTab.tsx) with no canvas counterpart; Cases tab — FE adds a 'Cases' tab (WorkspaceShellPage.tsx:36, MeCasesTab.tsx) with no canvas counterpart; Overview 'Quick actions' card — FE adds a Quick-actions grid (Log hours / Request leave / Edit skills / View profile / Account settings) not present in the canvas Overview (OverviewTab.tsx:158-214)
- **Order:** Canvas tab order is Overview → Time → Leave → Projects → Inbox → Settings (6 tabs). FE order is Overview → Time → Leave → Projects → Skills → Cases → Inbox → Settings (8 tabs): the two extra tabs (Skills, Cases) are inserted between Projects and Inbox, so Inbox/Settings are pushed to positions 7-8. The 4 shared tabs that both have (Overview/Time/Leave/Projects) are in the same relative order.
- _Evidence:_ Implementing route resolved via route-manifest.ts:314 (path '/me', flag workspaceMe) and router.tsx:333-340 (FeatureGuard→WorkspaceShellPage). Shell defines 8 tabs at WorkspaceShellPage.tsx:30-39 vs canvas 6 tabs at page-workspace.jsx:9-16. Tab-by-tab: Leave is a strong match (FE LeaveTab.tsx:274-487 mirrors canvas left BalanceMeter+request-form+live-preview+conflict-rail / right year-Calendar+leg

## MATCH canvases
- `page-director` → /dashboard/director (router.tsx:214, RoleGuard DIRECTOR_ADMIN_ROLES, FeatureGuard flag=dashDirector) (extra: PageHeader breadcrumb prepends a {href:'/', label:'Home'} crumb (DirectorDashboardPage.tsx:383) not present in the canvas breadcrumb=['Dashboards','Director']; benign navigation aid, not a structural section.)

## Reverse-pass — V2 nav routes without a DS canvas
- `/admin` — rt_80: navVisible=true, obsoleteInV2=false, titleV2=null (title='Admin', group=admin-config, roles=ADMIN_ROLES). Admin landing/hub route is reachable from V2 nav but no page-*.jsx canvas in /DS implem
- `/admin/settings` — rt_90: navVisible=true, obsoleteInV2=false, titleV2='Settings' (title='Platform Settings', group=admin-config, roles=ADMIN_ROLES). Carries an explicit V2 title ('Settings') so it is a first-class V2 n
