[
  {
    "flagId": "integrationsM365",
    "settingKey": "flag.feature.integrations.m365.enabled",
    "default": true,
    "gates": "Registry-declared (platform-flags.service.ts:659) but NOT consumed by any gate. No isEnabled('integrationsM365'), no isEnabledByKey, no @RequireFeature, no FE isFeatureEnabled. M365 directory sync ships unconditionally (M365ReconciliationPanel + integrations admin render off data/roles, not this flag).",
    "gatedSurfaces": [],
    "stagingEffective": "ON",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) forces every backend flag true; default true anyway. No FE consumer, so the FE force-set is irrelevant.",
    "hidesHalfBuilt": false,
    "evidence": "ga maturity, default true. grep found zero call sites outside the registry/FE mirror. Feature is built (M365ReconciliationPanel.tsx, integrations module). Flag is orphaned."
  },
  {
    "flagId": "integrationsJira",
    "settingKey": "flag.feature.integrations.jira.enabled",
    "default": true,
    "gates": "Registry-declared (platform-flags.service.ts:667) but NOT consumed by any gate. Jira PPM project sync ships unconditionally; flag never read.",
    "gatedSurfaces": [],
    "stagingEffective": "ON",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38); default true. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "ga maturity, default true. Zero call sites. Orphaned flag; integrations module is built end-to-end."
  },
  {
    "flagId": "integrationsRadius",
    "settingKey": "flag.feature.integrations.radius.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:675) but NOT consumed by any gate (no isEnabled/isEnabledByKey/RequireFeature/isFeatureEnabled). RADIUS adapter + reconciliation services exist and render regardless of the flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer of this flag, so the workspaceMe,dsRefresh FE force-set does not affect it.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. Feature is built end-to-end (src/modules/integrations/radius/* services + RadiusReconciliationPanel.tsx). Flag is orphaned, so it does not actually gate/hide the surface."
  },
  {
    "flagId": "integrationsOidc",
    "settingKey": "flag.feature.integrations.oidc.enabled",
    "default": false,
    "gates": "BACKEND-gated by settingKey via isEnabledByKey. oidc.controller.ts:130-137 assertEnabled() reads 'flag.feature.integrations.oidc.enabled' and throws UnauthorizedException('OIDC login is disabled on this tenant.') when OFF. Gates the OIDC SSO login endpoints.",
    "gatedSurfaces": ["src/modules/auth/oidc.controller.ts:130"],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true short-circuits isEnabledByKey to true (platform-flags.service.ts:977), so OIDC login is enabled on v2-staging. FE SsoAdminPage does not gate on this flag.",
    "hidesHalfBuilt": false,
    "evidence": "developing maturity, default false. OIDC handler is built end-to-end (D-155; oidc.controller.ts has full callback/redirect flow). Flag gates a real, working endpoint — controlled bank-IT scale-up rollout, not a stub."
  },
  {
    "flagId": "integrationsLdap",
    "settingKey": "flag.feature.integrations.ldap.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:693) but NOT consumed by any gate (no isEnabled/isEnabledByKey/RequireFeature/isFeatureEnabled). LdapDirectoryAdapter (F-4.7) runs off env/config, not this flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "developing maturity, default false. LDAP/AD adapter shipped (Sprint F-4 closeout, ldapts). Flag is orphaned — no call site reads it; the adapter is gated by env bind creds, not the flag."
  },
  {
    "flagId": "integrationsJsm",
    "settingKey": "flag.feature.integrations.jsm.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:702) but NOT consumed by any gate. JSM Cloud connector ships off config, not this flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "developing maturity, default false. JSM connector shipped (Sprint F-4 closeout). Flag is orphaned; no call site reads it."
  },
  {
    "flagId": "integrationsWebhookRegistry",
    "settingKey": "flag.feature.integrations.webhookRegistry.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:711) but NOT consumed by any gate. The webhook event-type registry is exposed unconditionally via GET /admin/webhooks/event-types (admin-config.controller.ts:345-350 returns WEBHOOK_EVENT_REGISTRY) + FE WebhooksAdminPage.tsx.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer of this flag.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. Built end-to-end: WEBHOOK_EVENT_REGISTRY (shared/events/webhook-event-types.ts) + endpoint admin-config.controller.ts:345 + FE WebhooksAdminPage. Flag is orphaned."
  },
  {
    "flagId": "notificationsBell",
    "settingKey": "flag.feature.notifications.bell.enabled",
    "default": true,
    "gates": "Registry-declared (platform-flags.service.ts:724) but NOT consumed by any gate. In-app notification bell (NotificationBell) renders unconditionally; polling drives the count (App.tsx:37).",
    "gatedSurfaces": [],
    "stagingEffective": "ON",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38); default true anyway. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "ga maturity, default true. Bell + 30s polling shipped (App.tsx). Flag is orphaned."
  },
  {
    "flagId": "notificationsSse",
    "settingKey": "flag.feature.notifications.sse.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:732) but NOT consumed by any gate. The @Sse('stream') endpoint (inbox.controller.ts:124) exists unconditionally; the flag never gates it. FE abandoned SSE for polling because the stream 503'd (App.tsx:15-16,39).",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no effect)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but flag is unconsumed so flip has no effect. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false; registry note 'currently broken (D-87 503)'. The named feature (real-time SSE) is broken/abandoned, BUT the flag is orphaned (no isEnabled('notificationsSse') anywhere) so it does not GATE/HIDE any surface — turning it ON would change nothing. Not hidesHalfBuilt per the strict 'hides a feature' criterion. NOTE: broken feature exists, flag is a no-op."
  },
  {
    "flagId": "notificationsPreferences",
    "settingKey": "flag.feature.notifications.preferences.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:742) but NOT consumed by any gate. Per-user notification preferences ship unconditionally: me-notification-prefs.controller.ts + FE AccountSettingsPage.tsx (real fetchMyNotificationPrefs/updateMyNotificationPrefs wiring) + WorkspaceShellPage.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "Registry labels 'scaffolded', but feature is actually built end-to-end: controller src/modules/in-app-notifications/presentation/me-notification-prefs.controller.ts + service + FE AccountSettingsPage.tsx (lines 12-17,122,140,170 wire real GET/PATCH). Flag is orphaned. Registry maturity label is stale."
  },
  {
    "flagId": "helpCenter",
    "settingKey": "flag.feature.helpCenter.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:755) but NOT consumed by any gate. Help Center ships off routes/data: help.controller.ts + help-admin.controller.ts + FE HelpArticleListPage/HelpArticleDetailPage. Neither this flag nor helpCenterMaster is read in app code.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. Help Center MVP built end-to-end (HD-9 closed; src/modules/help-center/* + frontend/src/routes/help/*). Flag is orphaned — gated 'for bank-branded content review' but the gate is not wired."
  },
  {
    "flagId": "onboardingTour",
    "settingKey": "flag.feature.onboardingTour.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:764) but NOT consumed by any gate. OnboardingTourProvider is mounted unconditionally in AppShell.tsx:255-267; useOnboardingTour/OnboardingTourModal do not check the flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "scaffolded maturity, default false. Tour is built (OnboardingTourProvider/Modal, useOnboardingTour, onboarding-tours.ts) and mounted in AppShell with no flag guard. Flag is orphaned; tour gating is by user completion state, not this flag."
  },
  {
    "flagId": "cmdkPeopleSearch",
    "settingKey": "flag.feature.cmdk.peopleSearch.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:773) but NOT consumed by any gate. CommandPalette.tsx checks route.flag and dsRefresh (lines 129,131) but never isFeatureEnabled('cmdkPeopleSearch'). People search in cmd+K is not gated by its own flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no FE effect)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but flag is unconsumed. FE force-set is workspaceMe,dsRefresh only; this flag is not in it.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. cmd+K palette is built (CommandPalette.tsx). Flag is orphaned — people-search behavior is not actually gated on it."
  },
  {
    "flagId": "workflowDefinitions",
    "settingKey": "flag.feature.workflowDefinitions.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:786) but NOT consumed by any gate. NO REST controller, NO FE UI. Only domain entity + repository ports + in-memory repo exist (src/modules/metadata/domain/entities/workflow-definition.entity.ts, in-memory-workflow-definition.repository.ts). metadata.module.ts registers ONLY MetadataDictionariesController.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no surface)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but no endpoint/UI exists to light up. No FE consumer.",
    "hidesHalfBuilt": true,
    "evidence": "scaffolded maturity, default false. Registry section header (platform-flags.service.ts:783-784) literally reads 'CUSTOMIZATION SCAFFOLDS (built but never consumed)'. Schema models + entities + in-memory repo exist but NO controller (metadata.module.ts has only MetadataDictionariesController) and NO FE UI. OFF-by-default flag whose feature is not built end-to-end => hides half-built."
  },
  {
    "flagId": "customFields",
    "settingKey": "flag.feature.customFields.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:795) but NOT consumed by any gate. NO REST controller, NO FE UI. Only domain entities + Prisma repo exist (custom-field-definition.entity.ts, prisma-custom-field-definition.repository.ts). metadata.module.ts exposes only MetadataDictionariesController.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no surface)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but no endpoint/UI exists. No FE consumer.",
    "hidesHalfBuilt": true,
    "evidence": "scaffolded maturity, default false. Under the 'built but never consumed' registry section (line 783). CustomFieldDefinition Prisma model + entity + repo exist, but no controller route and no FE consumer (grep customField in frontend/src returns only metadata.ts/lib mirrors). OFF + not end-to-end => hides half-built."
  },
  {
    "flagId": "entityLayouts",
    "settingKey": "flag.feature.entityLayouts.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:804) but NOT consumed by any gate. NO REST controller, NO FE UI. Only domain entity exists (entity-layout-definition.entity.ts) + Prisma model EntityLayoutDefinition. metadata.module.ts exposes only MetadataDictionariesController.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no surface)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but no endpoint/UI exists. No FE consumer.",
    "hidesHalfBuilt": true,
    "evidence": "scaffolded maturity, default false. Under the 'built but never consumed' registry section (line 783). EntityLayoutDefinition model + entity exist, no controller, no FE UI. OFF + not end-to-end => hides half-built."
  },
  {
    "flagId": "metadataDictionaries",
    "settingKey": "flag.feature.metadata.tenantDictionaries.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:813) but NOT consumed by any gate. UNLIKE the other customization scaffolds, this IS built end-to-end: MetadataDictionariesController (/metadata/dictionaries GET/POST/PATCH) + FE MetadataAdminPage (/metadata-admin route) + MetadataDictionaryList/MetadataEntryPanel. The flag is never read; the metadata admin route is RBAC-gated, not flag-gated.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no effect)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but flag is unconsumed. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. Built end-to-end: src/modules/metadata/presentation/metadata-dictionaries.controller.ts + frontend/src/routes/metadata-admin/MetadataAdminPage.tsx (route-manifest.ts:373 /metadata-admin). Flag is orphaned and does not gate the surface. Not half-built."
  },
  {
    "flagId": "rbacResponsibilityRuleReads",
    "settingKey": "flag.feature.rbac.responsibilityRule.reads.enabled",
    "default": false,
    "gates": "BACKEND-gated by flag id. rbac.guard.ts:100 — when ON and a route declares @ReadAction(kind), callers can ALSO pass via tenant ResponsibilityRules (union semantics, never narrows). OFF => branch inert.",
    "gatedSurfaces": ["src/modules/identity-access/application/rbac.guard.ts:100"],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (platform-flags.service.ts:977) makes isEnabled('rbacResponsibilityRuleReads') return true on v2-staging, so the read-action ResponsibilityRule expansion is active there.",
    "hidesHalfBuilt": false,
    "evidence": "developing maturity, default false. The read-access resolver (resolveAllowedRoles / ReadAccessResolverService.permits) is built and wired in rbac.guard.ts:100-105. OFF by default for a 30-day ACL soak (D-158), not because it is a stub. Real, working feature behind a controlled rollout."
  },
  {
    "flagId": "authTwoFactor",
    "settingKey": "flag.feature.auth.twoFactor.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:836) but NOT consumed by any gate. 2FA ships off auth flow: auth.controller.ts + two-factor.service.ts (BE) + TwoFactorSetupPage.tsx + LoginPage challenge (FE). Enrollment/challenge driven by per-user 2FA state, not this flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38). No FE consumer of this flag.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. 2FA built end-to-end (src/modules/auth/two-factor.service.ts + frontend/src/routes/auth/TwoFactorSetupPage.tsx + LoginPage). Flag is orphaned."
  },
  {
    "flagId": "featureViewAs",
    "settingKey": "flag.feature.viewAs.enabled",
    "default": true,
    "gates": "Registry-declared (platform-flags.service.ts:845) but NOT consumed by any gate. Admin 'View as' impersonation ships via impersonation-context.tsx + auth-context overlay (CLAUDE.md 8.13), not this flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38); default true anyway. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "ga maturity, default true. Impersonation built and shipped. Flag is orphaned."
  },
  {
    "flagId": "financialMultiCurrency",
    "settingKey": "flag.feature.financial.multiCurrency.enabled",
    "default": false,
    "gates": "BACKEND-gated by flag id. fx-rate.service.ts:78 — when ON, convert() returns null on a missing FxRate (strict multi-currency); when OFF, identity-converts (single-currency tenant). Gates strict consolidation behavior, not a UI surface.",
    "gatedSurfaces": ["src/modules/financial-governance/application/fx-rate.service.ts:78"],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (platform-flags.service.ts:977) makes isEnabled('financialMultiCurrency') true on v2-staging, so strict null-on-missing-rate behavior is active.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. FxRate model + convert() ship in v1; flag gates the strict consolidation EXECUTION path (returns null vs identity-convert). Real behavior, not a stub — controlled enable per registry note 'code ships in v1; consolidation execution gated'."
  },
  {
    "flagId": "financialFiscalCalendarEntity",
    "settingKey": "flag.feature.financial.fiscalCalendar.entity.enabled",
    "default": false,
    "gates": "BACKEND-gated by flag id. fiscal-calendar.service.ts:55 isEnabled() returns this.flags.isEnabled('financialFiscalCalendarEntity'). When ON, the full FiscalCalendar/FiscalPeriod entity path is used; OFF uses the D-160a quick-fix.",
    "gatedSurfaces": ["src/modules/financial-governance/application/fiscal-calendar.service.ts:55"],
    "stagingEffective": "ON (backend)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (platform-flags.service.ts:977) makes isEnabled('financialFiscalCalendarEntity') true on v2-staging, so the entity-backed fiscal calendar path is active.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. FiscalCalendar/FiscalPeriod entities exist (D-160b, flag-gated in Sprint F-7). The service has a real branch on the flag; v1 default uses the D-160a quick-fix. Gated entity rewrite, not a stub — feature path is implemented behind the flag."
  },
  {
    "flagId": "perfMaterialisedRollups",
    "settingKey": "flag.feature.perf.materialisedRollups.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:881) but NOT consumed by any gate. No isEnabled('perfMaterialisedRollups') anywhere; no materialized rollup tables in schema.prisma. Dashboards compute live; the rollup optimization is unbuilt.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no effect)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but flag is unconsumed and no rollup path exists, so flip has no effect. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. The materialized-rollup feature is NOT built (no rollup tables, no consumer reads the flag). It is a pure placeholder flag ('OFF until P95 SLO breach', T-13 D-147). Because the flag GATES NOTHING (no surface hidden — there is no rollup code path at all), it does not meet the 'hides a feature' criterion. NOTE: feature genuinely unbuilt but flag is a no-op placeholder."
  },
  {
    "flagId": "kpiDrilldown",
    "settingKey": "flag.feature.kpiDrilldown.enabled",
    "default": true,
    "gates": "Registry-declared (platform-flags.service.ts:894) but NOT consumed by any gate. KPI tile click-to-drilldown (UX Law 9) is implemented directly in KPI strip/StatCard components via href, not this flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38); default true anyway. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "ga maturity, default true. KPI drilldowns are shipped (UX Law 9 enforced). Flag is orphaned."
  },
  {
    "flagId": "actionItems",
    "settingKey": "flag.feature.actionItems.enabled",
    "default": true,
    "gates": "Registry-declared (platform-flags.service.ts:902) but NOT consumed by any gate. Action Items dashboard widget renders unconditionally; flag never read.",
    "gatedSurfaces": [],
    "stagingEffective": "ON",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38); default true anyway. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "ga maturity, default true. Action Items widget shipped. Flag is orphaned."
  },
  {
    "flagId": "pulseEnabled",
    "settingKey": "flag.pulse.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:910) but NOT consumed by any gate. Master Pulse switch is never read (no isEnabled('pulseEnabled')). Pulse module ships via controllers (pulse.controller.ts, mood-heatmap.controller.ts, project-pulse.controller.ts) + FE PulseTab/RadiatorTab; surfacing is driven by router (router.tsx:128 uses dsRefresh to pick pulse vs radiator tab), not pulseEnabled.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no effect)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but flag is unconsumed. No FE consumer of pulseEnabled.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false ('off per Decision-7'). Pulse module is built end-to-end (src/modules/pulse/* controllers + frontend PulseTab.tsx). The intended master switch is not wired (orphaned flag). Feature is built; flag does not gate it."
  },
  {
    "flagId": "helpCenterMaster",
    "settingKey": "flag.helpCenter.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:919) but NOT consumed by any gate. Alternate/duplicate master switch for Help Center; never read in app code (like helpCenter). Help Center ships via help.controller.ts + FE help routes regardless.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no effect)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but flag is unconsumed. No FE consumer.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. Duplicate of the helpCenter flag ('Master Help Center switch (alternate to flag.feature.helpCenter.enabled)'). Help Center is built end-to-end. Flag is orphaned."
  },
  {
    "flagId": "undoToast",
    "settingKey": "flag.undo.toast.enabled",
    "default": false,
    "gates": "Registry-declared (platform-flags.service.ts:928) but NOT consumed by any gate. showUndoToast (lib/undo-toast.ts) is called directly with no flag check in EmployeeDetailsPage.tsx:165, useStaffingDeskActions.ts:54, ProjectLifecycleControls.tsx:85. The undo toast fires regardless of the flag.",
    "gatedSurfaces": [],
    "stagingEffective": "ON (backend, no FE effect)",
    "stagingSource": "Backend FORCE_ALL_FLAGS_ON=true (docker-compose.v2-staging.yml:38) but flag is unconsumed. FE force-set is workspaceMe,dsRefresh only; undoToast is not in it and is not checked anyway.",
    "hidesHalfBuilt": false,
    "evidence": "beta maturity, default false. Undo toast is built and live (lib/undo-toast.ts; HD-8 chunk 8.4b) and fires unconditionally. Flag is orphaned — undo-toast.ts contains no isFeatureEnabled check. Feature is built; flag does not gate it."
  },
  {
    "flagId": "workspaceMe",
    "settingKey": "flag.workspaceMe.enabled",
    "default": false,
    "gates": "FRONTEND route-gated. route-manifest.ts:314 declares /me with flag:'workspaceMe'; router.tsx:333-339 wraps WorkspaceShellPage in <FeatureGuard flag=\"workspaceMe\">. OFF => /me shows FeatureGuard 'Module disabled' empty state and legacy /my-time, /leave, /notifications, /settings/account, /timesheets stay canonical. ON => unified /me tab shell (Overview/Time/Leave/Projects/Skills/Cases/Inbox/Settings). ROLLOUT-LOCKED (feature-flag-admin.service.ts:17 — cannot be toggled from admin UI).",
    "gatedSurfaces": ["frontend/src/app/router.tsx:335", "frontend/src/app/route-manifest.ts:314", "frontend/src/routes/me/WorkspaceShellPage.tsx"],
    "stagingEffective": "ON",
    "stagingSource": "Frontend forced ON via VITE_FORCE_FLAGS_ON=workspaceMe,dsRefresh baked into the v2-<sha> FE image (build-and-stage-v2.yml:38 default input; feature-flags.ts:214-219,228 FE_FORCE_KEYS). Backend also FORCE_ALL_FLAGS_ON=true. So /me workspace is live on v2-staging.",
    "hidesHalfBuilt": false,
    "evidence": "developing maturity, default false. The /me Employee Workspace shell + tabs (WorkspaceShellPage.tsx, OverviewTab/TimeTab/LeaveTab/ProjectsTab/SkillsTab/MeCasesTab) are all built. It is a rollout-locked controlled-flip flag (the V2 cutover gate), hiding a BUILT surface — not a stub. Hides a complete-but-not-yet-default feature."
  },
  {
    "flagId": "dsRefresh",
    "settingKey": "flag.dsRefresh.enabled",
    "default": false,
    "gates": "FRONTEND-gated at 33+ sites. route-manifest.ts:294/336/348/389/390 gates /approvals, /reports, /staffing-desk, /admin/governance/roles, /admin/v2-soak-checklist. Component-level isFeatureEnabled('dsRefresh') in router.tsx:128 (pulse vs radiator tab), V2Redirect.tsx:27, HomeRedirect.tsx:30, ProjectsPage.tsx:68, ProjectDetailPage.tsx:81, BudgetTab.tsx:151, TimeManagementPage.tsx:322, StaffingDeskPage.tsx:97, DeliveryManagerDashboardPage.tsx:53/144, LeaveTab.tsx:85, EmployeeDirectoryPage.tsx:43, CommandPalette.tsx:129, HrDashboardPage.tsx:247, ReportsPage.tsx:115, IntegrationsAdminPage.tsx:34, SsoAdminPage.tsx:71, et al. ROLLOUT-LOCKED (feature-flag-admin.service.ts:18). OFF => legacy chrome/redirects; ON => DS visual refresh + V2 nav/redirect topology.",
    "gatedSurfaces": ["frontend/src/app/route-manifest.ts:294", "frontend/src/app/route-manifest.ts:336", "frontend/src/app/route-manifest.ts:348", "frontend/src/app/route-manifest.ts:389", "frontend/src/app/route-manifest.ts:390", "frontend/src/app/router.tsx:128", "frontend/src/routes/V2Redirect.tsx:27", "frontend/src/routes/HomeRedirect.tsx:30", "frontend/src/routes/projects/ProjectsPage.tsx:68", "frontend/src/routes/projects/ProjectDetailPage.tsx:81", "frontend/src/routes/projects/tabs/BudgetTab.tsx:151", "frontend/src/routes/time-management/TimeManagementPage.tsx:322", "frontend/src/routes/staffing-desk/StaffingDeskPage.tsx:97", "frontend/src/routes/dashboard/DeliveryManagerDashboardPage.tsx:53", "frontend/src/routes/dashboard/DeliveryManagerDashboardPage.tsx:144", "frontend/src/routes/me/LeaveTab.tsx:85", "frontend/src/routes/people/EmployeeDirectoryPage.tsx:43", "frontend/src/components/common/CommandPalette.tsx:129", "frontend/src/routes/dashboard/HrDashboardPage.tsx:247", "frontend/src/routes/reports/ReportsPage.tsx:115", "frontend/src/routes/admin/IntegrationsAdminPage.tsx:34", "frontend/src/routes/admin/SsoAdminPage.tsx:71"],
    "stagingEffective": "ON",
    "stagingSource": "Frontend forced ON via VITE_FORCE_FLAGS_ON=workspaceMe,dsRefresh baked into the v2-<sha> FE image (build-and-stage-v2.yml:38 default; feature-flags.ts:214-219,228). Backend also FORCE_ALL_FLAGS_ON=true. So the DS refresh + V2 topology is the live surface on v2-staging.",
    "hidesHalfBuilt": false,
    "evidence": "developing maturity, default false. The DS-refresh primitives and the consumer adoption sites are built (33+ wired sites across routes/components). Rollout-locked V2 master flag — hides a BUILT redesign pending staging sign-off, not a stub."
  }
]
