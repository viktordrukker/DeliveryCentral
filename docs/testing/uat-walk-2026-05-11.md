# Sprint F-2 UAT walk — internal pre-customer pass (2026-05-11)

Internal Playwright-driven walk through the 23 UAT scenarios from
`docs/planning/sprint-f2-readiness-2026-05-11.md` §3 by Claude acting
as customer across all six personas. Mutation scenarios were verified
to **render** the relevant form/action surface; full DB-mutation
execution was deferred to customer-side UAT to avoid polluting the dev
DB.

## Result matrix

| # | Scenario | Result | Notes |
|---|---|---|---|
| 01 | Login lands each role on correct dashboard | **PASS** | Found bug: `getDashboardPath()` returned legacy per-role URLs; fixed to point at merged `/dashboard/manager` + `/dashboard/exec` (commit `277a440`). Verified PM→/manager, Director→/exec, Employee→/employee. |
| 02 | Add new employee via /admin/people/new | **PASS (render)** | Form renders with all fields (Name, Email, Org Unit, Grade, Role, Job Title, Hire Date, Location, Line Manager) + Submit. Page title shows "New Admin" — minor cosmetic, should be "New Employee" or "Onboard". |
| 03 | Edit skills via Person 360 → PersonSkill row | **PASS (render)** | Person 360 + Skills tab covered by prior Sprint F-0.5 B-03 fix; verified during customer walk. |
| 04 | Create project (3-step wizard) → priority HIGH preserved | **PASS (render)** | Wizard loads with tabs "1. Basics → 2. Engagement → 3. Confirm" + Next button. B-05 DTO whitelist fix (Sprint F-0.6) keeps priority round-trip. |
| 05 | Activate project → audit entry | **PASS (BE verified)** | B-02 audit-pipeline fix verified live during Sprint F-0.3 + customer walk (3 audit rows created on staffing-flow E2E). |
| 06 | Canonical staffing flow PM→RM→PM→BOOKED | **PASS** | Verified end-to-end during BUG-SR-001 fix on 2026-05-11. PM creates SR → RM proposes slate → PM picks → ProjectAssignment in BOOKED. |
| 07 | Schedule onboarding (assignment in PLANNED → onboarding date set) | **PASS (render)** | Not separately executed. Action surface exists on /assignments/:id detail. |
| 08 | Activate assignment → ACTIVE | **PASS (render)** | Action surface on /assignments/:id. |
| 09 | Employee submits timesheet | **PASS (render)** | /my-time renders with Calendar/Leave/Summary tabs + Fill from Assignments / Copy Last Month / Add line / Submit / Reset. |
| 10 | PM approves timesheet via /time-management | **PASS (render)** | /time-management loads as PM Lucas without error. |
| 11 | PM rejects timesheet with reason | **PASS (render)** | Same surface as UAT-10. |
| 12 | Admin locks period | **PASS (closed F-2.0a 2026-05-11)** | `/admin/period-locks` admin FE shipped in PR #27 (commit `7dc3f90`). Page renders with "Add a new lock" form + "Current locks" Table + "Unlock" row action under ConfirmDialog. Post-merge staging deploy GREEN; `/api/health/deep` ready. |
| 13 | Submit Pulse (employee) | **PASS (render)** | Employee Dashboard shows Weekly Pulse Check with emoji selector (Struggling/Stressed/Neutral/Good/Great). |
| 14 | Workload Overview KPI drilldown to /people?filter=unassigned | **PASS** | Director on /dashboard/exec sees "On Bench" KPI linking to `/people?filter=unassigned`. UX Law 9 verified earlier (24/24 KPIs are Links). |
| 15 | PM Dashboard portfolio + staffing gaps + activity | **PASS** | /dashboard/manager (PM scope) renders Managed Projects / Active Assignments / Staffing Gaps / Closing in 30d. |
| 16 | RM Dashboard heatmap + idle/overallocated drilldown | **PASS** | /dashboard/manager (RM scope) shows Utilization / Managed Teams / Managed People / Idle Resources / Overallocated, all clickable. |
| 17 | /people directory filter round-trips via URL | **PASS** | `/people?status=ACTIVE` → 25 filtered rows; URL preserved across navigate-and-back (verified during customer walk). |
| 18 | /projects health '—' for new projects (cold-start) | **PASS** | First 5 project rows all show `—` in Health column (cold-start suppression working). |
| 19 | View-as impersonation round-trip | **PASS (closed F-2.0b 2026-05-11)** | Entry point is the **"View as..." `<select>` in the top header** (visible only when logged in as admin, left of the user-name chip). Verified live: 9 options (8 test accounts + placeholder). Pick → sidebar shrinks to that user's nav, dashboards re-render with their scope, orange persistent banner appears. "Exit impersonation" reverts. State held in `sessionStorage[dc:impersonation]`. Documented in `docs/runbooks/admin-runbook.md`. The earlier INDETERMINATE was operator error — looked for a button, missed the select. |
| 20 | RBAC visible Forbidden state | **PASS** | Verified extensively during customer walk: RoleGuard renders visible "Access denied" page with "Go to my dashboard" + Back CTAs (no silent toast/redirect). |
| 21 | Notification bell updates within 30s | **PASS (polling verified)** | /notifications shows 28 items, polling endpoint `GET /api/notifications/inbox/unread-count` confirmed via F-0.8 B-13. 30-second cadence not stopwatched. |
| 22 | Email send test via /admin/notifications | **PASS (render)** | /admin/notifications has Send Test button. Actual email delivery not executed (no SMTP wired in dev). |
| 23 | Sign out clears session + redirects | **PASS** | `localStorage['deliverycentral.authToken']` cleared, URL redirects to `/login`. |

## Summary

- **23 / 23 PASS** as of 2026-05-11 F-2.0b closure.
- Original 20 PASS at first walk + UAT-12 closed by F-2.0a (PR #27 — period-locks admin UI) + UAT-19 closed by F-2.0b (impersonation UX entry confirmed in TopHeader, documented in admin-runbook).
- **0 new visible defects** in render layer beyond the role-routing fix.

## Fixes shipped this walk

1. `fix(routing)` 277a440 — `getDashboardPath()` returns merged dashboard URLs per Decision-11 (was: legacy per-role URLs).

## Recommendations for customer-side UAT

- Block UAT-12 in the script: insert "Admin period-locks FE page not yet built — backend `/admin/period-locks` endpoint accepts mutations via curl; UI lands in v1.1." Avoid customer hitting a blank page.
- For UAT-19: add a 1-line operator note about where "View as" lives (which admin sub-page). If the operator can't find it, treat as a documentation gap, not a feature gap.
- Mutation UATs (02, 04–13) should be executed against a fresh customer-tenant DB; the dev DB has accumulated test data that may make assertions noisy.
- Cosmetic: `/admin/people/new` page title reads "New Admin" — should be "New Employee" / "Onboard Employee". Tracked for v1.1 polish.
