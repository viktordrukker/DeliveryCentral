// AUTO-GENERATED by scripts/gen-migration-tests.cjs — DO NOT EDIT BY HAND.
// Regenerate via `npm run test:migrations:gen` after intentional migration changes.

import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';

const migrationsRoot = path.resolve(__dirname, '../../prisma/migrations');

function sha256File(p: string): string {
  return crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
}

describe('DM-R-13 per-migration contract', () => {
  describe('00000000000000_init', () => {
    const migrationDir = path.join(migrationsRoot, "00000000000000_init");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("378bedf11b1aba646d38e3b78dbb0794f69e8b8ace9a690747abae1a066aca08");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('00000000000001_domain_foundation', () => {
    const migrationDir = path.join(migrationsRoot, "00000000000001_domain_foundation");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("714a73c990efd9ce4ee09858217fb699ca5b3031a4a106fdfd34826e31242269");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260329130000_employee_creation_support', () => {
    const migrationDir = path.join(migrationsRoot, "20260329130000_employee_creation_support");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("8bbe01a93158e25c9497f421f64f8e2a82eb0de39d787355e70b2f3ffe3c1787");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260329150000_project_manager_charter_fields', () => {
    const migrationDir = path.join(migrationsRoot, "20260329150000_project_manager_charter_fields");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("d5dd4906ea85ca1b4740af5713053b536a2abc30c4ad8a84191c16d82bc3d0f9");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260329170000_project_close_status', () => {
    const migrationDir = path.join(migrationsRoot, "20260329170000_project_close_status");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6554391050352b73648beac072525a4c189747137ce2adec36c3c09ff5ba8547");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260329190000_m365_directory_foundation', () => {
    const migrationDir = path.join(migrationsRoot, "20260329190000_m365_directory_foundation");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("d9f1945b1c711d2ce8d47aeafaddbdba6ddaf005656148ba544c75a30fae7876");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260329203000_radius_account_foundation', () => {
    const migrationDir = path.join(migrationsRoot, "20260329203000_radius_account_foundation");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("609522e18ae85f168e50b359b5e45ae9c7f6a2dab1c7992a1b7f8cfe9a0fd2cf");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260329220000_notifications_core', () => {
    const migrationDir = path.join(migrationsRoot, "20260329220000_notifications_core");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("662f53f384c41100e9940e6fcab542790bd5f372a1bfaecc87bf523b91bcedab");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260330_dm_r_11_orphan_recovery', () => {
    const migrationDir = path.join(migrationsRoot, "20260330_dm_r_11_orphan_recovery");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("eebe8c6e20360c2212604caea65577b79664d635bf6c1dfb7cf1c2996ff29af1");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("2bf2d3de103ac31ef0c15b7c15a734c01fc3a22450029f85e20d82b65b9329a0");
    });
  });

  describe('20260404112000_notification_retry_policy', () => {
    const migrationDir = path.join(migrationsRoot, "20260404112000_notification_retry_policy");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("981bfa15142cab7d61a321f162d43630dc7a224089433c4da3b9e09762adac81");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260404130000_m365_reconciliation_review', () => {
    const migrationDir = path.join(migrationsRoot, "20260404130000_m365_reconciliation_review");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("4d4760a69223877503bb99062e1e2c3b49eaef7329076bdf0e20d59bb2df9660");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260404143000_radius_reconciliation_review', () => {
    const migrationDir = path.join(migrationsRoot, "20260404143000_radius_reconciliation_review");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("187af46b494e389e03f6985b0516492cf2d056f3e1dd632d8630b5139bc186c9");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260404153000_assignment_optimistic_concurrency', () => {
    const migrationDir = path.join(migrationsRoot, "20260404153000_assignment_optimistic_concurrency");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("14f99313cfca62b1aad144ee3251b1aa5d8625c9231e42818c28e8382c8cf181");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260404165000_project_lifecycle_optimistic_concurrency', () => {
    const migrationDir = path.join(migrationsRoot, "20260404165000_project_lifecycle_optimistic_concurrency");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("e3c27a57a9c4d667ae9880decd137c030483de91006f16d6afff54021400e0f6");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260404190000_add_auth_local_accounts', () => {
    const migrationDir = path.join(migrationsRoot, "20260404190000_add_auth_local_accounts");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("91e3febcb5bea5860da9d958d53551f65f6a5a76174e67b8034209c8ebb79c5d");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_in_app_notifications', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_in_app_notifications");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("7ad4fff30ee4c3302d1de287d110057ded9205f49adeb45f483c8916cf793418");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_period_locks', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_period_locks");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("5983c02a013c7ab19be55cd28fe08b8d6b655a6180b242ad4dc3aeea3934a50c");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_person_cost_rates', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_person_cost_rates");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("502c065629407ae35b08ae3a6b8f5b58f780be0216a6272e87eff7dd90929f4e");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_platform_settings', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_platform_settings");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("802db77142e6169c908cc16b827f4c13090b0d146e863e07b27c038c05fd8e0a");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_project_budgets', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_project_budgets");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("03c04987631a3b93b66b4b8e8bfdcd0c08811a312b0ed6610165744f6da5231a");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_pulse', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_pulse");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("3d5e4a33e7fd63fdbeca44971b41a26d6a8afdda45aa5040e8ad99488ed974a4");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_skills', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_skills");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("7515ec3b9d3048c7063cb7e25ec51fe0325529c7c5b74305fce1ffe061449f56");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_timesheets', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_timesheets");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6bf837a95d91538240e5f467eaaf7efd84e77d1b877d54a518ec65493ba0513e");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260405_work_evidence_capex', () => {
    const migrationDir = path.join(migrationsRoot, "20260405_work_evidence_capex");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6ec58f06178cc7a799ce499a1e6d4a1a75af443ff8acdf7582327a1386627f5a");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260407_staffing_requests', () => {
    const migrationDir = path.join(migrationsRoot, "20260407_staffing_requests");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("ae47f5ab399fbbe39a6b2d41455cf2011a7f6720d6da5efe73c47457d504a85c");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260408_leave_requests', () => {
    const migrationDir = path.join(migrationsRoot, "20260408_leave_requests");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("b15161fc0243d53ff37fbbd26fbc9f1602bf05fa03c4a69c9c368c9b21a2799d");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260411_add_missing_indexes', () => {
    const migrationDir = path.join(migrationsRoot, "20260411_add_missing_indexes");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c225cbd4300abaa0a6c3564a2f7125339dc368dd29cbac4571b9d9b5189114b1");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260416_add_overtime_models', () => {
    const migrationDir = path.join(migrationsRoot, "20260416_add_overtime_models");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("1fdbd8100482028d963f0208aa31aa0dbbc40e4dc1309f9802e79815c7b09d0c");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260416_time_consolidation', () => {
    const migrationDir = path.join(migrationsRoot, "20260416_time_consolidation");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("ecea097036a6c6996ccb5994cc23a7c422bb7b7726ead76c27614f988eb84cef");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260417_add_project_risks_enhanced_rag', () => {
    const migrationDir = path.join(migrationsRoot, "20260417_add_project_risks_enhanced_rag");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("f2cd66f1626b882a5cf546b27b44ff9ed882403befaadf8b44f67b345e3da1f5");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260417_dm3_relation_closure', () => {
    const migrationDir = path.join(migrationsRoot, "20260417_dm3_relation_closure");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c19f11282a6cad01e9152e5489a12c2d3b7d59c5a584cf93cbb538e7aa11894c");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260418_add_planner_scenarios', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_add_planner_scenarios");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("f9dcba2c481bad54ce496c701e84daf9c312453555198c193ab0f597f2308550");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260418_canonical_assignment_status', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_canonical_assignment_status");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("3004f9fe33e910d531e8dd5e155ea3a6d0eb40e2960780ead4abe1affae81688");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260418_dm2_expand_in_app_notifications', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_in_app_notifications");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("3663a646e8d8526925e1165119018cfc067d703b7b722cf526f26552ca770410");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("2f95cd91ddb41abbb8c368c29fe575e150d4699c105561212a62f1594045ca5a");
    });
  });

  describe('20260418_dm2_expand_leave_requests', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_leave_requests");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6ce1730deb017401f29d6a3ce1d0fc239b106dced9347e3526e37512f79e255d");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("da17e53d1bfb661e7da4ecab9f2c7584aa8544d7ed1a948507b9d45b0e37b955");
    });
  });

  describe('20260418_dm2_expand_period_locks', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_period_locks");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("69ddff9dfb96d85d333591bcdda4c51010712dbb71a9edeef3bd6d7a00167f9d");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("22fe7ddfc33079c4bf10950c3a6d55ec18acf305604603c14f01fcb3dcd65cb7");
    });
  });

  describe('20260418_dm2_expand_person_cost_rates', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_person_cost_rates");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("d62695865bc8c8f41c3366e5931131d3cca8075ce036764ed6ba15afb0e87170");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("fb8bd1c37a720cce3bb9614c11a1529d5dd10a691a6b84a5d0b9065fd8a4b712");
    });
  });

  describe('20260418_dm2_expand_person_skills', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_person_skills");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("94f8f9dd127fbf0d265e029645dbc841471082841e2256c9042259f67f845fa9");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("ba5bca8876be6455d3bb0bf4359af90ce49713fec0dd3542c2c32e69dd6a0794");
    });
  });

  describe('20260418_dm2_expand_project_budgets', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_project_budgets");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("053683c9fe0037f3d5b8763258f7648bf7e3c63063f83d862e5cb2f4c0756725");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("ab8eea298ef5335b13bf14e69a2be4d02de11aa352b6f806a16ab0b5ed52e79d");
    });
  });

  describe('20260418_dm2_expand_pulse_entries', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_pulse_entries");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("9a86a6b4913cbbd1294889afc77fe3f62ba3399e8a65d5af33409b49c01788a3");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("163be50de044ccfa9c152bf425caf49b618d63ba25f3379d5f70733de34d547d");
    });
  });

  describe('20260418_dm2_expand_skills', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_skills");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("b26821b581cd1db6d1b43cd22e64d7abb260bbbb639d5cd02583df5cf417bef3");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("a5c16b21e393b5df6557dd247690e5b88086ec3a945449f26c21047a06039696");
    });
  });

  describe('20260418_dm2_expand_staffing_request_fulfilments', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_staffing_request_fulfilments");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("768823d15e5656619b6b5dfb63827d6412d1478d0f2043f630a51641b13cfdb6");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("f998a83d30129e9367395ef55da8b3a2d79b97920f4e39a0a429a744de6acc93");
    });
  });

  describe('20260418_dm2_expand_staffing_requests', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_staffing_requests");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("ca13be535bfc1c9d49a4999cfa20309de298ee6a2f2b5f0cc87500059e51e16d");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("7f1c19d8da009d6d4b6800bb30c271701fd948f8c2c6b96495a030d6ece7079e");
    });
  });

  describe('20260418_dm2_expand_timesheet_entries', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_timesheet_entries");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("cb9464cc74ffa05970efc5339a614ae544458a028bb3a755ce36bfd893fdf0e5");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("5883334b735db29173e0d540b9b41987e609815671778b454ae70f1b9e208bf4");
    });
  });

  describe('20260418_dm2_expand_timesheet_weeks', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm2_expand_timesheet_weeks");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("d3c40bf96922bccff7889303feb36567683c1d2c9703263c9787381b979735cc");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("a504357ac423157040a488e1a7e3bc78b335ef01f4aa6debccf46dad75a87f8c");
    });
  });

  describe('20260418_dm4_check_constraints', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm4_check_constraints");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c1333df97a6f358bf3f048745338b75fed8aeaa7b2254f69338edc906b34ac3b");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("517957012ebe97f1c87494e502e45d8e6c2f19300b8d3bc0dd0a4a8863418b0c");
    });
  });

  describe('20260418_dm_r_20_role_separation', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm_r_20_role_separation");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("d9fb4fc2a3bd2ce8e596f937f3567c3a92e9e011b8db4a950e9ab27810717532");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("1bd8908e814538b577e22e9145475e1438ef0115028f85f07052a8409bf7136c");
    });
  });

  describe('20260418_dm_r_7_migration_audit', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_dm_r_7_migration_audit");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("b4c04611c8c0f28b1eddc4ceaead18cd826cbe67569e41fbeb91181ebe771773");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("1a124a7d15d774958bd8672f25ba6c373bcac7b1f88033c7712f312f3bde60f2");
    });
  });

  describe('20260418_phase21_notif_prefs_and_undo_actions', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_phase21_notif_prefs_and_undo_actions");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("8ad6b32a53532b2190e7b5da1b1aa305625403fce80042ae1854f9ca194f15c9");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260418_project_radiator_v1', () => {
    const migrationDir = path.join(migrationsRoot, "20260418_project_radiator_v1");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("bf110b724b73eb169328c69bc6e0bad692caecd27f597e8f8ccc232c8bfd202d");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260419_dm_r_21_ddl_event_trigger', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_dm_r_21_ddl_event_trigger");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("646a09b07b7f9d9afeaad2020a7dd2dd2c0e393367fec1698baca6a759c20ccc");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("e454316f78671205f9f82f4bfd0770e32e0d49aac1e134f5745038b88ca54434");
    });
  });

  describe('20260419_dm_r_22_hash_chain_audit', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_dm_r_22_hash_chain_audit");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("d52c602898904906fc0f076c9ea18bc6c9368aa7d7f176aec1a1fc39bad92c1a");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("740bf60f6627634258a7cccc04e27336c4a71a06bfcd7e077887773cb2b73a6a");
    });
  });

  describe('20260419_dm_r_22b_chain_seq', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_dm_r_22b_chain_seq");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("f673dcd1fc6ffeced3372463d628fc5e4ea4a7a787e2d2db5531f50aa11861c0");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("b79ac50ede4c6af3c166c9a6f77b860b447fe8fbd89d53c9c3ee9e8ebcaff87e");
    });
  });

  describe('20260419_dm_r_23_mass_mutation_guard', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_dm_r_23_mass_mutation_guard");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("0021fa36896009934f5f3f7823d63b94c92a4539bcae9b6d9311da6a412ae294");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("41d841310954c2213b7c28a3d6709d211b54ecbf8ee9187749549e9d28607a2e");
    });
  });

  describe('20260419_dm_r_31_honeypot_canaries', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_dm_r_31_honeypot_canaries");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("0a56ad0cff176078462d96065d7388edf7f8f906c287ddada049665a05c08975");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("ae26eab290e9cf4a2ce76963f42b53ec67e3d613126ff06e16f59e01c2f2fdcf");
    });
  });

  describe('20260419_dm_r_31b_honeypot_log_only', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_dm_r_31b_honeypot_log_only");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("89b316c2a71950f0a7a89090453f31a03e898aa4c7f5aa74994b8077d6cba740");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("05233cbea76f15ea408746fdc183899552a6ff0306d1eeedc91747670050dc1c");
    });
  });

  describe('20260419_pulse_reports', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_pulse_reports");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("5ed1401515c32d55347f35e82a3a1bddd4176ed7fedbbbc9c340ae0b094ed7d0");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260419_pulse_v2_foundation', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_pulse_v2_foundation");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("b1232624b6de6e67f4be3704045739eec4ddf0ad08714378a893891ca1b21127");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260419_v21_rag_decimal', () => {
    const migrationDir = path.join(migrationsRoot, "20260419_v21_rag_decimal");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("50e02781a0992f07db299c256a886990d2154aee97a3358e6e26d8eeb464ce2c");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260421_dm_4_5_timestamptz_conversion', () => {
    const migrationDir = path.join(migrationsRoot, "20260421_dm_4_5_timestamptz_conversion");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("8c562e597099ff8e11be2e6c0a4e3aa6583be838a6bd7329f6c45efb63554c0e");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260422_dm_4_2_work_evidence_type_enum', () => {
    const migrationDir = path.join(migrationsRoot, "20260422_dm_4_2_work_evidence_type_enum");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6873268e0b775028f9887d2f1429f7e2a655cc1c6d39d6ef63967b718992475b");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("ec753c78c6efc6985135a32a6af55aab7e7253a92fe823867336e9051fe15955");
    });
  });

  describe('20260422_dm_4_3_staffing_allocation_decimal', () => {
    const migrationDir = path.join(migrationsRoot, "20260422_dm_4_3_staffing_allocation_decimal");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("1fad56da107185c3c644943f0da308902a934216a56bf3716e1b8db781808217");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("204e26f34292bcad95d10d46281b60e583b378372fd6ee0fa2405bad2cb74c39");
    });
  });

  describe('20260423_dm_4_2_enum_batch_2', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_4_2_enum_batch_2");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c083743429a43d1931e8117cd0141e2ef6fc8619bb61dc10ec0680081de4fd00");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("0eaad4aac5f207f15a6d057fb87b3d49d38b837431b4e3146b1734737a80ad16");
    });
  });

  describe('20260423_dm_4_2_enum_batch_3', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_4_2_enum_batch_3");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("b530439fa218b27cbbfdef2b385c1e03bf855065876d933de5f5ad614b180bff");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("52199a0655624fda06a41ff7d7918044c6ffd4af240f1230c9c3539ed7020d6a");
    });
  });

  describe('20260423_dm_4_2_enum_batch_4', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_4_2_enum_batch_4");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("ad2614b019574c1172561ac9dbcbc942d1501227e3d8b5f7daf16770d9e9a651");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("37be95917e50bbee21ce1c0eecba67929593f50a469ed59656107056e3dffd4b");
    });
  });

  describe('20260423_dm_4_2_enum_revert_mismatched', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_4_2_enum_revert_mismatched");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c1c742caaced141dda78a4a114791c590be0131e1b4f2a7a141a541b6f321a2e");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260423_dm_4_2b_case_type_overtime', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_4_2b_case_type_overtime");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("84057a8c5208115609a61d1530024c8e1935fc4a2f663c4509958212ae339884");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("08f28cd84e8bfadbe2a3788ba9f392c42520810e9564fbb6a656ce3b304b0269");
    });
  });

  describe('20260423_dm_6a_1_currency', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_6a_1_currency");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("3aec903ca00ee22ad69d4f5a72d2ac4f0ae2158884da8566a526c9a9b9947801");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("bd5cdb798f5a094c1532ea44f691984813113559186b7e9aed392a17bfcdf17a");
    });
  });

  describe('20260423_dm_6a_2_budget_approval', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_6a_2_budget_approval");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c5e6dcb5f14b16dcac4689c8a787d289b4d554bc5123cea38847fc3a177c5353");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("286631bd330c2be05d3d4ffae1893de195a53b61b9b843e9db847915c4351baa");
    });
  });

  describe('20260423_dm_6a_3_dictionaries', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_6a_3_dictionaries");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("9b393f4f1e023bb2ba7dc8246392533fe4ff95977cf735b4a7b15d440d9cb266");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("be4214322dd0ddc29509196029e4ad62e00040e845b456cc4bd4bd17e1e1b8e5");
    });
  });

  describe('20260423_dm_6a_4_6_contact_employment_retro', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_6a_4_6_contact_employment_retro");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("195b713657e7d494911760c4d39c652e78c30605fa8ad18ebfc076e48807ad5f");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("3867319138509a47ea9889e8ac2b4bf365d8f15ed144eb9b4a155bdac5cb0536");
    });
  });

  describe('20260423_dm_6a_7_join_tables', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_6a_7_join_tables");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("59bd949f5288590dfa3c814bad0d832648106519ad526b6a094eb8c59a863a43");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("3a58dde694ae7027ae5a7c21746165df23c358cde37ab645e088c68c7dd05ab9");
    });
  });

  describe('20260423_dm_7_1_domain_event', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_1_domain_event");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("3515360f6cab91e513a775b7cf16b10cd205362ba689afcf6a8324f5ecc40d24");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("487ebcbe11dad54ac283d796b20892a65c4088abce4d0cffdc0a5aa3ebce6781");
    });
  });

  describe('20260423_dm_7_3_outbox_view', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_3_outbox_view");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6a53b89a0a146972fb489a8df45572dbae6fd0b51f009821d19302de6e8d1f67");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("09ed874c4d4ccb2811ef06d8be9939ec1c34c0488248052b0995f5d722d621dc");
    });
  });

  describe('20260423_dm_7_4_employee_activity_view', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_4_employee_activity_view");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c03061d3e929575d48f882d6c52fa91f7bd3c7d1841525a93d429bc17c032a3a");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("0cb8233ff466e04201acad6a9ab1cf8bd10675a4f9107f41d6d2aa492532ca25");
    });
  });

  describe('20260423_dm_7_5_0_tenant_foundation', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_5_0_tenant_foundation");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("9f0c6acdda0f01d1ce2993c765d9a519dce43d03fede9a962b2011207a310e69");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("6b3b4ed7a110f09da30b8013b929d20bdafa6c91df445ea82b5f39d93e361faf");
    });
  });

  describe('20260423_dm_7_5_5_rls_policies', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_5_5_rls_policies");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("71171548dbfe5588cfb38e987d8e4d0ee07a300013dab99e9dc4e78353ccba6e");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("be3abed5ad29b2cd2616624f6bd4457bb2d777ac2bc92c0a9c167671f050fa56");
    });
  });

  describe('20260423_dm_7_5_6a_tenant_unique_flips', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_5_6a_tenant_unique_flips");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("9eb4755bfb25946a0de011c784ea2b442838ab03847ea924b2b5c57af10e21db");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("c74c9aaa84630a5dc969e97ed75a73332e4e386bcb0dc85fc047109a83b90065");
    });
  });

  describe('20260423_dm_7_5_7_skill_tenant_unique', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_5_7_skill_tenant_unique");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("e8e98f1211d03d9b2a20117a210285fcbd5dfb1a3dfd7e3fb7956f696f1581c5");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("3e238bfcf962d0e1252048287a5b5c1ecb14db65113fca0ea8d5dd1adc51522f");
    });
  });

  describe('20260423_dm_7_5_notification_unified', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_5_notification_unified");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("40b0961e5ee80de296bc118f3908d58d9298a5daf111b180075d34f0faf9f8e1");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("f686d1305c53d9ad097568a0902c7b5c002353e74125d619aef68a69ded64983");
    });
  });

  describe('20260423_dm_7_6_aggregate_type_enum', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_7_6_aggregate_type_enum");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6b6ecbd92102011ecc6d45cb09d571eca4aa2dce2f7cedbe8881e4b72f910799");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("a1467f538467caa89019a3557a50c02e8b3e49e8a70f78f2083ea4ab8f819fe4");
    });
  });

  describe('20260423_dm_8_10_capacity_audit', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_10_capacity_audit");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("edf3d47bc9a092a53a3fad88737a9d86f2ab2340935c00da430133ae8aceab50");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("2b367e3a630d36c895ae7f9df768c8bd09e7c495658d5c1cacc753feb985b70d");
    });
  });

  describe('20260423_dm_8_1_version_columns', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_1_version_columns");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("c3bdd4e39d208dc8d159323fb8c569b9c035899095ed19a9dc39d0c0f7c8ed7e");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("4281e359a4427cf07dd6fa39de390b06692477ca857426d7bc247c47c58a2dda");
    });
  });

  describe('20260423_dm_8_3_partial_archived_indexes', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_3_partial_archived_indexes");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("3e7269301891f4cded43876f3ddb08f2e9608d159a932b9dd61f3c79f4896af1");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("bc868c81b365c50ad56b7ac6486d36e4fd135b11462ff1108ebbef9be952dc25");
    });
  });

  describe('20260423_dm_8_4_postgres_roles', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_4_postgres_roles");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("4ed873d1f44abc8426fa6af4a792f204e7fe392458879c82d81dbefe6d39fc4b");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("6c8ad000fc7727fa4e2d1f0eb2da8e9f26df7a4f9b826efe143006c1c4e11b18");
    });
  });

  describe('20260423_dm_8_5_person_rls', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_5_person_rls");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("69341696250445eb3c8b5f961192f29b03262013eb020e72964476f010cbd25b");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("e79cc7d442d03cace525087bb60d9030d68eb2edb9868a3e9e9ae214638ba0d7");
    });
  });

  describe('20260423_dm_8_6_pii_markers', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_6_pii_markers");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("f0c137801e6137ef4d8dc4b66596e8540147aec55155661aeff01f3ffa6acce7");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("5e377a9661c0871410f2c0bfc7cbe77c50c3bcf914cea8191df25e70e03ebac2");
    });
  });

  describe('20260423_dm_8_7_pg_trgm_search', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_7_pg_trgm_search");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("8d76e751e15e3219f2b1d8f4f32e4459fd7429128e4d9fb2ebb0190cbd452e45");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("ea716a17e79b74adb9233337b9ca4b6bb259042928bfd7692637f4e9b34926ab");
    });
  });

  describe('20260423_dm_8_8_materialized_views', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_8_materialized_views");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("07039f73f5eba0b4111593c34ccbce258fb042679222784a646f9e0ef9561911");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("ade6df83571e276776dcc51a0898c26eef9ce5474fd690375ccb608411cf4c4b");
    });
  });

  describe('20260423_dm_8_9_pg_stat_statements', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_8_9_pg_stat_statements");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("bfa584fdc8c0aaa6bc33885747d167d73802fdcc810737b19fd54446fd87b1df");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("39cc956002b93fccd36acf639a7135757e0252f3e6d009f941e46998c4571343");
    });
  });

  describe('20260423_dm_r_22c_rebuild_post_timestamptz', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_r_22c_rebuild_post_timestamptz");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("296eb5a29a35f5c1443e38d053d63a9631ee5c4144bf9c7fca676ee53ea23cfb");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("63bd810ae6fe3320360abe86dabd02ed78a14399aedce7220b3b6fe58266e631");
    });
  });

  describe('20260423_dm_r_22d_rebuild_post_aggregate_enum', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_r_22d_rebuild_post_aggregate_enum");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("2304db409a19cf57e0d335f787faf1be7223120eaf141fa776823f3c4a874c78");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("f3a179b26437dfe4e8b318cdca4b03657a1e894e6c9144fb42a1d1df0bf075fd");
    });
  });

  describe('20260423_dm_r_31c_honeypot_return_new', () => {
    const migrationDir = path.join(migrationsRoot, "20260423_dm_r_31c_honeypot_return_new");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("e072a444ae307a2b97085db2afc960cdd9747fa5607044f9b53c9dbd940473e5");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("17d0683e2255aad683202a43fe19a71c589dc6a3200b0b83b00ac36bdbbbae2e");
    });
  });

  describe('20260429_wo_phase1_slate_and_in_review', () => {
    const migrationDir = path.join(migrationsRoot, "20260429_wo_phase1_slate_and_in_review");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("6eb1a5e10439473ee1513fa6e2f903b8e91ba5338e7942494a3fe3a00a3f2c0b");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260429_wo_phase4_add_draft_status', () => {
    const migrationDir = path.join(migrationsRoot, "20260429_wo_phase4_add_draft_status");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("8df7f9f774d29b4e2b81d43d06b401e03a65abbf57cf6d1fd73474ca0d2573dd");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260430_my_time_work_label', () => {
    const migrationDir = path.join(migrationsRoot, "20260430_my_time_work_label");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("f325703a3ea320e113c36576d2ca92486860f6cc57451c3231768a1ab424b8be");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("3c8977849d6d7ea2733fa30e30e8ffbc417dbf10880bdcaee31fc3a71ce8fb49");
    });
  });

  describe('20260430_staffing_request_proposal_slate', () => {
    const migrationDir = path.join(migrationsRoot, "20260430_staffing_request_proposal_slate");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("90db4058d3593d478a28bd2382be99b655f37a6e4e58d9f3ed54df63e04aef2e");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260502_reconcile_schema_drift_post_dm_r', () => {
    const migrationDir = path.join(migrationsRoot, "20260502_reconcile_schema_drift_post_dm_r");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (FORWARD_ONLY)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("FORWARD_ONLY");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("d3500ec5f7b8dc830be075ced4e063636a8d839c24367272f1c5c3819ac17992");
    });
  
    it('FORWARD_ONLY must not carry rollback.sql (fake rollbacks silently lose data)', () => {
      expect(fs.existsSync(path.join(migrationDir, 'rollback.sql'))).toBe(false);
    });
  });

  describe('20260502_setup_wizard', () => {
    const migrationDir = path.join(migrationsRoot, "20260502_setup_wizard");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("9aac85f5ac4e9e49daa26d3a6bb73ed2f36e3152c57f2676d22819eb3dd2c037");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("4971a76ccd0f49eaa3a5bd5496a97409c109d08a81c23e674dcdef01ce31104a");
    });
  });

  describe('20260502_setup_wizard_grants', () => {
    const migrationDir = path.join(migrationsRoot, "20260502_setup_wizard_grants");
  
    it('migration.sql exists + non-empty', () => {
      const p = path.join(migrationDir, 'migration.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('posture matches frozen classification (REVERSIBLE)', () => {
      const hasReversible = fs.existsSync(path.join(migrationDir, 'REVERSIBLE.md'));
      const hasForwardOnly = fs.existsSync(path.join(migrationDir, 'FORWARD_ONLY.md'));
      const p = hasReversible ? 'REVERSIBLE' : hasForwardOnly ? 'FORWARD_ONLY' : 'UNCLASSIFIED';
      expect(p).toBe("REVERSIBLE");
    });
  
    it('migration.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'migration.sql'))).toBe("7ddb765d0a1773db29bc9c51b554d90c05739c520cc8f8bd6800960de5663907");
    });
  
    it('rollback.sql exists + non-empty (REVERSIBLE)', () => {
      const p = path.join(migrationDir, 'rollback.sql');
      expect(fs.existsSync(p)).toBe(true);
      expect(fs.statSync(p).size).toBeGreaterThan(0);
    });
  
    it('rollback.sql SHA-256 is frozen', () => {
      expect(sha256File(path.join(migrationDir, 'rollback.sql'))).toBe("cc94cffa7e22329770ced9809c679c505da249269118bf1cc31a207d90e9629f");
    });
  });
});
